# Handoff — state as of 2026-08-08 (delegation increment LANDED)

## 🟢 TOM RULED 2026-08-08, late — "fix it, don't document it", and he took the 0.3.0 hold

His words: *"let's make sure that we get everything fixed up rather than just sort of documented
limitations where we can do that"*, then *"you've got the go ahead to publish and push"*, then —
after I recommended holding — *"okay, then yeah, let's keep on going."*

**`main = 415bfdf`, PUSHED and verified by re-fetching.** `origin/main` in sync.

⛔ **`lys-core 0.3.0` REMAINS HELD, now by an explicit ruling rather than by my own caution.** I
recommended holding *against* his clearance and said I would be acting against my own advice if he
wanted it shipped anyway; he agreed to hold. **The argument is his own instruction turned on the
decision: publishing freezes `lys/delegation/v1` while `sequence`'s replay defence is read by no
code, which converts the largest documented limitation we have into a permanent one.** The fold is
one increment, not a phase.

### The ordered plan he approved

### 📍 PROGRESS AGAINST THIS PLAN — read this first

**`main = 6250745`.** Pushed through `415bfdf`; `01469fc`, `3f31227` and `6250745` are committed
locally and **not yet pushed**. Gates at HEAD, verified by me in isolation: **32 suites,
907 all-features / 655 default, 0 failed**, fmt + both clippy shapes + both doc shapes clean.

- ✅ **A1 done** (`01469fc`) — `Ed25519Identity::verify` refuses non-canonical public keys.
  ⛔ **The finding under the finding: the correct predicate already existed but was
  `#[cfg(feature = "unstable-anchor")]`, so the DEFAULT build could not enforce it whatever called
  it.** Third instance in this repo of "correct in one feature shape, absent in the other".
  Narrowing is exactly 38 encodings, 20 previously accepted as keys; **no reachable `Ok` changes**
  (each is a second spelling of a point with `y <= 18`, so verifying under one needs its discrete
  log). Honest limit: deleting the call fails **no** test in either shape — nothing can
  distinguish the implementations without a discrete log — so only negation or over-strictness is
  caught there. dalek's behaviour is now pinned by a test so the rule cannot quietly become dead.
- ✅ **A3 + A4 done** (`6250745`) — `AnchorDelegation` → `Delegation`, and genesis is verified at
  open via `verify_genesis_delegation` (free function) + `Anchor::open_verifying_genesis`.
  Deliberate: **no caller-supplied subject** (the invariant is that leaf 0's subject *is* the
  store's origin; taking it from the caller drops the binding) and **the operational key is not
  checked** (leaf 0 names the key delegated *at genesis*, revocation is an append, so the check
  would forbid rotation while reading as confirming the key). Both pinned by tests.
  ⛔ **Under a parse-only injection the HEADLINE case stayed green** — `b"genesis"` is not COSE, so
  the parse refuses it and verification never runs. Three of four checks could be gone with the
  marquee test passing. **An injection fixture must be well-formed in every respect except the
  rule under test**, or it exercises the wrong layer.
- ⬜ **A2 outstanding** — vectors D and E (below).
- ⬜ **B1 next and largest** — the key-history artifact + the fold.

**A. Close the documented limitations — this is the "fix it" half, and it is now authorised.**

1. **`Ed25519Identity::verify` accepts non-canonically-encoded public keys.** Shipped, ungated,
   semver-bound, which is why it sat as *his* release decision. **The ruling makes it.** Tighten it.
   Note the delegation module already has `is_usable_ed25519_public_key` — the shape to reuse.
2. **Vectors D and E** (§6.1.1): D is genesis, `sequence = 0`, 23-byte subject, `not_before = 23`,
   closing the low side of the inline boundary; E is `not_before = 2⁶³` — **the value §1.2 argues
   the `u64` bound over and tests nowhere.** Both parties, independently, as always.
3. **Rename `AnchorDelegation`** — not a wire name, so it does not freeze with the format, but it
   semver-binds the instant 0.3.0 ships. Same free window that made the content-type rename free.
4. **The DP16 invariant holds only at creation** — `Anchor::open` never inspects leaf 0. Checking
   at open needs a root key argument and, done properly, the fold from B.

**B. Then the sequence, unchanged from his earlier ruling.**

1. **Key-history artifact + the fold.** The one that matters: it makes the ordering rule
   *enforced* rather than described, and lets a stranger learn the operational key **from the log**
   instead of being told. Unblocks 0.3.0. ⚠️ **Every wire format in this crate has had a bug found
   by IMPLEMENTATION rather than review — expect this one to as well, which is the argument for
   building it before anything depends on it.**
2. **Transport** (increment 12, DP24) — one refusal status per substantive refusal, or the
   transport rebuilds the parsing oracle where strangers can reach it.
3. **Private end-to-end run = DP17's "ratify".** First point a production key is a real question;
   **I flagged the key ceremony as one Tom should be present for.**
4. **Publish, with the evidence behind it.**

**C. Unscheduled, and to be decided rather than discovered:** tile-backed / Tessera-compatible
storage (a log anyone can mirror, not one we host) and counter-anchoring to public infrastructure
(so trusting us becomes optional).

---

# Original handoff — mid-flight state, 2026-08-08

Written immediately before a context compaction. Everything here is either verified at source
or explicitly labelled as unverified. **Read `DELEGATION-V1.md` for the format contract.**

## Repo

`main = d51a7b2`, pushed, in sync (verified by re-fetching). Increments 1–11b of `lys-anchor`
are landed. Verified gate state at `d51a7b2`: **32 suites, 872 all-features / 646 default,
0 failed**, all six gates clean in both feature shapes.

Published on crates.io: `lys-core 0.2.0`, `lys 0.2.0`, `lys-log-store 0.2.0` (the last
published today). `lys-anchor` and `lys-anchor-cli` are `publish = false`.
**`lys-core 0.3.0` is deliberately HELD** — see below.

## IN FLIGHT — the delegation payload generalisation. NOT COMMITTED.

The operator ruled that `origin` becomes a **typed subject**, so one format serves an anchor
(subject = a domain) and a future member-with-seats consumer (subject = a seat). `origin` is
gone; the payload is renumbered to six labels. `DELEGATION-V1.md` §0.5 records the ruling, the
three consequences decided rather than asked about, and the admitted cost.

**Uncommitted work in the tree at the time of writing:** the spec revision in
`DELEGATION-V1.md`, plus whatever the implementing agent has written under
`crates/lys-core/src/delegation/` and `crates/lys-anchor/src/anchor/genesis.rs`.
**Gates were NOT re-run after the spec corrections below.** Run all six before trusting
anything.

### ⛔ The enum renumbering — the most important uncommitted decision

The independent encoder found a **freeze-time** defect: with kinds `{domain=1, seat=2}` and
roles `{operational=1, speaks_for=2}`, **every valid pair has `kind == role`**, so an
implementation that wired label 1 to its role field and label 4 to its kind field would emit
**byte-identical output for every valid artifact** and no test or vector could catch it. It
would stay latent until a v2 introduced a differing pair, by which time every v1-era
implementation carried a silent field swap. **Not fixable by choosing a different test
vector** — it is a property of the numbering.

**Decided, and the implementing agent has been told:**

```
subject_kind:  1 = domain,        2 = seat
role:          2 = operational,   3 = speaks_for
valid pairs:   (1,2) and (2,3)     — nothing else decodes
```

Two properties this buys: no valid pair has `kind == role`, so a swap is detectable on *every*
case; and every swap produces an **invalid pair** (`(1,2)`→`(2,1)`, `(2,3)`→`(3,2)`), so it is
refused at decode rather than merely different. **Roles starting at 2 is not arbitrary and
must not be "tidied" back to 1** — that is what makes the fields distinguishable in the bytes.

### The independent vector is STALE and must be regenerated a fourth time

The round-3 vector was computed with `role = 1`. The renumbering to `role = 2` invalidates the
payload, preimage, signature and artifact (the protected header is unaffected — the rename is
already in it). **Do not let either party copy the other's numbers**; the whole value of the
exercise is that the encoder party has never read the Rust. `crates/lys-core/tests/
delegation_vector.rs` should be left stale and failing until a fresh independent vector exists.

Confirmed independently: the new content type `application/vnd.lys.delegation.v1+cbor` is
**38 bytes**, head `7826`, so the protected header is **79 bytes** (was 86).

### Spec defects fixed in this pass (all mine, from revising in haste)

§3.1 pinned the old content type (highest severity — it is the normative home of the
cross-format defence); `sequence` referred to as label 5; "keys ascending 1,2,3,4,5" over a
six-entry map; three `origin` paragraphs never renamed to `subject_value`; §2.1's "for origin
`O`". Also corrected: **a blind search-and-replace rewrote both sides of the rename record**,
destroying the account of what changed — restored, with the old name written in two pieces so
no future replace can eat it.

### ✅ Spec defects — ALL FIXED, and the list was incomplete

The six items this section used to list are done: §2.3 (rewritten as three decode failures),
§2.4 item 3, §0.5 consequence 3, §4's stale signature and field count, §5's unstated genesis
kind, §6.1's regeneration warning. **Do not re-fix them.**

Four more were found by sweeping instead of working from the list, which is the point worth
keeping: **a defect list written while tired is itself a stale document, and it reads as
complete.**

7. **§1.2's `(kind, role)` table still carried the OLD numbering** — `(1,1)` and `(2,2)`. This
   was the highest-severity miss on the list-that-wasn't: the table is the normative home of the
   pair rule, so it contradicted the renumbering *decision* while every prose paragraph around it
   had been updated. Fixed, plus an explicit "there is no `role = 1`" clause.
8. **The renumbering was recorded nowhere Tom could veto it.** §0.5 flagged "three consequences I
   decided rather than asked about"; the enum numbering was a fourth and larger one. Now
   consequence 4, with the encoder's full argument.
9. **The format's prose name had diverged from its wire name.** The content type was renamed to
   `application/vnd.lys.delegation.v1+cbor`, but this document's title and `CLAUDE.md`'s freeze
   list still said `lys/anchor-` + `delegation/v1`. Since `CLAUDE.md` is the authoritative record
   of *which formats have frozen*, a reader comparing it against an artifact could not tell which
   name froze. Renamed to **`lys/delegation/v1`** in the spec title, `CLAUDE.md`, and
   `BUILD-PLAN.md`'s index row and increment heading. Not on the wire, so it cost nothing.
10. **§3.5.1's timing requirement covered two fields and there are now three.** The 32.8×
    measurement was taken over `{kid, origin}`; the typed subject adds a *kind* comparison that
    can leak the same way and has never been measured leaking. The requirement is now stated over
    the whole comparison set, with the gap named: *a measured leak on one field is an argument
    about the shape of the function, not an inventory of its fields.*

**§6.2 gained four injection rows**, all sent to the implementing agent: `role = 1`;
`(kind, role) = (1, 3)` (both halves valid, pair is not — unreachable by either single-field
check); labels `1`/`4` swapped → `(2,1)`, which fails *role decode* before any pair forms; and
`subject_kind` flipped to `2` with the value untouched, which is the cross-*kind* arm of §3.3 and
a different rule from the value arm.

⭐ **The swap row is the only injection in that table whose value is that it can be written at
all.** Every other row tests whether a guard fires; that one tests whether the format leaves room
for a guard to exist. Under roles-from-`1` the swapped artifact would have been byte-identical to
a valid one, so **no injection could have been written** — which is why an injection table cannot
find this defect class and a second party reasoning about the enum space could.

### Literals verified independently, so they need no recomputing

Content type **38 bytes**, head `7826`; protected header **79 bytes** total; `sequence = 300`
encodes `19012c`. The check also reproduced the known-bad `190130 = 304`, so the instrument was
proven rather than assumed — the shell-measurement lesson applied to a hex computation.

## ✅ Round 4 of the two-party vector exercise — what it produced

**Vector A is landed and green** (5 tests). Both parties agreed byte-for-byte on protected,
payload and signature, with a sensitive negative control. **The independent party hand-wrote both
the CBOR and the Ed25519 from RFC 8949 and RFC 8032** — no `ciborium`, no `ed25519-dalek` — and
validated its signer against all five RFC 8032 §7.1 vectors *parsed out of the RFC text* before
computing anything. So the axis is independence of **implementation and of library**, not merely
of author. Still one machine, one toolchain: not platform.

**Vectors B and C computed and agreed**, landing now. B is the seat arm `(2,3)` and reaches the
inline `not_before` case A structurally cannot; C supplies the four-argument-byte integer and the
first `tstr`/`bstr` *lengths* above one argument byte. **D and E are specified in §6.1.1 and NOT
generated** — D is genesis (`sequence = 0`), E is `not_before = 2⁶³`.

### Findings this round, in order of how much they cost to have missed

1. ⛔ **Four normative §1.2 rejections had NO injection row** — `sequence == u64::MAX`, empty
   `subject_value`, the size cap, invalid UTF-8. Both §6.2 amendments before this concerned rules
   guarded *twice*; these were guarded **zero** times. **An injection table is a list of what
   somebody thought of, so it can never be audited against itself** — the audit must run from the
   rules inward.
2. ⛔ **§6.2's §3.2 and §3.3 rows described byte-tampering, which cannot model either attack.**
   Flip a byte in `kid` and the signature breaks too, so §3.6 fires as well — and §3.2's premise
   is that the artifact is *cryptographically perfect*. Both injections must be **re-signed**.
   Found independently by both parties.
3. ⛔ **The 4096 cap's referent was ambiguous and wire-visible** — "artifact" or "`subject_value`"
   disagree over **211 lengths** (3886–4096). Now pinned to the artifact, on both encode and
   decode, with the derived maximum stated as the number **3885**.
4. **Two unspecified well-formedness questions**, both constructible: invalid UTF-8 in
   `subject_value` (now required valid), and whether label 3 must be a valid curve point (now
   explicitly **not** validated — because the only available check is dalek's, which reduces `y`
   mod `p`, so the rule would have stated a guarantee stronger than its check).
5. **I claimed A+B covered every producible head width. False** — three were missing, and the
   `uint` four-argument-byte gap was the one a broken branch could hide in. ⭐ **Claiming coverage
   immediately after closing one hole is what lets the remaining holes read as handled.**
6. **The width axis is now complete, and complete because it is COARSE.** Width is a property of
   a range; shortest-form is a property of a **boundary**. A+B+C cross one of ten boundaries, from
   above only. The sharpest: B pins `sequence = 24` while the realistic off-by-one emits `1817`
   for 23 — **the gap is inside the boundary B was written to pin.**
7. **`the_frozen_literals_are_self_consistent` had a second party that went stale in lockstep.**
   Its lengths and offsets were genuine §6.1 facts, but nothing forced them to move when the
   format did. ⭐ **A second party that can go stale in lockstep with what it judges is not a
   second party.** Replaced with a cursor walk in which no assertion is a bare number describing
   the hex, verified by failing on the stale vector at the content type.
8. **The go-cose gate was confirmed a real second party BY INJECTION** — Rust `CONTENT_TYPE`
   changed to `v2` at the *same length*, Go file untouched, both gates failed on Go's own check.
9. **B's `subject_value` length moved 31 → 32 → 33 → 32**, each candidate colliding at a
   different nesting level. Settled at 32: the payload-level tie with the key is guarded by
   **major-type pinning** and buys adjacent `7820`/`5820` coverage, where the 31-byte envelope tie
   was guarded by nothing. ⭐ **For each collision, name what else separates the two values — if
   the answer is nothing, change one.**

### Verified gate state after landing A, B and C

**32 suites, 886 all-features / 646 default, 0 failed**; `fmt` clean, both clippy shapes zero
diagnostics, both `cargo doc` shapes zero warnings. **Run by me, not taken from the agent's
report** — the numbers matched, which is the point of checking rather than a reason not to.

Each vector's purpose is now *demonstrated* rather than asserted, by one injection apiece:
forcing `not_before` to always emit the eight-argument-byte head fails **B alone**; breaking the
four-argument-byte branch fails **C alone**. Note the honest limit the implementer volunteered:
both injections fail the *reproduction* tests, **not** the cursor walk — the walk judges the
literals against the spec table, the reproduction tests judge the code against the literals.
Anyone reading "shortest-form is checked here" should know which test does the checking.

Also added, beyond the brief and worth keeping: `no_vector_ties_the_protected_and_payload_bstr_heads`
turns the 31-vs-32 finding into a guard, so a future edit to B's subject value cannot silently
restore the envelope tie and force the reasoning to be rediscovered.

### Still owed on the vector set

- **D and E are specified in §6.1.1 and not generated.** D closes the low side of the inline
  boundary and pins genesis's payload; E is `not_before = 2⁶³`, the value §1.2 argues the `u64`
  bound over and tests nowhere. **Vectors are not frozen** — these can land after the format does.
- **Ten shortest-form boundaries, one touched, from above only.** Four each in `not_before` and
  `sequence` (23|24, 255|256, 65535|65536, 2³²−1|2³²) plus two in the `subject_value` length; the
  third is unreachable under the 4096 cap, and both closed enums are inline-only so contribute
  none.
- **`AnchorDelegation` is still named for anchors** though the format now serves seats. Not a wire
  name, so it does not freeze with the format — but it is a public item that semver-binds the
  moment `lys-core 0.3.0` ships, which is exactly the window that made the content-type rename
  free. Same argument, same window, not yet taken.

## ✅ THE ADVERSARIAL REVIEW RAN — outcome

**Cleared:** forgery, signature malleability, cross-format confusion, cross-kind confusion,
pair-check bypass, encode/decode divergence through every issuing entry point, and timing on all
three configured comparisons. Every forged artifact was **validly signed over a preimage the
reviewer's own independent encoder built**, so each refusal is a refusal of a cryptographically
perfect artifact rather than of a broken one — and the encoder reproduced all three golden
vectors byte-for-byte as its positive control.

Highlights: a 25-cell hand-signed `(kind, role)` matrix — only `(1,2)` and `(2,3)` accepted, in
**both** entry points, so an invalid pair cannot reach a consumer through the parse-only route.
The cap boundary is exactly as specified: 3885 accepted, 3886 refused, both sides. And the §3.3
cross-kind attack was built for real — a valid seat delegation whose seat identifier is literally
`example.test`, signed by the trusted root key — and refused by a domain verifier while a
value-only verifier would have accepted it. **The kind argument is doing real work.**

### ⛔ F1 — the worst finding was MINE, and it was written today

§1.2 said the delegated key is **not** validated as a curve point. **The shipped decoder validates
it**, and the code doing so was already committed. My justification — "the only available
validation is dalek's, which reduces `y` mod `p`" — was false: `is_usable_ed25519_public_key`
does an explicit canonical-`y` check *precisely because* dalek accepts `[0xff; 32]`.

Three lessons, and the third is the transferable one:

1. **A real finding was generalised past its subject.** `Ed25519Identity::verify` does accept
   non-canonical keys — measured, recorded, still open. Different function. "dalek is the only
   available validation" was inferred, never checked.
2. **The direction was the dangerous one**: a stranger implementing the text would **accept** what
   lys **refuses**. The sibling half of the same subsection (invalid UTF-8) matched the code;
   only this half was backwards.
3. ⭐ **A specification asserting what the code does, written without reading the code, is a
   second party that has quietly become a first one.** This document's authority is that it was
   written *before* the implementation and can disagree with it. Describing shipped behaviour from
   memory keeps that authority in the tone and loses it in substance — and it is worse than an
   ordinary error, because it is normative on the wire and no reader has cause to check it.

**Resolved by making validation normative for BOTH key slots** (F2 found `kid` unvalidated while
label 3 was, with `encoding.rs`'s stated invariant claiming otherwise — `[0xff; 32]` occupies the
slot it says cannot be occupied). Fixing the code rather than the comment.

### Other findings, all in hand

- **F1a — the timing test had no `subject_kind` arm.** The one field the spec flagged as "never
  measured leaking" was guarded by nothing; an early return there fires only for a case no arm
  produces. Measured clean by the reviewer (1.003× against an 8.57× positive control), now getting
  its arm.
- **F1b — the counter that makes an early return visible is `#[cfg(test)]`.** It gives signature
  verification an observable side effect *in test builds only*; in the shipped build nothing
  structurally forbids the optimiser sinking the call under a branch. **The instrument guarantees
  the property only in the build where the instrument exists.** Recorded in §3.5.1.
- **F3 — genesis permits the root key and the operational key to be the same key.** DP16's whole
  reason for two keys, silently void, at the one leaf that can never be corrected, with nothing to
  flag it. Same argument that fixed the subject kind in the constructor. Being fixed there.
- **F4 — DP17 confirmed intact**: nothing in either CLI crate reaches the delegated-genesis
  constructor.

## The adversarial review — the brief it ran against, kept for the next one

CLAUDE.md requires an adversarial review for cryptographic changes, and the last one found the
replay defect, so this is not a formality. Written down in advance so the review cannot be
graded against whatever it happens to find.

**The generalisation added exactly three things to attack**, and each has a specific question:

1. **The typed subject widened the comparison set from `{kid, origin}` to
   `{kid, subject_kind, subject_value}`.** The 32.8× timing leak was measured over two arms and
   the third has never been instrumented. Attack: does an early return on the *kind* leak, and is
   the `&`-combination actually preserved after the refactor? A behavioural test cannot see this
   — the previous round needed a thread-local counter, and a first injection placed *after* the
   signature check "passed" while proving nothing (phantom resolution).
2. **The `(kind, role)` pair check is new logic on the decode path.** Attack: is the pair
   validated *before* any field is used, or can an invalid pair reach a consumer? Is `(2,2)`
   refused as firmly as `(1,3)`? Does the pair check run on the *decode* path rather than only in
   `verify_delegation`, so `from_cose_bytes` cannot hand out an invalid pair to a parse-only
   caller?
3. **`sequence`'s partition gained the kind.** Attack: is there any path where a domain and a seat
   with colliding identifier strings share a counter? This is enforced nowhere yet — the fold does
   not exist — so the honest answer may be "prose only", and the review should say so rather than
   accept the spec's word.

**Do not let the review re-derive the enum finding as though it were new** — it is already fixed
and recorded in §0.5 consequence 4. Its job is the three items above plus whatever it finds
unprompted.

⚠️ **And the standing warning: every wire format in this crate has had a bug found by
IMPLEMENTATION rather than by review, including this one.** A clean review is therefore weak
evidence on its own; the private end-to-end run is what has actually caught things.

## Still owed / open

- **Adversarial review of the generalised format before it lands** (CLAUDE.md requires it for
  cryptographic changes, and the last one found the replay defect).
- **`Ed25519Identity::verify` accepts non-canonically-encoded public keys** — dalek reduces y
  mod p rather than rejecting `y >= p`; `is_weak()` is false for all-`0xff`. Measured. Shipped,
  ungated, semver-bound: tightening it is the operator's release decision, not taken.
- **The DP16 invariant holds only at creation** — `Anchor::open` never inspects leaf 0, so a
  store created by a default build and later opened by an all-features build is a non-DP16
  anchor and nothing flags it.
- **DP17 exposure is one flag wide**: nothing in `lys-anchor-cli` reaches the delegated-genesis
  constructor, so the moment `anchor init` grows a `--root-key`, DP17 is violated by a command.
- **After this: the key-history artifact + the fold** (this is what finally *enforces*
  `sequence`, and what unblocks publishing `lys-core 0.3.0`), then transport, then a private
  end-to-end run — which is DP17's "ratify" — then publication with evidence behind it.
- Two ROADMAP Phase-4 targets are unbuilt and unscheduled: **tile-backed/Tessera-compatible
  storage** (a log anyone can mirror rather than one you host) and **counter-anchoring to
  public infrastructure**.

## Alignment with manifold — asked, unanswered

manifold **needs nothing and set no deadline**; its current behaviour is already honest. The
generalisation stands on lys's own merits plus one operator-stated future consumer.

The one open question that could still force a v2: **what a seat identifier actually looks
like** — `subject_value` is about to freeze and manifold has not specified its shape. The spec
now states that a subject value is scoped by the **authority key** rather than by a global
namespace, that `(authority key, subject value)` is the pair that must be unique, and that lys
**defines no structure for the value and will not parse one** — a delimiter inside a signed
identifier would freeze a parsing decision alongside it.

---

## 🔄 IN FLIGHT ACROSS A COMPACTION — a Workflow is running, do NOT duplicate its work

Launched 2026-08-08 at Tom's own suggestion, so work continues through the compaction.

- **Run ID `wf_491bb3ad-508`** · task `wb7qnjsim`
- Script: `~/.claude/projects/-Users-tom-Developer-ablative-stack-lys/a351a251-26f9-4749-9143-5b1919eb0f63/workflows/scripts/lys-vectors-de-and-fold-design-wf_491bb3ad-508.js`
- Transcripts + `journal.jsonl` (read this before concluding a result was empty):
  `~/.claude/projects/-Users-tom-Developer-ablative-stack-lys/a351a251-26f9-4749-9143-5b1919eb0f63/subagents/workflows/wf_491bb3ad-508`
- Resume after editing: `Workflow({scriptPath: <above>, resumeFromRunId: "wf_491bb3ad-508"})`

**What it is doing, in two independent tracks:**

1. **Vectors D and E** — two parties who cannot see each other (one forbidden to read `crates/`
   at all), then a byte-for-byte reconciliation *with a negative control on the comparison
   itself*. E is `not_before = 2⁶³`, the value §1.2 argues the `u64` bound over and has never
   tested. D is genesis (`sequence = 0`) plus the **low** side of the inline boundary — the
   realistic off-by-one direction, which no existing vector touches.
   ⚠️ **If either party reports that the crate REFUSES an input, that is a finding, not a
   fixture to adjust.**
2. **The fold design** — three designs from different lenses (minimal / adversarial / stranger
   reimplementing from the document alone), each adversarially critiqued, then synthesised.
   The synthesis is instructed to separate its recommendation from **the decisions that are
   Tom's**: anything freezing a format, changing what a verifier must trust, or affecting
   publication timing.

**On receiving it:** verify at source, never on report. Vectors land in
`crates/lys-core/tests/delegation_vector.rs` **only after both parties agree**. The design is a
recommendation — the fold is the thing that unblocks `lys-core 0.3.0`, so its format decisions
carry freeze weight and go to Tom.

## ⚠️ Standing correction from Tom, 2026-08-08 — do not idle

*"You've been stopped for hours when you could have kept on going."* I had been ending turns to
**report** rather than to continue, treating a completed increment as a handover when nothing was
blocked. **Ending a turn is for when something is needed from Tom, or the work is finished.**
Before ending any turn, ask whether there is an unblocked authorised next step — and if there is,
take it. Long silence while working is fine; silence while idle is not. Irreversible acts
(publishing, production keys) still stop and ask.

---

# 📅 2026-08-09 — continuation. Read this section first; it supersedes conflicts above.

## Landed today (local commits; see "pending" below for push state)

| commit | what |
|---|---|
| `58a8ab7` | `KEY-HISTORY-FOLD-QUESTIONS.md` — the fold's review rubric, written **before** any design arrived |
| `ea96f17` | Four stale blockers in `BUILD-PLAN.md` — F1–F4 were ruled on a day earlier and the plan never noticed |
| `f3a3409` | `attestation::verify_attestation_by_signer` — the attributing verifier |
| `191e8e6` | `DELEGATION-V1.md` — the derived-maximum correction and the D/F split |

## ⛔ The most important finding of the day: §1.2's derived maximum is not a constant

`not_before_unix_ms` and `sequence` are `u64` shortest-form, so each is 1–9 bytes. The maximum
`subject_value` therefore **moves with them**: 3885 for vector A's values, **3895** for both
inline, **3879** for both near `u64::MAX`. The spec published `3885` as a law and called every
other field fixed-width. Sixteen lengths on which conforming implementations disagree, in both
directions.

**`lys-core` is correct** — `check_encodable` derives the bound by encoding and measuring, and
the boundary test keys on the derived value. Only the prose was wrong, and **an independent
party read the prose, hard-coded 3885, and instantiated the predicted defect inside the same
exercise that predicted it.**

⭐ Replacing a derivation with a constant is only safe once you have checked the quantity is
invariant. That check was asserted in the same breath by the same author, with nothing able to
disagree.

## 🔨 Rulings I made today (all vetoable, all recorded in the spec)

1. **Vector D splits into D and F.** `23` is the only value at the top of the CBOR inline range,
   so the `tstr` and `uint` boundaries both want it and neither can move — the artifact moves
   instead. One boundary each, no two fields in either vector sharing a value.
2. **D's `subject_value` is `twenty-three-bytes.test`.** It was going to be `lys-seat-…` under
   `subject_kind = 1 (domain)` — a seat-shaped string in a domain slot, the confusable pairing
   §3.3 exists to prevent, in the vector most likely to be copy-pasted. **That was my
   correction to an earlier error of mine**, and both parties caught it independently.
3. **E's bytes stand** (both parties agreed) **but E does not measure its stated claim.** An
   `i64` implementation cannot represent 2⁶³, so it can never be asked to encode E. The
   discriminating test is a decoder property and is specified in §6.1.1; no golden vector can
   contain it.
4. **2⁶³ removed from the head-*width* gap table** — `1b7fff…` and `1b8000…` are both eight
   argument bytes. It is a type-modelling boundary and was making that axis look covered.

## 🔄 IN FLIGHT right now — do not duplicate

- **Two independent parties recomputing D and F** (one forbidden to read `crates/`). E is
  already agreed and does not need recomputation.
- **Workflow `wf_491bb3ad-508`** — fold designs and critiques; synthesis pending.

⚠️ **A workflow agent wrote `zz_tmp_size1.rs` into `crates/lys-core/tests/` and `git add -A`
would have committed it.** Removed. **Check `git status --porcelain` before every commit while
agents are running**, and tell every dispatched agent to write only to the scratchpad.

## ✅ A2 IS DONE — vectors D and F landed in `f73d7d0`

Both computed by two independent parties and agreeing byte for byte; `lys-core`
reproduces both. **E is deliberately NOT frozen** — its bytes are agreed, and it cannot measure
the claim it exists for, because an `i64` implementation can never be asked to encode 2⁶³. The
discriminating test is a decoder property and no golden vector can hold it.

⚠️ **Adding D and F changed no test count** (918/666 before and after) — they are iterated
inside the existing six tests. The `assert_eq!(checked, 5)` in each is the only thing
distinguishing "two vectors added" from "two vectors added and never walked".

## Pending, in order

1. ~~Land D and F~~ ✅ done.
2. **B1 — the key-history artifact and the fold.** Judge the workflow's designs against
   `KEY-HISTORY-FOLD-QUESTIONS.md`, which was deliberately written first so the rubric is not
   shaped by the submissions. Q1 (what is current when the highest sequence is not yet
   effective) changes what a signed delegation *means* and is **Tom's**, not a designer's.
3. Push. Then transport (increment 12, unblocked but deliberately not next).

## Gate state

At `f3a3409`, run by me in isolation: fmt clean; both clippy shapes 0; **32 suites, 915
all-features / 663 default, 0 failed**; both `cargo doc` shapes 0 warnings, forced uncached and
timed at 3.6s so the result is a measurement rather than a cache. The four docs commits since
have not been re-gated — **run all six before pushing.**
