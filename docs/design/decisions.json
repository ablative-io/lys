{
  "project": "lys",
  "updated": "2026-09-26",
  "decisions": [
    {
      "id": "ADR-001",
      "title": "Secrets are held behind a handle the door swaps for the credential",
      "status": "decided",
      "scope": "secrets",
      "date": "2026-09-22",
      "decided_by": "Tom",
      "context": "Agents need to use credentials without ever holding them, and revoking access must be instant even for a static key.",
      "decision": "A seat holds a short-lived handle bound to its identity. The real credential sits in the door's encrypted store and never leaves the server. The door's proxy checks SpiceDB, swaps the handle for the credential, forwards the call and writes one audit line. Built in Rust inside the door; no OpenBao unless credentials minted on demand are later needed.",
      "quote": "use something with the service, like a temporary key, and that is then mapped so the actual credential can be used.",
      "consequences": [
        "Revoking is dropping the handle, instant for a static key too.",
        "The real key is rotated upstream only when it is suspected leaked.",
        "A key that cannot rotate is still used through the proxy and never read."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-002",
      "title": "The token revolver is the first consumer of the handle",
      "status": "decided",
      "scope": "secrets",
      "date": "2026-09-22",
      "decided_by": "Tom",
      "context": "The workers that run Claude sessions and builders walk their own ordered list of accounts when a session prints its usage-limit words.",
      "decision": "The worker asks the broker for its next account instead of walking its own list; the store keeps the set of real accounts behind one handle and the proxy takes the next in turn and logs which one served each call. This replaces the account pool file.",
      "quote": "that's exactly the kind of thing that we'd want to plug into this, to be able to draw in from there",
      "consequences": [
        "Usage is attributed in one place.",
        "Resting an account is a change in the store, with no file copied to any machine."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-003",
      "title": "Everything is pegged to a human authority",
      "status": "decided",
      "scope": "identity",
      "date": "2026-09-22",
      "decided_by": "Tom",
      "context": "Agents are provisioned under people and act with delegated access.",
      "decision": "A person signs in first; an agent is provisioned under that person with its own identity; the person's permissions are the ceiling and the agent holds an explicit subset; every grant says who may exercise it and who may pass it on; withdrawing the authority stops every grant derived from it. The exact delegation schema is not settled by this decision.",
      "quote": "Basically everything needs to be pegged against the human's base permissions.",
      "consequences": [
        "A handle is issued only under a grant that traces to a person."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-004",
      "title": "Manifold is optional and every project stands alone",
      "status": "decided",
      "scope": "project",
      "date": "2026-09-22",
      "decided_by": "Tom",
      "context": "The statement names seats being started and ended.",
      "decision": "The engine that starts or ends a seat is whichever one runs the agent: manifold, aion, or a customer's own. Each project in the stack works without the others; an engine without the broker reads its own pool file as it does today.",
      "quote": "It's in the stack as an optional execution engine, but it's not mandatory... I don't want anybody really planning it as a structural cog.",
      "consequences": [
        "No broker requirement depends on manifold being present."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-005",
      "title": "The identity database is PostgreSQL, possibly on a network device",
      "status": "decided",
      "scope": "identity",
      "date": "2026-09-23",
      "decided_by": "Tom",
      "context": "At 14:08 in the Dot room Tom said he had hoped to get rid of PostgreSQL, and asked whether the team thought it was the best solution. The working team decision was one PostgreSQL service and one database for Rauthy and SpiceDB, with separate roles and schema namespaces.",
      "decision": "PostgreSQL is used for the identity product's database. It may be set up on one of the network devices rather than on Tom's Mac.",
      "quote": "Yeah, so we can use Postgres, that's fine. We might just set it up on one of the network devices so it doesn't have to run here.",
      "consequences": [
        "Row 02's one PostgreSQL service and database stands.",
        "The host is not assumed to be Tom's Mac; the deployment names its database address as configuration."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-006",
      "title": "A role changes only by a deliberate, explicit act",
      "status": "decided",
      "scope": "identity",
      "date": "2026-09-23",
      "decided_by": "Tom",
      "context": "Version 4 of the identity mock-up gave roles a builder. At 18:54 on 23 September the room asked Tom whether editing a role changes every holder at once or leaves each holder with the copy made when they got it.",
      "decision": "Editing a role makes a new version of it, and a holder never changes version silently as a side effect of the edit. A holder moves between versions either by a deliberate, recorded act or by an explicit rule shown in advance to the person the holder answers to, such as a move at the holding's next renewal. Which of the two is the default is open. A provisional role is a role held under a grant with an end date: when the date passes the holding lapses, and nothing renews it unless someone grants it again.",
      "quote": "whichever way you went, you'd want it to be deliberate, … and if not deliberate, then explicit … their old lease expires and they don't get a new one",
      "consequences": [
        "The role screen shows which version each holder is on, and who still holds an older one.",
        "A holding's end date belongs to its grant, not to the role version: a version change never extends a provisional holding.",
        "A lapsed provisional holding leaves the audit record of its end, and the grants it carried stop at the next check.",
        "Whether a role is a live group or a template copied at grant time stays open."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-007",
      "title": "The product starts an agent by giving its start command, never by running it",
      "status": "decided",
      "scope": "identity",
      "date": "2026-09-23",
      "decided_by": "Tom",
      "context": "On 22 September Tom settled that the product owns an agent's lifecycle as its record and decision, with a runtime carrying it out (ADR-004). On 23 September at 18:56 he asked how a person actually starts a new agent from it.",
      "decision": "The product is not an execution engine. For the first release an agent's file gives the command that starts it on a chosen machine: the command carries the agent's identity and its handles, never a credential's value, and it is rendered from the agent's kept launch record. A started agent reports back, so the sessions screen shows what is running. A terminal inside the product, sandboxes, virtual machines and containers are later runtimes that plug in, and none is built into this product.",
      "quote": "I don't want this to become like an execution kind of engine … in the first instance, it's starting out with basically like, cool, here's the command that you need to start Waffles",
      "consequences": [
        "Every agent carries a launch record: the machine, the runtime, the program, its arguments, the working directory and the handles it needs, by name.",
        "A session is shown running only after the agent reports its start, signed with its own key and naming its launch record; a report that is not authenticated as that identity and that launch record is refused. Copying the command is not a start.",
        "A session that never reports stays unconfirmed."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-008",
      "title": "An agent's file shows its lys certificate",
      "status": "decided",
      "scope": "identity",
      "date": "2026-09-23",
      "decided_by": "Tom",
      "context": "Reviewing version 4 of the identity mock-up at 18:55 on 23 September, Tom asked that the lys certificate and receipt work be part of the product.",
      "decision": "An agent's file shows its lys certificate once one is issued: what it claims, who signed it, when it was issued and when it expires, with the signed receipts of the changes made to it. An agent registered before any key or proof of possession was supplied shows its certificate as not issued, never a placeholder.",
      "quote": "just making sure that sort of like the original lys certificating stuff is in there",
      "consequences": [
        "A change to its grants or role version issues a new certificate; the old one is withdrawn in the log, never edited.",
        "Anyone can verify the certificate and its inclusion in the log offline with standard tools.",
        "Production keys and receipts outside tests remain Tom's acts.",
        "The conformance list checks that a registered agent's certificate, or its not-issued state, and its receipts can be read from its file."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-009",
      "title": "People sign in through a maintained Rauthy fork of our own",
      "status": "decided",
      "scope": "identity",
      "date": "2026-09-22",
      "decided_by": "Tom (the fork, 14:35 to 14:42); Waffles (its branch and upgrade rule, 15:30:50)",
      "context": "Rauthy v0.36.2 refuses a second provider for a user ('user is already federated'), so linking Google and GitHub to one person needs a change Rauthy upstream is unlikely to accept. Sources: docs/design/identity/STATEMENT-2026-09-22.md:191 (Decided 14:35, Tom); IDENTITY-001 authority (docs/design/identity/briefs/IDENTITY-001.json:10, Tom 14:35 to 14:42; docs/design/identity/briefs/IDENTITY-001.json:17, Waffles 15:30:50) and review_decisions (docs/design/identity/briefs/IDENTITY-001.json:35).",
      "decision": "Rauthy authenticates people, and its one-provider-per-user limit is changed in a fork we maintain, ablative-io/rauthy, not contributed upstream as a prerequisite. The maintained branch is ablative, created from upstream v0.36.2 commit dd61ac3c84d6b238108dc8438b53043b5177a662; the fork's main stays an untouched upstream mirror; lys pins an exact commit of ablative as the submodule vendor/rauthy. Upgrades rebase ablative onto upstream release tags only, each in its own gated row; no cherry-picks and no reset of main.",
      "quote": "The platform keeps its own maintained fork of Rauthy rather than depending on an upstream contribution.",
      "consequences": [
        "The fork carries its security fixes for as long as it lives: its maintenance owner is Chippy, with Waffles reviewing security and update decisions (docs/design/identity/RAUTHY-BASELINE.md:39), and the release-and-advisory check recurs before every install (docs/design/identity/STATEMENT-2026-09-22.md:177).",
        "Real sign-in waits on a rebase onto an upstream release carrying both hardening commits, the separate future brief IDENTITY-002 (docs/design/identity/briefs/IDENTITY-001.json:462-473).",
        "A change inside the fork belongs to the fork's own repository: a brief of this repository names such a file with its owner and never lists it as its own file."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-010",
      "title": "Every product shares one design and keeps its own accent; the identity product's is orange",
      "status": "decided",
      "scope": "identity",
      "date": "2026-09-22",
      "decided_by": "Tom (15:18, 15:20 and 15:21:43), relayed by Waffles (15:19 and 15:21)",
      "context": "Tom asked for a consistent design across the products and named Aion's browser appearance as the one he is happiest with; Cambium is not yet the reference. Sources: docs/design/identity/STATEMENT-2026-09-22.md:194; IDENTITY-001 authority (docs/design/identity/briefs/IDENTITY-001.json:14-15) and review_decisions (docs/design/identity/briefs/IDENTITY-001.json:43-44).",
      "decision": "The identity screens follow Aion's structure, typography, spacing and interaction, and Rauthy's client themes take the same colours, with no build dependency on Cambium or Aion. Each product keeps its own accent within the estate colour family: Cambium green, Aion blue and black, Argus light blue, Haematite mustard. The identity product's accent is orange (accent #D4975A, deep #A86B2E, wash #3D2A17 in the estate colour tokens), set apart from Manifold's copper. No product is silently made Aion-blue, and purple is not used.",
      "quote": "Every product shares one design and keeps its own accent.",
      "consequences": [
        "Rauthy's pages carry our colours and corner radius only; their fonts and layout stay upstream's (docs/design/identity/STATEMENT-2026-09-22.md:191).",
        "Exact accessible light and dark accent values are declared and reviewed before a theme is accepted.",
        "Whether one design system is extracted for every product is not settled by this decision."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-011",
      "title": "An identity is registered, active, suspended or retired",
      "status": "proposed",
      "scope": "identity",
      "date": "2026-09-22",
      "decided_by": "Archie, for the working team (proposal of 17:10); not ruled by Tom",
      "context": "Tom, 17:08, put identity, access, permissions and the start of lifecycle management before everything else (docs/design/identity/STATEMENT-2026-09-22.md:183). Archie proposed these states so the directory has something to register an identity into; the document calls itself 'a proposal for the room, not a settled design' (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:1-8).",
      "decision": "An identity is in one of four states: registered (exists in the directory, no grants, no credential handle, may not act), active (may act within its grants), suspended (kept whole, grants kept but not effective) and retired (permanent, history kept, never reactivated; a new identity is made instead). Register, activate, suspend, reinstate and retire are the only transitions, each one signed audit record naming the authenticated actor and their provenance, the identity, from, to, when and reason. Having a grant or a credential is a fact beside the state, not a state. A person is registered by first sign-in; an agent is registered by a signed-in person, who carries it as its responsible person for life and may cause every transition of their own agents. Source: docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:17-44 and docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:71-95.",
      "quote": "We got to talk about identity access management, permissions, and the start of lifecycle management before we get to the nitty gritty of the lifecycle that we're actually managing",
      "consequences": [
        "DIRECTORY-003 records the state beside each identity; no check reads it until the permission work of road step 2.",
        "The room's own questions stay with the proposal: whether a reason is mandatory on suspend and retire, and whether activation needs a credential handle (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:101-103)."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-012",
      "title": "A harness launch template is kept in the home by hash, and each render is recorded on the session beside its context path",
      "status": "decided",
      "scope": "home",
      "date": "2026-09-26",
      "decided_by": "Tom's ruling of 22 September 2026 that the home keeps a preset template per harness (relayed at docs/design/identity/STATEMENT-2026-09-22.md line 181), with the card lead's answers on HOME-002 of 26 September 2026",
      "context": "The home holds the record, the canon and the handover, but nothing turns them into a running session on a harness. The record's lys.harness_event kinds were closed at five (RECORD.md), the home had only sessions/ and blocks/, and appending an entry advances the head, so recording a render on the session would have moved the very head a second render hashes.",
      "decision": "A launch template per harness is a JSON object with named slots (transcript, mcp, env, secrets, instructions) plus flags, stored in the home under templates/ by its SHA-256; lys-home renders a template and a session into files and runtime variables with command mappings in text, prints the launch line and never runs it, and records each render as a sixth lys.harness_event kind, template_render, hung as a side leaf beside the context path with the written paths in a manifest block named by hash. Rejected: a transcript converter or adapter protocol per harness, a template kept outside the home (a seat document of another tool), and a render event that advances the head, which would change the session head hash between two renders of the same session.",
      "quote": "",
      "consequences": [
        "RECORD.md's list of lys.harness_event kinds grows to six; the five existing kinds and the 512-byte data cap do not change.",
        "The session gains an append that does not move the head and a head hash: the SHA-256 of the head entry's line.",
        "Claude Code's transcript slot is filled by resuming the rendered file by path with --fork-session, so the rendered file is never written after the render.",
        "A use-only secret is only ever its handle; a readable secret is refused until the secrets rows build the broker reader, whose command mapping the template carries as text.",
        "Each further harness is its own template and its own measurement; manifold, the door and aion may consume the printed line and none is a dependency (ADR-004)."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-013",
      "title": "The context record is a lys.given custom entry of document hashes, never copies",
      "status": "proposed",
      "scope": "home",
      "date": "",
      "decided_by": "",
      "context": "Step 4 of the identity platform is what a session was given, not only what it did. When the launch template renders a Claude Code session, nothing records which instruction documents that session was given, in what versions or order. Stage 6 will later sign over whatever shape this record takes, and the repository's wire-format rule freezes a shape once anything is signed under it, so the shape is decided on purpose now rather than left to the first implementation (docs/design/home/briefs/HOME-003.json).",
      "decision": "The context record is one lys.given custom entry, appended after the render event, whose data is the harness name, the Claude Code version the load order was measured on, the kinds as two lists, resolved (claude_md_chain, user_claude_md, memory_index, appended_instructions, mcp_config, environment_names) and unlisted (claude_md_imports and claude_rules, which this entry does not list and a later entry at the first request records), the config directory as its path and its source (template or home), the documents in the measured order each as kind, path, byte length and SHA-256, and the names of the environment variables the template set. It is not a copy of each document into the block store, and not a content-bearing record, because the entry must hold no content under home P7 and CN3. It is unsigned and unencrypted now, and because it names hashes only, signing and encryption at rest can be added later without changing what is recorded.",
      "quote": "",
      "consequences": [
        "RECORD.md carries lys.given's data shape beside the other lys custom entries.",
        "A later change to the shape is a new custom type or version alongside, never a mutation of lys.given, once anything is signed over it.",
        "Settings, hook output, plugin skill and agent listings and output styles are recorded by later cards as further kinds on this same entry shape.",
        "A check of a file against the record is by hash only, and prints neither file."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-014",
      "title": "A lantern is a custom entry in its session, and its note grows only by epilogue entries",
      "status": "decided",
      "scope": "home",
      "date": "2026-09-26",
      "decided_by": "The stage 5 lantern request, which names the record shape; recorded with HOME-004",
      "context": "Stage 5 of the home needs lanterns: a note plus a point in a transcript, declared on purpose, that a later session or a fork walks back to. The home record is Pi's session tree and lys adds entry kinds only as custom entries (home P2, CN4). The shape chosen here is the one the stage 5b fork will cut from, so it has to be fixed before anything is lit under it.",
      "decision": "A lantern is a `lys.lantern` custom entry appended at its session's head, carrying in custom.data the entry id of its point (an existing entry of the same session that is not itself a lantern or an epilogue, the head or any entry the head has moved past), the note as written, who lit it and when. Its note grows only by `lys.lantern_epilogue` custom entries naming the lantern's entry id and carrying the further words, who added them and when; a lantern's story is its entry followed by its epilogues in order, and nothing is rewritten. Rejected: Pi's `label` entry on the target (it replaces or clears a label rather than growing one, and carries no author or time), a lantern store beside the session outside Pi's grammar (a lantern would stop travelling with its session), and editing the lantern's note in place (the record is append-only, P1).",
      "quote": "",
      "consequences": [
        "HOME-004 defines both custom types and documents their data fields in docs/design/home/RECORD.md.",
        "Lighting appends at the head, so the head moves onto the lantern; the stage 5b fork cuts at the lantern's point, never at the head.",
        "A lantern or epilogue is never a lantern's point; adding to a lantern is an epilogue.",
        "Entry ids are unique only within a session, so an epilogue and a recall by point always name the session with the id."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-015",
      "title": "A lantern's note and epilogues are the person's annotations, not transcript",
      "status": "decided",
      "scope": "home",
      "date": "2026-09-26",
      "decided_by": "The lead of the home card, answering the stage 5 survey",
      "context": "Home P7 and CN3 forbid transcript contents in output, logs and errors. A lantern's note is written as a line of the session file, and recall exists to print it, so the principle and the stage 5 request conflicted until it was settled whether a note is transcript.",
      "decision": "A lantern's note and its epilogues are the person's own annotations, not transcript. Recall prints them by design; they are the one exception to P7 and CN3, and only recall prints them. Transcript lines (message, tool and compaction content) still never appear in output, logs or errors, and a recall row never carries a line of the transcript around its point. Rejected: treating notes as transcript and printing only their hashes, which makes recall useless to the person who wrote them.",
      "quote": "",
      "consequences": [
        "P7 and CN3 of docs/design/home/design.json are amended with HOME-004 to state the exception.",
        "The lys-home README and RECORD.md say that lantern notes and epilogues are the one text the crate prints, and only recall prints them.",
        "Errors from lighting and epilogues still name no note, epilogue or transcript text: ids, paths and counts only."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-016",
      "title": "A rendered file's derived uuids are a fixed, versioned contract",
      "status": "proposed",
      "scope": "home",
      "date": "2026-09-26",
      "decided_by": "",
      "context": "The Claude Code render gave every entry id that is not uuid-shaped a fresh random uuid, so two renders of one session head differed and no hash could name a rendered file; the launch template then shaped one from the SHA-256 of the entry id alone, deterministic but outside any namespace, without a role, and equal for the same entry id in any two sessions. Deriving those uuids from the record under a fixed scheme makes a render deterministic, and from the first receipted render event onward a hash names a rendered file, so the derivation fixes those hashes the way a wire format fixes signatures. The fork and launch cards need the same kind of derived id.",
      "decision": "Derive every uuid a render needs and the record does not hold as UUIDv5 under the session's namespace and the name `<entry id>#<role>`, with a closed set of roles (`record` first); the session's namespace is UUIDv5 over one fixed lys namespace (32c05904-d1f1-550c-9eee-2f6c8f98b665, itself UUIDv5 of the RFC 9562 URL namespace over `lys/home/claude-code/render-uuid/v1`) and the id of the session being rendered, so the same entry id in two sessions never derives one uuid. Treat the namespace, the session namespace rule, the name form and the roles as frozen: a change is a new version alongside, never a mutation. The fork and launch cards derive by this scheme. Rejected: drawing fresh ids (nondeterministic), keying on a hash of the entry id alone without a namespace, a role and a session (two roles on one entry collide, two sessions with one hand-authored id collide, and it is not reproducible with standard UUID tooling), mixing in the target session id (the record does not hold it), and leaving the scheme mutable until a later card (every recorded hash would move with it).",
      "quote": "",
      "consequences": [
        "HOME-007 implements the derivation for the role `record` and pins a fixture hash in PROOF-RESUME.md.",
        "A fork or launch card that needs a derived id adds a role to the closed set, never changes the namespace, the session namespace rule or the name form.",
        "Changing the derivation breaks every recorded render hash and is written as a new version with its own namespace, alongside the old."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-017",
      "title": "A fork is a child session cut from the parent's own lines at a lantern's point, with its ancestry on both sides",
      "status": "decided",
      "scope": "home",
      "date": "2026-09-26",
      "decided_by": "The stage 5b fork request and the lead's answers to its survey; recorded with HOME-006",
      "context": "A lantern is a pathway back to a conversation with the self that lit it, and Claude Code resumes a session only from its end. The home record is Pi's session tree (P2); Pi's own fork writes a new session whose header's parentSession is the source file's path and copies the source entries. The fork has to say which session a lantern was lit in, since a copied line keeps its id and a child holds a copy of every lantern on the copied chain.",
      "decision": "A fork resolves a lantern to the session it was lit in, read from the lys.lantern data's lit_in when the record carries it and otherwise by the older-record rule (one holder cuts, several refuse lantern_ambiguous until a session is named), and cuts that session's root-to-point chain at the last assistant message at or before the point, through the index. The child is a new session under the parent's cwd whose header's parentSession is the parent file's path relative to the home, holding each cut entry as the parent file's own line bytes, then one lys.forked_from custom entry as its head naming the parent session, the lantern, the point, the cut entry, whether the coordinate was carried and the carried entry; the parent gains one lys.fork custom entry at its head naming the child. Nothing else is copied and no block is written. Rejected: re-serialising the copied entries (the copy would stop hash-matching the parent's lines), a fork store beside the sessions outside Pi's grammar, cutting at a point no lantern names, and a header field beyond Pi's parentSession.",
      "quote": "",
      "consequences": [
        "HOME-006 defines both custom types and documents their data in docs/design/home/RECORD.md; a copy of a lantern's line in a child is a copy, never a second lantern.",
        "A lantern that sits before any assistant message is refused nothing_to_fork: a point before the first reply would carry only a seed, which is a new session and not a fork.",
        "The fork report carries ids and counts only: entries copied, distinct block hashes the copied entries name that the store holds, and those it does not.",
        "The lantern card records lit_in in a later round; until it does, every lantern the light act writes resolves by the older-record rule."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-018",
      "title": "A user-message point is carried as a seed prompt beside the rendered file, never copied into the child",
      "status": "decided",
      "scope": "home",
      "date": "2026-09-26",
      "decided_by": "The lead's answers to the fork survey; recorded with HOME-006",
      "context": "A lantern may mark a user message. Copying it into the child would leave the child ending on a question no reply answers, and dropping it would lose the coordinate the lantern marks. The measured cut of the lantern design carries the message as the first prompt instead.",
      "decision": "When the point is a user message the cut stops at the assistant message before it and the message is carried, not copied: lys.forked_from records its id with coordinate_carried true and counts, by kind, the parts of it that are not text. The Claude Code render of such a child writes the message's text parts, in order, as a seed prompt beside the rendered file under an in-band marker line naming the parent session, the point and the lantern, and names it in the render report; the template's launch line, printed by render-launch only, passes that file as the resumed session's first prompt. A part that is not text never refuses a fork or a render and never enters the seed. Rejected: copying the user message into the child's chain, refusing a fork for a non-text part, and putting the seed's text in the report or the loss account.",
      "quote": "",
      "consequences": [
        "The seed lives only in the render's output directory, as the rendered JSONL does; the report, the loss account and the fork entries carry counts and ids.",
        "A seed path that already exists is refused by name and the render writes nothing.",
        "The launch line is printed, never run (ADR-007); the proof runs it by hand and records which line it ran."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-019",
      "title": "The secrets broker lives in lys, as crates/lys-secrets; Cambium consumes it and is never its home",
      "status": "decided",
      "scope": "secrets",
      "date": "2026-09-26",
      "decided_by": "The lead's answers to the secrets survey; recorded with SECRETS-003",
      "context": "SECRETS-002 named door-owned files in the cambium checkout while its design's CN4 refused to let those proposals make Cambium a required runtime, and left the broker's host open. Every code row's files wait on which repository they sit in, and ADR-004 requires every project to stand alone.",
      "decision": "The broker lives in the lys repository as the crate crates/lys-secrets, part of the standalone identity platform. A person installs Lys to hold secrets; Cambium reaches the broker through the door as a consumer and is never its home. Rejected: the broker inside the cambium door that SECRETS-002 names, which would make Cambium a runtime a person must install to keep a credential.",
      "quote": "",
      "consequences": [
        "Every SECRETS-003 code row's files sit in the lys repository, under crates/lys-secrets.",
        "CN4 of the secrets design is settled by this entry; SECRETS-002's open reading of the host is closed by SECRETS-003 without editing SECRETS-002.",
        "The cambium paths SECRETS-002 names are not walls for any row; no cambium file is edited for the broker."
      ],
      "supersedes": [],
      "superseded_by": ""
    },
    {
      "id": "ADR-020",
      "title": "A lease is a broker record under the audit log, not a signed format",
      "status": "decided",
      "scope": "secrets",
      "date": "2026-09-26",
      "decided_by": "The lead's answers to the secrets survey; recorded with SECRETS-003",
      "context": "SECRETS-002 R9 puts a lease's time window in the signed delegation, while the draft the survey found made the lease a broker record. lys/delegation/v1 has no not_after by design (crates/lys-core/src/delegation/mod.rs:218-221), and a signed format freezes wire bytes that strangers verify.",
      "decision": "The lease is a broker record under the audit log: it counts uses, time and spend against a delegation and is read only by the broker, so nothing a stranger verifies changes. The signed delegation keeps the time window SECRETS-002 R9 gives it, and the lease's own not_after never widens that window. Rejected: a signed lease format, a new version beside lys/delegation/v1 with its own adversarial review.",
      "quote": "",
      "consequences": [
        "No new signed format and no new version beside lys/delegation/v1 is written for the lease.",
        "v1's lack of not_after is recorded as a fact the lease's window covers.",
        "A lease a stranger verifies offline, if a later row needs one, is a new brief with its own adversarial review."
      ],
      "supersedes": [],
      "superseded_by": ""
    }
  ]
}
