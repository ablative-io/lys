{
  "cluster": "secrets",
  "title": "Secrets broker: agents hold handles, never credentials",
  "intention": "Every identity, person or agent, uses credentials through a short-lived handle the door swaps for the real credential, so no credential ever reaches an agent and revoking is instant.",
  "problem": "Credentials live in files on each machine and in each worker's environment. An agent that holds a key cannot have it taken back, usage cannot be attributed in one place, and resting an account means copying a file to every machine.",
  "solution": "A broker in Rust inside the door: an encrypted store of real credentials, handles bound to identities, a proxy that checks SpiceDB, swaps the handle, forwards the call and writes one audit line, rotation across several accounts under one handle, OAuth refresh at the proxy, sealed records tagged in SpiceDB, and leases counted by uses, time window and spend. The token revolver is its first consumer.",
  "principles": [
    {
      "id": "P1",
      "text": "A handle, never a credential: the credential never leaves the server."
    },
    {
      "id": "P2",
      "text": "Revoking is dropping the handle; what was already handed to a process needs its own revocation story, stated per case."
    },
    {
      "id": "P3",
      "text": "Every use writes one audit line naming the seat, the handle, the real account and the time."
    },
    {
      "id": "P4",
      "text": "A key is used through the proxy, never read; only memories are read, in the smallest piece asked for."
    },
    {
      "id": "P5",
      "text": "Every grant traces back to the person who authorised it."
    }
  ],
  "decisions": [
    "ADR-001",
    "ADR-002",
    "ADR-003",
    "ADR-004"
  ],
  "goals": [
    "An implementation brief, SECRETS-002, covers every part of the temporary key model in the statement with numbered requirements and acceptance criteria.",
    "Every checklist item and user story of the broker is covered by a SECRETS-002 requirement.",
    "The token revolver can take its next account from the broker instead of its own list."
  ],
  "non_goals": [
    {
      "text": "OpenBao or another external secrets engine",
      "reason": "Tom's model is built in Rust inside the door; an external engine is only reconsidered if credentials minted on demand are needed."
    },
    {
      "text": "The seat login through a proxy handle and base URL",
      "reason": "Not tested with a subscription login; the statement keeps it open to prove later and nothing depends on it."
    },
    {
      "text": "The delegation schema",
      "reason": "The statement says it is not settled."
    }
  ],
  "structure": [
    {
      "path": "docs/design/secrets/briefs/SECRETS-002.json",
      "note": "the secrets broker implementation brief",
      "brief": "SECRETS-001"
    },
    {
      "path": "docs/design/secrets/briefs/SECRETS-002.md",
      "note": "its rendered markdown",
      "brief": "SECRETS-001"
    },
    {
      "path": "docs/design/secrets/design.json",
      "note": "this design; the structure gains a row for every path SECRETS-002 names",
      "brief": "SECRETS-001"
    },
    {
      "path": "docs/design/secrets/DESIGN.md",
      "note": "rendered design",
      "brief": "SECRETS-001"
    },
    {
      "path": "docs/design/secrets/checklist.json",
      "note": "checklist; gains the broker implementation items",
      "brief": "SECRETS-001"
    },
    {
      "path": "docs/design/secrets/CHECKLIST.md",
      "note": "rendered checklist",
      "brief": "SECRETS-001"
    },
    {
      "path": "docs/design/secrets/stories.json",
      "note": "stories; gains the broker implementation stories",
      "brief": "SECRETS-001"
    },
    {
      "path": "docs/design/secrets/USER-STORIES.md",
      "note": "rendered stories",
      "brief": "SECRETS-001"
    }
  ],
  "inventory": [
    {
      "path": "docs/design/identity/STATEMENT-2026-09-22.md",
      "note": "the authority: 'Secrets: the temporary key model' through 'What revoking reaches', 'Two rules from Tom, 13:55', 'Lifecycle, budgets and leases' and 'Where it lives: lys'"
    },
    {
      "path": "docs/design/decisions.json",
      "note": "the project decision ledger this cluster anchors to"
    },
    {
      "path": "crates/",
      "note": "the five lys crates (lys, lys-core, lys-anchor, lys-anchor-cli, lys-log-store): sealed envelopes, the delegation format, the log. The door repository, where the statement places the store and the proxy, is the cambium checkout, apps/cambium in the ablative estate on this Mac; it is read, never written, by this brief"
    }
  ],
  "constraints": [
    {
      "id": "CN1",
      "text": "No credential, token or key value is ever written, read or quoted in any document of this cluster."
    },
    {
      "id": "CN2",
      "text": "No row of SECRETS-002 is dispatched before Waffles has reviewed it."
    },
    {
      "id": "CN3",
      "text": "Every path written in a document of this cluster is relative to the repository root, whatever directory a session starts in; a command runs from its own tree and spells its paths from there."
    }
  ],
  "gate": [
    {
      "tree": ".",
      "legs": [
        {
          "name": "fmt",
          "command": "cargo fmt --all",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        },
        {
          "name": "clippy-all-features",
          "command": "cargo clippy --all-targets --all-features -- -D warnings",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        },
        {
          "name": "clippy",
          "command": "cargo clippy --all-targets -- -D warnings",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        },
        {
          "name": "tests",
          "command": "cargo test --workspace --all-features",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        },
        {
          "name": "doc-all-features",
          "command": "cargo doc --no-deps --all-features",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        },
        {
          "name": "doc",
          "command": "cargo doc --no-deps",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        }
      ]
    },
    {
      "tree": "docs",
      "legs": [
        {
          "name": "render",
          "command": "sh -c ': \"${DS2_METHOD:?DS2_METHOD names the design method tree on the machine serving the round}\"; exec python3 \"$DS2_METHOD/scripts/render-cluster.py\" design/secrets'",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        },
        {
          "name": "validate",
          "command": "sh -c ': \"${DS2_METHOD:?DS2_METHOD names the design method tree on the machine serving the round}\"; exec python3 \"$DS2_METHOD/scripts/validate.py\" design/secrets'",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        },
        {
          "name": "coverage",
          "command": "sh -c ': \"${DS2_METHOD:?DS2_METHOD names the design method tree on the machine serving the round}\"; exec python3 \"$DS2_METHOD/scripts/check-coverage.py\" design/secrets'",
          "requires": [
            "place:here"
          ],
          "cadence": "round"
        }
      ]
    }
  ]
}
