Rauthy and OpenFGA proof plan — 21 September 2026, Melbourne; requested by Waffles for delivery before 18:00.

Build in this order. Hours are provisional engineering effort for one implementer, including tests, not elapsed-time promises. Use an owner-authorized isolated scratch estate, fake records and separate credentials; no production Aion writes or installation on Tom's box. Pin Rauthy v0.36.2/Hiqlite and OpenFGA v1.21.0/SQLite. No execution has started. Tom clarified at 17:04: one person/account with Google, GitHub and passkey sign-in methods. Production grants remain configurable; a labelled test grant does not settle organisation policy.

1. **First complete scratch proof — 4 hours.** Requires a ready HTTPS scratch host, fixed WebAuthn domain, Google OAuth test credentials/callback, and a person available for sign-in/passkey enrollment. Provisioning and owner waiting time are excluded. With those ready, this can run today without the fork. Setup: 0.5 hours. Human: 1 hour to enroll a user-verifying passkey and explicitly link Google on one Rauthy account; fresh Google and local passkey logins must return the same person/subject, with MFA behavior recorded. Agent: 0.75 hours to provision a confidential client, validate issuer/audience/expiry and prove its enduring agent ID survives a worker restart; no mailbox and no assumption that machine tokens contain `sub`. Revocation: 1 hour to grant one fake-record action, execute it, pause a two-action task on a test barrier, delete the grant and await acknowledgement, then resume. The second action must make a fresh `HIGHER_CONSISTENCY` check and be refused, with zero second side effects despite a valid token. Evidence/restart repeat: 0.75 hours. No polling. This proves refusal of the next action, not rollback of an already admitted one.

2. **Concurrent admission and revocation — 6–10 hours.** Define and enforce ordering between grant removal and action admission in the scratch executor; a remote permission check alone cannot make the subsequent write atomic. Test both race orders, outages, duplicate messages and restart. Record grant changes and prove push-driven cancellation/revalidation for running work and live delivery. Receipt names the last authorized action and subsequent refusals.

3. **Application integration proof — 12–20 hours.** Share one typed principal/grant contract across Rust and Elixir, with server-owned browser sessions. Prove protected test paths in Cambium, Aion and Argus, including queued/running work, delivery, listings/counts and attachments. Separate discovery, contact, assignment, administration and the agent's own powers. Reject wrong-audience tokens and invented actor attribution. Retain a route inventory: this is representative coverage, not full estate rollout.

4. **Maintained Rauthy fork: multiple providers — 24–40 hours.** First 2 hours inventory every federation path and revise the estimate. Preserve account IDs while migrating to verified provider bindings. Test ownership uniqueness, concurrent/colliding links, unlinking the final credential, MFA/recovery, deletion, sessions and interrupted migration. Google, GitHub and passkey must reach one person. No email-only merging. Plan to own the narrow patch; upstream acceptance is optional. Recurring security-update and release-merge effort is not yet sized.

5. **Recovery and handback — 8–12 hours.** Prove backups/restoration, credential/key rotation, lost-device recovery and interrupted grant changes. Walk Mac Touch ID and iPhone Face ID through WebAuthn. Measure permission/list latency. Retain pinned versions/model ID, principal IDs, redacted outcomes, event ordering and side-effect counts; name failed/unrun legs and the fork maintenance owner.

Total: **54–86 engineering hours**, excluding prerequisites, upstream waiting, ongoing fork maintenance and full rollout. Deliver step 1 separately and revise later estimates from evidence. No application/device/restore tests or venue battery run; document whitespace and `git diff --check` passed. Sources and expanded acceptance details: PROOF-DETAIL.md. Recommendation: RECOMMENDATION.md.
