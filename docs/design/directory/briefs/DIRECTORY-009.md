---
type: brief
id: DIRECTORY-009
cluster: directory
title: Move an identity between its lifecycle states by signed record, and gate every check on the folded state
---

# DIRECTORY-009: Move an identity between its lifecycle states by signed record, and gate every check on the folded state

> **Cluster:** directory
> **Depends on:** DIRECTORY-003, DIRECTORY-006
> **Blocked by:** The lead's sign-off of this brief before any row is dispatched (DIRECTORY-001 boundary: no row brief is dispatched until the lead has reviewed it). The files this brief adds under crates/lys-identity/src/lifecycle/ and crates/lys-identity-server/ are named in each row's files list, and this sign-off is their manifest review (CN9). Check: python3 scripts/design/validate.py docs/design/directory and python3 scripts/design/check-coverage.py docs/design/directory both exit 0, and the card that carries this brief records the sign-off before card_build_v3 runs; a build started without it is a defect of the chain, not a row of this brief., R2, R3, R4, R5 and R6 wait on DIRECTORY-003 landed on main: crates/lys-identity/ and crates/lys-identity-server/ exist there and DIRECTORY-003.json carries an execution record with status landed. Check: git ls-tree -r --name-only origin/main crates/lys-identity crates/lys-identity-server prints paths, and python3 -c "import json; print(json.load(open('docs/design/directory/briefs/DIRECTORY-003.json'))['execution']['status'])" prints landed. Until then no code row starts; R1 is documents only and does not wait., R2 and R3 wait on the landed lys-identity exposing DIRECTORY-003 R5's transition commit path and on docs/design/identity/IDENTITY-EVENTS.md carrying the five transition operations, register, activate, suspend, reinstate and retire, with from, to and reason in the typed payload (the sign-in event is DIRECTORY-003's; R1 lists this as an input to its joint review, never as an edit). Check: rg -c 'register|activate|suspend|reinstate|retire' docs/design/identity/IDENTITY-EVENTS.md prints at least 5, and cargo doc -p lys-identity --no-deps lists the function that commits a transition in the crate's public API. If the landed crate commits transitions only through a path that also writes a stored state, R3 stops and names it, and the act that answers is a DIRECTORY-003 revision through the lead under CN9., R2 and R3 wait on the landed lys-identity manifest being reconciled with this brief's paths: if crates/lys-identity/src already holds a module named lifecycle, or a function that sets a lifecycle state, this brief is revised under CN9 to name its files beside the landed ones and to name the one line that fills DIRECTORY-003 R5's state field from the fold, before either row is dispatched. Check: ls crates/lys-identity/src/lifecycle prints nothing at dispatch of R2, and rg -n 'fn set_state|fn set_lifecycle' crates/lys-identity/src prints nothing; where either prints, the revision is the act that answers., R4 waits on DIRECTORY-006 R4 landed on main: crates/lys-identity/src/grants/permission.rs exists there with its freshness decision and the clock it names (docs/design/directory/briefs/DIRECTORY-006.json:136-147). Check: git ls-tree --name-only origin/main crates/lys-identity/src/grants/permission.rs prints the path, and rg -n 'fresh' crates/lys-identity/src/grants/permission.rs prints a line. R4's one-line hook at that decision's entry is named by revision under CN9 once the landed file is read; this brief edits nothing else of DIRECTORY-006's., R5 waits on the file of crates/lys-identity-server/ where DIRECTORY-003's OIDC session handling resolves a Rauthy-authenticated issuer-subject to a registered identity at sign-in and at refresh. That file is named by DIRECTORY-003's reviewed manifest, not known at 1756688cc08169bef4a9ac37b9b079efb9e38b18, so this brief is revised to name it exactly before R5 is dispatched (CN9). Check: rg -n 'issuer' crates/lys-identity-server/src prints the resolution point, and the revised R5 files.modify lists that path., R5 and the receipt brief DIRECTORY-007, which stands on its own branch (brief/directory/63bd2f2e-c397-4e1d-b4a5-5353f7fd1324) and not on main: its R4 was reconciled with this row on 27 September 2026 at 01:08 (Apollo's review, the lead's ruling): DIRECTORY-007 R4 is independent of lifecycle state, every sign-in attempt leaves exactly one receipt, and a refused attempt leaves a sign-in-refused receipt carrying the refusal's name as its reason, whatever that name is, so neither landing order forces a rework and this blocker is struck. R5 leg 3 counts transition leaves by operation, and where DIRECTORY-007 has landed each refusal leaves one sign-in-refused leaf with reason lifecycle_suspended. Check: git show origin/brief/directory/63bd2f2e-c397-4e1d-b4a5-5353f7fd1324:docs/design/directory/briefs/DIRECTORY-007.json | rg -c 'signin_refused_reason' prints 1; after both land, rg -n 'lifecycle_suspended' crates/lys-identity-server/tests/sign_in_receipts.rs prints a line. This brief edits no file of DIRECTORY-007., R6 waits on the record read route DIRECTORY-005 declares in crates/lys-identity-server/src/routes.rs and on DIRECTORY-006 R5's grant list in crates/lys-identity-server/src/grants.rs, which the provisioned view reads. Check: git ls-tree --name-only origin/main crates/lys-identity-server/src/routes.rs crates/lys-identity-server/src/grants.rs prints both paths. Where DIRECTORY-007's common receipt has also landed (git ls-tree --name-only origin/main crates/lys-receipt/src/receipt.rs prints the path), the record field of R6's read is that receipt; until then it carries DIRECTORY-003 R2's receipt fields, and the brief is revised to say which before R6 is dispatched.
> **Design anchor:**
> - ADR-003 — Everything is pegged to a human authority — A person signs in first; an agent is provisioned under that person with its own identity; the person's permissions are the ceiling and the agent holds an explicit subset; every grant says who may exercise it and who may pass it on; withdrawing the authority stops every grant derived from it. The exact delegation schema is not settled by this decision.
> - ADR-011 — An identity is registered, active, suspended or retired — An identity is in one of four states: registered (exists in the directory, no grants, no credential handle, may not act), active (may act within its grants), suspended (kept whole, grants kept but not effective) and retired (permanent, history kept, never reactivated; a new identity is made instead). Register, activate, suspend, reinstate and retire are the only transitions, each one signed audit record naming the authenticated actor and their provenance, the identity, from, to, when and reason. Having a grant or a credential is a fact beside the state, not a state. A person is registered by first sign-in; an agent is registered by a signed-in person, who carries it as its responsible person for life and may cause every transition of their own agents. Source: docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:17-44 and docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:71-95.
> - ADR-027 — An identity's lifecycle state is folded from its signed transition records and never set directly — The state at any moment is the fold of the identity's transition records in log order, from registered. No API, migration or repair sets a state; a transition validates against the folded state and the cause rule, appends its record through the directory's event path, and the state is read again by folding. A read answers the fold or refuses by name at the coordinate of a record the transition table refuses, and never answers a state past it, repairs the log or skips the record. Rejected: a stored state column set at transition and audited after it; a repair path that writes a state; and treating provisioned as a fifth state rather than a view over the facts beside the state.
> - ADR-028 — A suspension takes effect at the next check, a session refresh is a check, and no Rauthy session is revoked at once — A suspension or retirement takes effect at the next check and not before. The directory's resolution of a Rauthy-authenticated session at sign-in and at refresh is a check, so a session that outlives a suspension is refused at its next refresh and at every access check before that, by state, whatever grants it holds. Nothing in DIRECTORY-009 calls Rauthy to revoke, end or invalidate a session, keeps a list of sessions to end, or rolls back an admitted action. An at-once revocation of the Rauthy session would be a later revision of the suspend transition, and whether suspending also ends the sign-in session at Rauthy stays open as the design records it. Rejected: revoking the session at Rauthy at once within this brief, and treating a refresh as something other than a check.
> **Checklist:**
> - C36 — docs/design/identity/LIFECYCLE-CONTRACT.md carries the four states with provisioned as a view, the five transitions each with who may cause it and no policy actor, the record rule, the fold rule, the check conjunction, the next-check rule with a refresh as a check, the retired rule, what the screen shows, the six named refusals with their acts, and what IDENTITY-EVENTS.md must carry before the code rows start.
> - C37 — A transition is admitted only from the administrator or, for an agent, its responsible person as its registration record names them; every other actor, and an unattested one, is refused lifecycle_actor_not_permitted with nothing appended (d009_r2_ac1 to d009_r2_ac4).
> - C38 — The state is answered only by folding the identity's transition records in log order over a type of exactly four values; nothing sets a state; a record the table refuses makes the read refuse lifecycle_fold_invalid at its coordinate; reads never write the log (d009_r3_ac1 to d009_r3_ac5).
> - C39 — An access check is the conjunction, active and grant exists and grant fresh, state first: a registered, suspended, retired or unknown identity is refused by state with the grant answerer called zero times, the grant legs pass through DIRECTORY-006 R4's decision with its names, and an answered check appends nothing (d009_r4_ac1 to d009_r4_ac5).
> - C40 — At sign-in and at every refresh the directory service refuses a suspended or retired identity by name before any grant question, calls Rauthy to revoke nothing, rolls back no admitted action, and admits an active identity at refresh whatever grants it holds (d009_r5_ac1 to d009_r5_ac5).
> - C41 — One typed read answers an identity's state, the record that put it there, the actions available in that state, its full history and provisioned as a view, and one typed list is filterable by state and kind; a retired identity reads exactly as a live one and no operation deletes, hides, redacts or truncates a record (d009_r6_ac1 to d009_r6_ac5).
> **Stories:**
> - S16 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the administrator, I want to suspend, reinstate or retire an identity only by a signed act whose record is the one thing that changes its state, so that no state is ever set without a record saying who changed it, from what, to what and why.
> - S17 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the responsible person, I want a suspended or retired agent of mine refused at every check and at its next session refresh by its state alone, whatever grants it holds, so that halting it never depends on finding and revoking every grant first.
> - S18 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the administrator, I want the screen to show an identity's state and the record that put it there, for a retired identity exactly as for a live one, so that I can answer why a check refused from the record and not from memory.

## Purpose

A person or agent on the platform is in exactly one lifecycle state at a time, registered, active, suspended or retired, as the lifecycle document lays them out (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:19-24, ADR-011); provisioned is a view over the facts beside the state, not a state of its own (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:26-28). This brief writes the code that moves an identity between those states and the code that reads the state to admit or refuse. Every transition names who may cause it and is itself one signed, service-attested audit record of IDENTITY-001's shape (docs/design/identity/briefs/IDENTITY-001.json:40-41 and docs/design/identity/briefs/IDENTITY-001.json:52), committed through the directory's event path that DIRECTORY-003 R2 builds and DIRECTORY-003 R5 records the transitions through, so the state at any moment is folded from the log and never set directly (ADR-027, CN19). An access check is the conjunction the lifecycle document states, the identity is active and the grant exists and the grant is fresh (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:48-53), with the state leg first, so a suspended or retired identity fails every check by state alone, whatever grants it holds. Rauthy stays the source of the sign-in event and SpiceDB the answer to the grant question (DIRECTORY-006 R4); the state lives in the directory beside them. The lead's two answers are folded in as written. L1: the suspension takes effect at the next check and not by revoking the Rauthy session at once; a refresh is a check, so a session that outlives the suspension is refused at its next refresh and at every access check before that, by state, whatever grants it holds; nothing in this brief calls Rauthy to revoke a session, and an at-once revocation would be a later revision of the transition, not this one (ADR-028, CN20). L2: retired identities keep their audit history readable; the retired row of the lifecycle document says history and audit are kept and the identity is never reactivated, and the log is append-only and signed under IDENTITY-001, so retirement closes the identity and never its record; an operator reads a retired identity's state, its transitions and the record that put it there exactly as for a live one, and no row of this brief deletes or hides an audit record (CN21). The screen of step 7 shows the state and the record that put it there: this brief builds the typed read that screen consumes and the contract that says what it shows, and not the screen. Already on main and not reopened: the state names and transitions of the lifecycle document, the audit-record shape of IDENTITY-001, DIRECTORY-003 R5's recording of the state with the projection equal to replay, and the three acts the anchor decisions reserve, publishing lys-anchor, generating a production key and emitting a receipt outside a test (docs/design/lys-anchor/DECISIONS.md:333-335, docs/design/decisions.json:139).

## Task

Six rows, in order: R1 the lifecycle contract document (2 hours), R2 who may cause each transition (3 hours), R3 the state folded from the log and never set (4 hours), R4 the access check as the conjunction with the state leg first (3 hours), R5 the suspension taking effect at the next check, a refresh being a check (3 hours), R6 the typed read the screen consumes, with retired identities readable (3 hours): 18 focused implementer hours, a new increment beside IDENTITY-001's ceiling (CN7), not inside it. R1 is documents only and lands first; R2 and R3 start only when DIRECTORY-003 has landed with its transition commit path and event vocabulary; R4 only when DIRECTORY-006 R4 has landed; R5 only when the session resolution point is named by revision; R6 only when the record read route and the grant list have landed (blocked_by). Nothing at 1756688cc08169bef4a9ac37b9b079efb9e38b18 exists under crates/lys-identity/ or crates/lys-identity-server/; every code row of this brief stands on files DIRECTORY-003 and DIRECTORY-006 create, and each such dependency is a blocker with a check a stranger can run, never an assumption. What stands on main and is not reopened: the states and transitions of docs/design/identity/LIFECYCLE-STATES-2026-09-22.md, the audit-record shape and the service-attestation rule of IDENTITY-001, DIRECTORY-003 R5's table of refused transitions, and the three acts the anchor decisions reserve. The two answers realised: L1 (next check, a refresh is a check, no revocation at Rauthy, an at-once revocation a later revision) in R4, R5 and ADR-028; L2 (history kept and readable) in R3, R6 and CN21. Where a count could be argued (how many records a scenario leaves, how many refusals a leg asserts, how many times the grant answerer is called) it is decided by rule in this brief and the legs assert those numbers. Every code row is red first: its first commit adds its test file alone and the named tests fail; its second commit makes them pass. No wall clock anywhere in the new code: every time value a test needs is injected by the harness, and the freshness leg uses the clock DIRECTORY-006 R4 names, injected. Every refusal is named and carries the act that answers it. Test names are snake_case and carry the criterion ids (d009_r2_ac1 and so on) so a search proves the mapping. No name of a person and no date inside code. Every path is relative to the repository root. Files of other briefs this brief touches are each one added line at a named point: crates/lys-identity/src/grants/permission.rs (DIRECTORY-006 R4, one line at the decision's entry, R4), the session resolution point of DIRECTORY-003's server (one line, R5), crates/lys-identity-server/src/routes.rs (added route lines, R6) and the two lib.rs module lists; nothing else of DIRECTORY-003, DIRECTORY-006 or DIRECTORY-007 is edited (CN9).

## Requirements

### R1: Write the lifecycle contract as it stands: the states, the transitions and their causers, the fold, the check, the next-check rule, the retired rule and what the screen shows

Documents only. THE SYSTEM SHALL add docs/design/identity/LIFECYCLE-CONTRACT.md holding, in this order: (1) the four states, registered, active, suspended and retired, as a table whose rows begin with the state name and carry the lifecycle document's meaning and may-act column (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:19-24), followed by the sentence that provisioned is a view (has any grant, has a handle) and not a state, so a grant can be added or removed without a state change (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:26-28); (2) the transition table, one row per transition beginning with what it is recorded as, register, activate, suspend, reinstate and retire, with from, to and who may cause it: register by an administrator, or by a person for themselves by first sign-in, which in step 1 registers nobody (DIRECTORY-003 R1) and arrives with step 2's self-registration; activate, suspend, reinstate and retire by an administrator, meaning a signed-in person the directory's administrator policy admits (P9), and, when the identity is an agent, also by its responsible person as its registration record names them (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:77-81); and the statement that no policy causes a transition in this contract: the only automatic effect a suspension or retirement has on other identities is the grant-check one DIRECTORY-006 R4 enforces (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:87-89), and a policy that moves an identity between states would be a later revision of this contract; (3) the record rule: every transition is one signed, service-attested audit record of IDENTITY-001's shape naming the attested actor and their provenance, the identity, from, to, when and the reason given (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:40-44, docs/design/identity/briefs/IDENTITY-001.json:41 and docs/design/identity/briefs/IDENTITY-001.json:52), committed through the directory's event path (DIRECTORY-003 R2) and returned with its receipt; the reason is recorded as given, and whether it is mandatory on suspend and retire stays the room's open question (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:101, ADR-011); (4) the fold rule (ADR-027): the state at any moment is the fold of the identity's transition records in log order, from registered; no API, migration or repair sets it; a read answers the fold or refuses by name at the coordinate of a record the table refuses; (5) the check rule: an access check passes only when the identity is active and the grant exists and the grant is fresh, in that order, the state leg answered from the fold and the grant legs from DIRECTORY-006's permission decision, with SpiceDB the answer to the grant question and Rauthy the source of the sign-in event; a suspended or retired identity refuses every check by state alone, before the grant question is asked, whatever grants it holds; an answered check commits no event; (6) the next-check rule (ADR-028): a suspension or retirement takes effect at the next check; the directory's resolution of a Rauthy session at sign-in and at refresh is a check, so a refresh is a check; nothing in this contract calls Rauthy to revoke a session; an action already admitted is never rolled back; an at-once revocation would be a later revision of the suspend transition; (7) the retired rule: a retired identity's history and audit are kept, its state, its transitions and the record that put it there are readable exactly as a live identity's, nothing deletes or hides a record, and it is never reactivated, a new identity being made instead (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:24); (8) what the screen shows per identity: name and kind, the state and the actions available in that state and nothing else, the record that put it there as the last audit line, and a list view of the same columns filterable by state and kind (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:57-61), with the statement that the typed read of R6 is what the screen consumes and the screen build is not this brief; (9) the refusal table, one row per refusal beginning with its name, when it fires and the act that answers it: lifecycle_not_active (activate the identity), lifecycle_suspended (reinstate it), lifecycle_retired (none; make a new identity), lifecycle_unknown_identity (register it), lifecycle_actor_not_permitted (have the administrator or the agent's responsible person cause the transition), lifecycle_fold_invalid (verify the leaf at the named coordinate against its receipt and read the log by hand; the log is never repaired); and (10) what docs/design/identity/IDENTITY-EVENTS.md must carry before R2 to R6 start, as inputs to DIRECTORY-003's joint review and never as edits: the five transition operations in its vocabulary with from, to and reason in the typed payload. The document SHALL NOT restate any of these as a new decision, SHALL NOT fix an encoding, SHALL NOT edit docs/design/identity/LIFECYCLE-STATES-2026-09-22.md, any IDENTITY-001 file, docs/design/identity/IDENTITY-EVENTS.md or docs/design/identity/DIRECTORY-CONTRACT.md, and SHALL NOT decide anything the design records as open. Estimate: 2 hours.

**Acceptance:**
- Leg 1 of 5: grep -c '^| registered |\|^| active |\|^| suspended |\|^| retired |' docs/design/identity/LIFECYCLE-CONTRACT.md prints 4, grep -c '^| provisioned' docs/design/identity/LIFECYCLE-CONTRACT.md prints 0, and grep -c 'provisioned is a view' docs/design/identity/LIFECYCLE-CONTRACT.md prints at least 1.
- Leg 2 of 5: grep -c '^| register |\|^| activate |\|^| suspend |\|^| reinstate |\|^| retire |' docs/design/identity/LIFECYCLE-CONTRACT.md prints 5, and grep -c 'responsible person' docs/design/identity/LIFECYCLE-CONTRACT.md prints at least 1, and grep -c 'no policy causes a transition' docs/design/identity/LIFECYCLE-CONTRACT.md prints 1.
- Leg 3 of 5: each of grep -c 'takes effect at the next check', grep -c 'a refresh is a check', grep -c 'nothing in this contract calls Rauthy to revoke', grep -c 'never rolled back', grep -c 'readable exactly as a live identity', grep -c 'never reactivated' and grep -c 'folded from the log and never set directly' over docs/design/identity/LIFECYCLE-CONTRACT.md prints at least 1.
- Leg 4 of 5: grep -c '^| lifecycle_' docs/design/identity/LIFECYCLE-CONTRACT.md prints 6, and grep -c 'lifecycle_not_active\|lifecycle_suspended\|lifecycle_retired\|lifecycle_unknown_identity\|lifecycle_actor_not_permitted\|lifecycle_fold_invalid' docs/design/identity/LIFECYCLE-CONTRACT.md prints at least 6.
- Leg 5 of 5: git diff --stat 1756688cc08169bef4a9ac37b9b079efb9e38b18 -- docs/design/identity/briefs docs/design/identity/LIFECYCLE-STATES-2026-09-22.md docs/design/identity/IDENTITY-EVENTS.md docs/design/identity/DIRECTORY-CONTRACT.md crates deploy surface vendor prints nothing on the R1 commit, and sh scripts/design/gate.sh exits 0 on it.

**Files:**
- create: docs/design/identity/LIFECYCLE-CONTRACT.md

**Checklist:**
- C36 — docs/design/identity/LIFECYCLE-CONTRACT.md carries the four states with provisioned as a view, the five transitions each with who may cause it and no policy actor, the record rule, the fold rule, the check conjunction, the next-check rule with a refresh as a check, the retired rule, what the screen shows, the six named refusals with their acts, and what IDENTITY-EVENTS.md must carry before the code rows start.

**Stories:**
- S18 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the administrator, I want the screen to show an identity's state and the record that put it there, for a retired identity exactly as for a live one, so that I can answer why a check refused from the record and not from memory.

### R2: Admit a transition only from the actor the table names, and refuse every other actor by name

WHEN a transition of an identity is requested with an attested actor, THE SYSTEM SHALL admit the request only when the actor may cause it under R1's transition table: for register, an administrator (the person's own first sign-in registers nobody in step 1, DIRECTORY-003 R1, and this row admits it for nobody); for activate, suspend, reinstate and retire, an administrator, or, when the identity is an agent, its responsible person as the agent's registration record names them (DIRECTORY-003 R1). Every other actor SHALL be refused lifecycle_actor_not_permitted, whose message names the transition, the identity and the act that answers, that the administrator or the agent's responsible person cause it, and nothing SHALL be appended. The rule lives in crates/lys-identity/src/lifecycle/causes.rs and runs before DIRECTORY-003 R5's table check and before the append; it reads the responsible person from the identity's registration record and never from the request. A request whose actor is not an attested person SHALL be refused the same way: no policy actor exists in this brief. In step 1 the server admits only the configured administrator (P9, DIRECTORY-003 R3), so the responsible-person leg is exercised at the crate level with attested test actors and is reached from the server only when step 2's admission arrives; this row SHALL NOT change any admission of the server. The actor of a permitted request is recorded in the transition's audit record as the service attests them (docs/design/identity/briefs/IDENTITY-001.json:41): the administrator or a person, never a signature the actor did not make. No wall clock: every time value the tests need is injected through crates/lys-identity/tests/lifecycle_support/mod.rs, which also holds the fixture log builder and the attested test actors and is never compiled into the library. Estimate: 3 hours.

**Acceptance:**
- Leg 1 of 5, d009_r2_ac1: the administrator drives register, activate, suspend, reinstate and retire on one agent: exactly 5 records are appended, in that order, each naming the administrator as the attested actor; the fold after reopen equals the fold before; cargo test -p lys-identity --test lifecycle_causes d009_r2_ac1 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 2 of 5, d009_r2_ac2: the agent's responsible person drives activate, suspend, reinstate and retire on their own agent: 4 records, each naming that person as the actor; a second person who is not the responsible person is refused lifecycle_actor_not_permitted on each of the same 4 transitions with 0 records appended; the test asserts 4 admissions, 4 refusals and a record count of 5 (the register record and the 4 admitted transitions); cargo test -p lys-identity --test lifecycle_causes d009_r2_ac2 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 3 of 5, d009_r2_ac3: a person attempting activate, suspend, reinstate and retire on themselves and on another person, 8 requests, is refused lifecycle_actor_not_permitted 8 times with 0 records appended, and the administrator's activate of that other person is admitted; the test asserts 8 refusals and 1 admission; cargo test -p lys-identity --test lifecycle_causes d009_r2_ac3 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 4 of 5, d009_r2_ac4: a request whose actor is not an attested person is refused lifecycle_actor_not_permitted for each of the five transitions, 5 refusals and 0 records, and each refusal's message contains 'administrator' and 'responsible person'; cargo test -p lys-identity --test lifecycle_causes d009_r2_ac4 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 5 of 5, the mapping and red first: rg -c 'fn \w*d009_r2_ac(1|2|3|4)(_|\b)' crates/lys-identity/tests/lifecycle_causes.rs prints 4; rg -n 'SystemTime::now|Utc::now|Local::now|Instant::now' crates/lys-identity/src/lifecycle prints nothing; on the row's first commit cargo test -p lys-identity --test lifecycle_causes fails to compile or lists the four names as failed.

**Files:**
- create: crates/lys-identity/src/lifecycle/mod.rs
- create: crates/lys-identity/src/lifecycle/causes.rs
- create: crates/lys-identity/src/lifecycle/error.rs
- create: crates/lys-identity/tests/lifecycle_causes.rs
- create: crates/lys-identity/tests/lifecycle_support/mod.rs
- modify: crates/lys-identity/src/lib.rs

**Checklist:**
- C37 — A transition is admitted only from the administrator or, for an agent, its responsible person as its registration record names them; every other actor, and an unattested one, is refused lifecycle_actor_not_permitted with nothing appended (d009_r2_ac1 to d009_r2_ac4).

**Stories:**
- S16 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the administrator, I want to suspend, reinstate or retire an identity only by a signed act whose record is the one thing that changes its state, so that no state is ever set without a record saying who changed it, from what, to what and why.

### R3: Answer the state only by folding the identity's transition records, and set it nowhere

THE SYSTEM SHALL answer an identity's lifecycle state only by folding that identity's transition records, in log order, from registered, in crates/lys-identity/src/lifecycle/fold.rs, over a state type in crates/lys-identity/src/lifecycle/state.rs that has exactly the four values registered, active, suspended and retired and no other. There SHALL be no function, field write, migration or repair in crates/lys-identity that sets a state: a transition validates against the folded state (DIRECTORY-003 R5's table) and the cause rule (R2), then appends its record through the directory's event path (DIRECTORY-003 R2), and the state is read again by folding. The state field DIRECTORY-003 R5's projection keeps, wherever its landed manifest keeps it, SHALL be filled from this fold and from nothing else, by the one line named by revision before this row is dispatched (blocked_by), and SHALL be rebuilt from the fold at open and equal to it after every append (P4, P5). WHEN the fold meets a record the table refuses, a transition from a state it cannot leave or a record for an identity that has no register record before it, THE SYSTEM SHALL refuse the read with lifecycle_fold_invalid naming the log coordinate of the offending record and the act that answers, verifying the leaf at that coordinate against its receipt and reading the log by hand, and SHALL NOT answer a state, repair the log or skip the record. The fold SHALL read leaves through the LeafStore trait (crates/lys-log-store/src/store.rs:120) and SHALL NOT call Log::open (crates/lys-log-store/src/log.rs:99, whose open may write a repaired pin) or append (crates/lys-log-store/src/log.rs:168). provisioned SHALL NOT be a value of the state type; it is a view computed beside the state (R6). No wall clock: the when of a record is the record's own, and every time value the tests need is injected through crates/lys-identity/tests/lifecycle_support/mod.rs. Estimate: 4 hours.

**Acceptance:**
- Leg 1 of 6, d009_r3_ac1: register, activate, suspend, reinstate and retire on one agent: the fold answers registered after the first record, active after the second, suspended after the third, active after the fourth and retired after the fifth, 5 states asserted in order; after the log is reopened the fold answers retired; cargo test -p lys-identity --test lifecycle_fold d009_r3_ac1 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 2 of 6, d009_r3_ac2: a fixture log the harness builds with the records of three identities interleaved, 3 for the first (register, activate, suspend), 2 for the second (register, activate) and 5 for the third (register through retire), 10 leaves in all, folds to suspended, active and retired, and the fold's visit count over the log is exactly 10; cargo test -p lys-identity --test lifecycle_fold d009_r3_ac2 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 3 of 6, d009_r3_ac3: a fixture log of 6 leaves whose sixth, at index 5, is an activate after that identity's retire is refused lifecycle_fold_invalid naming index 5 with a message containing 'verify the leaf'; a second fixture whose first record for an identity, at index 0, is an activate with no register before it is refused naming index 0; the test asserts 2 refusals, 0 states answered, and that each log's leaf count is unchanged after the read; cargo test -p lys-identity --test lifecycle_fold d009_r3_ac3 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 4 of 6, d009_r3_ac4: the listing of every value of the state type has length 4 and no value's name contains 'provision'; the projection's state after the 5 appends of leg 1 equals the fold after reopen, asserted by equality for each of the 5 steps; cargo test -p lys-identity --test lifecycle_fold d009_r3_ac4 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 5 of 6, d009_r3_ac5: after 3 reads of the state of each identity of leg 2, the count of files under the log directory and the SHA-256 of each are equal to those recorded before the first read; cargo test -p lys-identity --test lifecycle_fold d009_r3_ac5 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 6 of 6, the mapping, the wall and red first: rg -n 'fn set_state|fn set_lifecycle' crates/lys-identity/src prints nothing; rg -n 'Log::open|\.append\(' crates/lys-identity/src/lifecycle/fold.rs prints nothing; rg -n 'SystemTime::now|Utc::now|Local::now|Instant::now' crates/lys-identity/src/lifecycle prints nothing; rg -c 'fn \w*d009_r3_ac(1|2|3|4|5)(_|\b)' crates/lys-identity/tests/lifecycle_fold.rs prints 5; on the row's first commit cargo test -p lys-identity --test lifecycle_fold fails to compile or lists the five names as failed.

**Files:**
- create: crates/lys-identity/src/lifecycle/fold.rs
- create: crates/lys-identity/src/lifecycle/state.rs
- create: crates/lys-identity/tests/lifecycle_fold.rs
- modify: crates/lys-identity/src/lifecycle/mod.rs
- modify: crates/lys-identity/src/lifecycle/error.rs
- modify: crates/lys-identity/tests/lifecycle_support/mod.rs

**Checklist:**
- C38 — The state is answered only by folding the identity's transition records in log order over a type of exactly four values; nothing sets a state; a record the table refuses makes the read refuse lifecycle_fold_invalid at its coordinate; reads never write the log (d009_r3_ac1 to d009_r3_ac5).

**Stories:**
- S16 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the administrator, I want to suspend, reinstate or retire an identity only by a signed act whose record is the one thing that changes its state, so that no state is ever set without a record saying who changed it, from what, to what and why.

### R4: Answer an access check as the conjunction, state first, so a suspended or retired identity fails by state alone

THE SYSTEM SHALL answer an access check, whether this identity may perform this action on this resource now, in crates/lys-identity/src/lifecycle/gate.rs as the conjunction in this order: (a) the identity's folded state (R3) is active, else refuse by state, lifecycle_unknown_identity for an identity with no record (act: register it), lifecycle_not_active for registered (act: activate it), lifecycle_suspended for suspended (act: reinstate it), lifecycle_retired for retired (act: none; make a new identity); (b) the grant exists; and (c) the grant is fresh, (b) and (c) both answered by DIRECTORY-006 R4's permission decision (crates/lys-identity/src/grants/permission.rs, its GRANT_FRESHNESS mechanism, docs/design/directory/briefs/DIRECTORY-006.json:140) against the clock that decision names, injected, and passed through with the refusal names that decision gives. WHILE the state leg refuses, THE SYSTEM SHALL NOT ask the grant question: a suspended or retired identity fails every check by state alone, whatever grants it holds, and the grant answerer is called zero times. A check SHALL NOT consult Rauthy, and SHALL ask SpiceDB only through the grant leg. An answered check SHALL commit no event and append nothing. The gate is called from the permission decision by one added line at its entry (the file is DIRECTORY-006 R4's; the line is named by revision, blocked_by), so that in step 1, where nothing answers a permission check (CN11), nothing calls the gate; this row adds no other caller and no route. The grant answerer and the clock SHALL be traits the caller injects; the tests inject a counting answerer and a fixed clock through crates/lys-identity/tests/lifecycle_support/mod.rs, and no code of this row reads a wall clock. Estimate: 3 hours.

**Acceptance:**
- Leg 1 of 6, d009_r4_ac1: an active identity, the answerer primed to say the grant exists and is fresh: the check is admitted and the answerer was called exactly 1 time; cargo test -p lys-identity --test lifecycle_gate d009_r4_ac1 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 2 of 6, d009_r4_ac2: an active identity with the answerer primed to say no grant is refused with the answerer's own grant-missing name; an active identity with the answerer primed to say the grant exists but is stale under the injected clock is refused with the answerer's own stale name; the test asserts 2 refusals, that neither name begins with lifecycle_, and that the answerer was called 2 times; cargo test -p lys-identity --test lifecycle_gate d009_r4_ac2 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 3 of 6, d009_r4_ac3: the same check for an identity that is registered, one that is suspended, one that is retired and one that is unknown, each with the answerer primed to say the grant exists and is fresh: 4 refusals named lifecycle_not_active, lifecycle_suspended, lifecycle_retired and lifecycle_unknown_identity in that order, the answerer called 0 times, and each message containing its act, 'activate', 'reinstate', 'new identity' and 'register' respectively; cargo test -p lys-identity --test lifecycle_gate d009_r4_ac3 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 4 of 6, d009_r4_ac4: an active agent whose answerer holds one grant on one resource: the check is admitted; the administrator suspends the agent and the same check is refused lifecycle_suspended while the answerer's grant count is still 1; the administrator reinstates the agent and the same check is admitted; the administrator retires the agent and the same check is refused lifecycle_retired; the test asserts 2 admissions, 2 refusals and a grant count of 1 throughout; cargo test -p lys-identity --test lifecycle_gate d009_r4_ac4 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 5 of 6, d009_r4_ac5: 20 checks, 10 admitted and 10 refused by state, leave the log's leaf count unchanged and the answerer's mutation count at 0; cargo test -p lys-identity --test lifecycle_gate d009_r4_ac5 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 6 of 6, the mapping, the wall and red first: rg -c 'fn \w*d009_r4_ac(1|2|3|4|5)(_|\b)' crates/lys-identity/tests/lifecycle_gate.rs prints 5; rg -n 'SystemTime::now|Utc::now|Local::now|Instant::now' crates/lys-identity/src/lifecycle/gate.rs prints nothing; git diff --numstat <landed DIRECTORY-006 commit> HEAD -- crates/lys-identity/src/grants/permission.rs prints exactly one line whose two numbers are 1 and 0; on the row's first commit cargo test -p lys-identity --test lifecycle_gate fails to compile or lists the five names as failed.

**Files:**
- create: crates/lys-identity/src/lifecycle/gate.rs
- create: crates/lys-identity/tests/lifecycle_gate.rs
- modify: crates/lys-identity/src/lifecycle/mod.rs
- modify: crates/lys-identity/src/lifecycle/error.rs
- modify: crates/lys-identity/src/grants/permission.rs
- modify: crates/lys-identity/tests/lifecycle_support/mod.rs

**Checklist:**
- C39 — An access check is the conjunction, active and grant exists and grant fresh, state first: a registered, suspended, retired or unknown identity is refused by state with the grant answerer called zero times, the grant legs pass through DIRECTORY-006 R4's decision with its names, and an answered check appends nothing (d009_r4_ac1 to d009_r4_ac5).

**Stories:**
- S17 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the responsible person, I want a suspended or retired agent of mine refused at every check and at its next session refresh by its state alone, whatever grants it holds, so that halting it never depends on finding and revoking every grant first.

### R5: Make a suspension take effect at the next check, where a refresh is a check, and revoke nothing at Rauthy

WHEN the directory service resolves a Rauthy-authenticated session to a registered identity, at sign-in and at every refresh of that session, THE SYSTEM SHALL fold the identity's state first (R3) and refuse a suspended identity lifecycle_suspended (act: reinstate it) and a retired identity lifecycle_retired (act: none; make a new identity), by name, before any grant question is asked and before any session is admitted or renewed, in crates/lys-identity-server/src/lifecycle_gate.rs, called by one added line at the resolution point of DIRECTORY-003's OIDC session handling, whose file is named by revision before dispatch (blocked_by). A suspension or retirement SHALL take effect at the next check and not before: THE SYSTEM SHALL NOT call Rauthy to revoke, end or invalidate a session, SHALL NOT keep a list of sessions to end, and SHALL NOT roll back an action admitted before the transition; a session that outlives a suspension is refused at its next refresh and at every access check before that (R4), by state, whatever grants it holds. A refresh is a check of state alone: an active identity holding no grant is admitted at refresh, and a suspended identity holding a fresh grant is refused at refresh with the grant answerer called zero times. Rauthy stays the source of the sign-in event: this row reads the session Rauthy authenticated and never creates, ends or edits one, and no fork file is touched (ADR-009). This row is the step-2 enforcement the step-1 recording waited for (CN11, ADR-011): once it lands, the directory service refuses a suspended or retired identity's sign-in and refresh on state, and the sign-in-refusal receipt of the receipt brief, where landed, carries the state refusal as its reason; this row itself appends nothing and leaves no receipt. An at-once revocation of the Rauthy session would be a later revision of the suspend transition and is not this row (ADR-028). No wall clock: the row takes its time from the clock the landed server injects, and the tests inject theirs. Estimate: 3 hours.

**Acceptance:**
- Leg 1 of 6, d009_r5_ac1: a registered and active person signs in through the test issuer and is admitted with a session; the administrator suspends the person; the session's next refresh is refused lifecycle_suspended with a message containing 'reinstate', and a fresh sign-in by the same person is refused lifecycle_suspended; the test asserts 1 admission and 2 refusals; cargo test -p lys-identity-server --test lifecycle_session d009_r5_ac1 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 2 of 6, d009_r5_ac2: the test issuer stub records every request it receives; across the suspend and the two refusals of leg 1 the count of requests whose path contains 'logout', 'revoke' or 'session' is 0; the test asserts that count and that the stub's session for the person still exists at the stub after both refusals; cargo test -p lys-identity-server --test lifecycle_session d009_r5_ac2 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 3 of 6, d009_r5_ac3: one check admitted for the person before the suspend is recorded by the harness with its result; after the suspend and the refused refresh the harness's admitted list still has length 1 and the same result; counted by operation, the log gains exactly 1 transition leaf across the suspend (the suspend record) and 0 transition leaves across the two refusals; where crates/lys-receipt/src/receipt.rs is on main (DIRECTORY-007 landed) each of the two refusals leaves exactly 1 leaf of operation sign-in-refused whose reason is lifecycle_suspended, and otherwise the two refusals leave 0 leaves of any operation; cargo test -p lys-identity-server --test lifecycle_session d009_r5_ac3 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 4 of 6, d009_r5_ac4: the administrator reinstates the person and the next refresh is admitted; the administrator retires the person and the next refresh is refused lifecycle_retired with a message containing 'new identity', and a fresh sign-in by the retired person is refused lifecycle_retired; the test asserts 1 admission and 2 refusals; cargo test -p lys-identity-server --test lifecycle_session d009_r5_ac4 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 5 of 6, d009_r5_ac5: an active person holding no grant is admitted at refresh, 1 admission; a suspended person whose answerer holds a fresh grant on a resource is refused at refresh lifecycle_suspended and the answerer was called 0 times; cargo test -p lys-identity-server --test lifecycle_session d009_r5_ac5 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 6 of 6, the mapping, the wall and red first: rg -c 'fn \w*d009_r5_ac(1|2|3|4|5)(_|\b)' crates/lys-identity-server/tests/lifecycle_session.rs prints 5; rg -n 'logout|revoke|invalidate|SystemTime::now|Utc::now|Local::now|Instant::now' crates/lys-identity-server/src/lifecycle_gate.rs prints nothing; git diff --stat <this row's base commit> HEAD -- crates/lys-identity vendor/rauthy prints nothing on this row's two commits; on the row's first commit cargo test -p lys-identity-server --test lifecycle_session fails to compile or lists the five names as failed.

**Files:**
- create: crates/lys-identity-server/src/lifecycle_gate.rs
- create: crates/lys-identity-server/tests/lifecycle_session.rs
- modify: crates/lys-identity-server/src/lib.rs

**Checklist:**
- C40 — At sign-in and at every refresh the directory service refuses a suspended or retired identity by name before any grant question, calls Rauthy to revoke nothing, rolls back no admitted action, and admits an active identity at refresh whatever grants it holds (d009_r5_ac1 to d009_r5_ac5).

**Stories:**
- S17 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the responsible person, I want a suspended or retired agent of mine refused at every check and at its next session refresh by its state alone, whatever grants it holds, so that halting it never depends on finding and revoking every grant first.

### R6: Expose the typed read the screen consumes: the state, the record that put it there, the history, a retired identity readable as a live one

THE SYSTEM SHALL expose, through the standalone identity server, one typed read per identity, in crates/lys-identity-server/src/lifecycle_read.rs with its route declared in crates/lys-identity-server/src/routes.rs beside the record read (its exact path reconciled with the landed manifest, CN9), answering: the identity's name and kind; its folded state (R3); the transition record that put it there, with the operation id, the attested actor and their provenance, from, to, when, the reason given and the log coordinate, as DIRECTORY-003 R2's receipt carries them (docs/design/identity/briefs/IDENTITY-001.json:52), or as the receipt brief's common receipt carries them once landed (blocked_by); the actions available in that state and nothing else, registered: activate; active: suspend, retire; suspended: reinstate, retire; retired: none; every transition record of the identity in log order; and provisioned as a view, true when the identity holds any grant DIRECTORY-006 R5's list answers and false otherwise, never stored and never a state (the credential-handle half of the view is false until a handle exists in the directory, which no row of this cluster yet brings). THE SYSTEM SHALL expose one typed list of the same columns, one row per identity, filterable by state and by kind (docs/design/identity/LIFECYCLE-STATES-2026-09-22.md:57-61). A retired identity SHALL be read exactly as a live one: the same fields, its full history, its retire record as the record that put it there and its actions empty; THE SYSTEM SHALL NOT delete, hide, redact or truncate any audit record of any identity, and no operation of this brief removes a record. A read SHALL commit no event, leave no receipt, and open no log for writing. An unknown identity SHALL be refused lifecycle_unknown_identity (act: register it). The reads admit the caller the server already admits for the record read (DIRECTORY-003 R3, and DIRECTORY-006 R5's administrator inspection) and add no admission of their own. The screen that shows this is not built here: the read is the interface the screen consumes. No wall clock. Estimate: 3 hours.

**Acceptance:**
- Leg 1 of 6, d009_r6_ac1: after the administrator registers and activates one agent, the read answers state active, the record that put it there equal field for field to the activate transition's receipt (operation id, actor, from registered, to active, when, reason, coordinate 1), actions exactly [suspend, retire] and a transition list of length 2; cargo test -p lys-identity-server --test lifecycle_read d009_r6_ac1 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 2 of 6, d009_r6_ac2: after the administrator suspends and then retires that agent, the read answers state retired, the record that put it there is the retire record at coordinate 3, actions are empty, and the transition list has length 4 in the order register, activate, suspend, retire, each naming its actor; 3 further reads leave the log's leaf count unchanged; cargo test -p lys-identity-server --test lifecycle_read d009_r6_ac2 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 3 of 6, d009_r6_ac3: an active agent with no grant reads provisioned false; after the administrator gives it one grant through DIRECTORY-006's act it reads provisioned true with state still active and a transition list still of length 2; after the grant is revoked it reads provisioned false; the test asserts the 3 readings and that 0 lifecycle records were added; cargo test -p lys-identity-server --test lifecycle_read d009_r6_ac3 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 4 of 6, d009_r6_ac4: with a fixture of 4 identities, one in each state, two of them agents, the list filtered by each state returns exactly 1 row, filtered by kind agent returns exactly 2, unfiltered returns 4, and the retired identity's row is present unfiltered with its retire record as the record that put it there; cargo test -p lys-identity-server --test lifecycle_read d009_r6_ac4 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 5 of 6, d009_r6_ac5: the read of an identity that does not exist is refused lifecycle_unknown_identity with a message containing 'register', 1 refusal; and rg -n 'fn delete|fn hide|fn redact|fn remove|fn truncate' crates/lys-identity-server/src/lifecycle_read.rs crates/lys-identity/src/lifecycle prints nothing; cargo test -p lys-identity-server --test lifecycle_read d009_r6_ac5 prints 'test result: ok. 1 passed; 0 failed'.
- Leg 6 of 6, the mapping, the wall and red first: rg -c 'fn \w*d009_r6_ac(1|2|3|4|5)(_|\b)' crates/lys-identity-server/tests/lifecycle_read.rs prints 5; rg -n 'SystemTime::now|Utc::now|Local::now|Instant::now' crates/lys-identity-server/src/lifecycle_read.rs prints nothing; git diff --numstat <this row's base commit> HEAD -- crates/lys-identity-server/src/routes.rs prints one line whose second number is 0; on the row's first commit cargo test -p lys-identity-server --test lifecycle_read fails to compile or lists the five names as failed.

**Files:**
- create: crates/lys-identity-server/src/lifecycle_read.rs
- create: crates/lys-identity-server/tests/lifecycle_read.rs
- modify: crates/lys-identity-server/src/routes.rs
- modify: crates/lys-identity-server/src/lib.rs

**Checklist:**
- C41 — One typed read answers an identity's state, the record that put it there, the actions available in that state, its full history and provisioned as a view, and one typed list is filterable by state and kind; a retired identity reads exactly as a live one and no operation deletes, hides, redacts or truncates a record (d009_r6_ac1 to d009_r6_ac5).

**Stories:**
- S18 (Administrator, Suspends, reinstates and retires identities, and reads why a check refused) — As the administrator, I want the screen to show an identity's state and the record that put it there, for a retired identity exactly as for a live one, so that I can answer why a check refused from the record and not from memory.

## Boundaries

- Only the files listed in this brief's requirements change; a row that needs a file outside its wall stops and names it, and the lead approves a brief revision before that file is edited (CN9). Files of DIRECTORY-003, DIRECTORY-006 and DIRECTORY-007 are touched only where a row names one added line or added route lines: crates/lys-identity/src/grants/permission.rs (R4), the session resolution point (R5), crates/lys-identity-server/src/routes.rs (R6) and the two lib.rs module lists; nothing else of theirs is edited and no file of DIRECTORY-007 is.
- The state is folded from the signed transition records and never set directly (ADR-027, CN19): no API, migration or repair writes a state, and a read answers the fold or refuses by name.
- The four states are the lifecycle document's and no fifth is added; provisioned is a view and never a state; the transition table is DIRECTORY-003 R5's and no transition is added, so a retired identity is never reactivated.
- No production node, no production key, test identities only, no receipt outside a test, and development isolation throughout (CN2): the three acts the anchor decisions reserve (docs/design/lys-anchor/DECISIONS.md:333-335, docs/design/decisions.json:139) stay reserved.
- No change to Cambium or Aion, no fork change and no signing key inside Rauthy (ADR-009), and no call to Rauthy to revoke, end or invalidate a session (ADR-028, CN20): a suspension or retirement takes effect at the next check, where a refresh is a check, and no admitted action is rolled back. An at-once revocation is a later revision of the suspend transition, not this brief, and whether suspending also ends the sign-in session at Rauthy stays open as the design records it.
- Everything the design records as open stays open: whether a reason is mandatory on suspend and retire, whether an agent's activation needs a credential handle, whether a grant an agent delegated onward falls with its suspension, the grant representation, and the name of the identity service. No policy actor causes a transition in this brief.
- An answered check commits no event and leaves no receipt; a read commits no event; nothing here appends to the log except the transitions DIRECTORY-003 R5's commit path appends.
- No audit record is deleted, hidden, redacted or truncated by any row; a retired identity's history is read exactly as a live identity's (CN21).
- The screen of step 7 is not built here; R6 is the typed read it consumes. Rauthy stays the source of the sign-in event and SpiceDB the answer to the grant question; neither is asked by a state leg.
- No wire format is frozen by this brief: the transition record's signed encoding stays with docs/design/identity/IDENTITY-EVENTS.md under DIRECTORY-003's joint review; lys-core, lys-log-store and every shipped tag are unchanged.
- No wall clock in any new code; no name of a person and no date inside code; no credential, token or key value in code, tests, fixtures, logs or documents.
- A live demonstration is a verification step a person performs after a row lands, never an acceptance criterion (CN5). No row is dispatched until the lead has reviewed it.

## Verification

- From the repository root: sh scripts/design/gate.sh exits 0, and python3 scripts/design/validate.py docs/design/decisions.json and python3 scripts/design/validate.py docs/design/roadmap.json each exit 0.
- From the repository root, after each code row: cargo fmt --all; cargo clippy --all-targets --all-features -- -D warnings; cargo clippy --all-targets -- -D warnings; cargo test --workspace --all-features; cargo doc --no-deps --all-features; cargo doc --no-deps, all clean, with git status showing no change from cargo fmt (gates before any commit; heavy runs at the gate venue, CN8).
- From the repository root: rg -c 'fn \w*d009_r2_ac(1|2|3|4)(_|\b)' crates/lys-identity/tests/lifecycle_causes.rs prints 4; rg -c 'fn \w*d009_r3_ac(1|2|3|4|5)(_|\b)' crates/lys-identity/tests/lifecycle_fold.rs prints 5; rg -c 'fn \w*d009_r4_ac(1|2|3|4|5)(_|\b)' crates/lys-identity/tests/lifecycle_gate.rs prints 5; rg -c 'fn \w*d009_r5_ac(1|2|3|4|5)(_|\b)' crates/lys-identity-server/tests/lifecycle_session.rs prints 5; rg -c 'fn \w*d009_r6_ac(1|2|3|4|5)(_|\b)' crates/lys-identity-server/tests/lifecycle_read.rs prints 5.
- For each code row, the row's first commit adds only its test file and fails by name, and its second commit passes; the review reads both commits.
- The row diffs touch no path under vendor/rauthy or docs/design/identity/briefs/, no file of DIRECTORY-007, and of DIRECTORY-003's and DIRECTORY-006's files only the named added lines: git diff --stat <base> HEAD -- vendor/rauthy docs/design/identity/briefs prints nothing, and git diff --numstat <base> HEAD -- crates/lys-identity/src/grants/permission.rs crates/lys-identity-server/src/routes.rs prints lines whose second number is 0.
- rg -n 'SystemTime::now|Utc::now|Local::now|Instant::now' crates/lys-identity/src/lifecycle crates/lys-identity-server/src/lifecycle_gate.rs crates/lys-identity-server/src/lifecycle_read.rs prints nothing; rg -n 'fn set_state|fn set_lifecycle' crates/lys-identity/src prints nothing; rg -n 'logout|revoke|invalidate' crates/lys-identity-server/src/lifecycle_gate.rs prints nothing; and a search of every file this brief touches finds no credential, token or key value.
- The ledgers are append-only: git diff -U0 <base> HEAD -- docs/design/roadmap.json docs/design/decisions.json docs/design/directory/checklist.json docs/design/directory/stories.json | grep -c '^-[^-]' prints 0.
