{
  "cluster": "home",
  "title": "The home: a session held under its identity, resumable by any harness that can be measured",
  "intention": "A session's history lives under the agent's identity, not under a harness. The record is what the model was given and what came back, in order, with each provider's native blocks kept whole. A harness's own resume file is rendered from that record on demand, so an agent moves machines, harnesses or providers and continues; the original bytes are never rewritten. Code this cluster built before the chain existed is judged by the chain as it stands, and a card is done only when that judgement holds on the rows it carries: done means judged, never merely merged.",
  "problem": "Today a session exists only as its harness's file. Archie's Claude Code file measured 141,931,097 bytes on 24 September 2026: the conversation itself is under 3% of it, tool results 14%, thinking 6%, and about 60% is harness bookkeeping the model never sees; Waffles' file is 3.37 GB and Claude Code loads it whole. Nothing central holds a session, so nothing can render a smaller working session, move one to another device, swap provider and come back, or say what a session was given. Norn already has the canonical event model (SessionEvent, conversion to provider messages, compaction as a derived event, provider epoch boundaries) proved by its own tests; Claude Code has no importer to it and no renderer from it. HOME-001's rows R1 to R11 were built by hand and reached main before every carded row had to pass through its board's chain, so nothing on main records the chain's judgement of crates/lys-home, lys has no ast-grep rule set to judge it with, and two of those rows (R7, R10) have no code on main at all.",
  "solution": "Adopt Pi's session tree as the home record (Tom, Dot 13:27 and 13:28: Pi's tree, not Norn): one append-only JSONL per session, a header line, then entries each carrying id, parentId and timestamp, a leaf pointer for the current position, forks by moving the pointer, compaction and branch summaries as entries that keep their originals. lys adds nothing to that grammar: harness events (approvals, tool completion) and proxy call records ride Pi's custom entry type under lys customType names, so a home file stays readable by Pi's own parser. Two captures feed it: the model traffic through the door's proxy (the same process that swaps the credential, SECRETS-002 R1), and the harness's own events from its transcript. Content blocks are stored once by hash; entries reference them. A harness resume file is a rendered projection of the root-to-leaf path: for Claude Code, a JSONL written under a chosen uuid at the harness's own path, then resumed by that id with --fork-session. Provider-native reasoning stays on the message with its provider, api and model and is rendered whole only to the same three; another model gets readable thinking as text and opaque blocks dropped, each named in a loss account. lys grants say who may read and resume. Every resume path is measured on a named harness version before anything relies on it.\n\nHOME-002 brings the hand-built rows under the chain without touching them. It measures crates/lys-home exactly as it stands at 0073b9660f00ecd3ca6f13ffd3a9e59aabd8e6ff and writes docs/design/home/PROOF-CHAIN.md: the commit measured and the tools measured with; one Jev verdict per file (jev_ask, the whole file as a diff against the empty tree, the path as the part); fmt in its checking form; the two clippy legs, which are clippy pedantic because the workspace lints set pedantic to warn under -D warnings; the workspace test run with lys-home's executed tests counted; an ast-grep scan under the estate's four sweep rules (ADR-013), which this card adds as configuration; and .land/gates.sh. Every finding a named check reports is its own line, naming the HOME-001 rows it holds (the rows naming its file, the file a test sibling tests, or the card named by the pull request that introduced its lines), and becomes its own card; what the author observes and no check reports sits under its own heading and is carded only on the lead's word. A table reads HOME-001 at that commit and records, row by row, which named files exist; a row is built only when every file it names, code and document alike, is there, so no card is set done over work that does not exist, and a row whose code is there without its document is a finding. src_land's Jev judges this card's landing diff, which is the proof and design rows; the per-file verdicts are the crate's, and the proof says so. The seven step 5 cards are set done only on a green landing that records no finding (ADR-012).",
  "principles": [
    {
      "id": "P1",
      "text": "The original bytes are never rewritten. A rendered resume file, a compaction and a translation are derived records stored beside their source and pointing at it (CONTEXT-ROADMAP-2026-09-22.md stage 4)."
    },
    {
      "id": "P2",
      "text": "The record is Pi's session tree as read from the Pi checkout at 3d5cbe98 (packages/coding-agent/src/core/session-manager.ts: SessionHeader, SessionEntryBase {type, id, parentId, timestamp}, message, model_change, compaction {summary, firstKeptEntryId}, branch_summary {fromId, summary}, label, custom {customType, data}). lys adds entry kinds only as custom entries; it never adds a field to Pi's grammar and never adopts Norn's SessionEvent (Tom, Dot 13:28)."
    },
    {
      "id": "P3",
      "text": "A provider's opaque blocks (Anthropic signed thinking, OpenAI encrypted reasoning) stay on the assistant message with its provider, api and model, and are rendered whole only when all three match the target (Pi transform-messages.ts:95-109); another model gets readable thinking as plain text and opaque blocks dropped, each named by hash in the loss account (Tom, Dot 13:24: keep reasoning traces per provider so a session can swap and swap back)."
    },
    {
      "id": "P4",
      "text": "A content block is stored once by its hash; requests that resend the whole conversation reference blocks, they do not copy them."
    },
    {
      "id": "P5",
      "text": "Capture the model traffic and the harness's local events both, so the two can be mapped against each other (Tom, Dot 13:23); neither alone is the record."
    },
    {
      "id": "P6",
      "text": "A resume path is a per-harness, per-version measurement, never an assumption: Claude Code 2.1.281's --resume takes a session id and reads ~/.claude/projects/<cwd-slug>/<id>.jsonl; a seeded two-record file resumed there at 13:25 on 24 September and answered from its content."
    },
    {
      "id": "P7",
      "text": "Transcript contents never appear in a post, a log line, an error, a test name or a rendered page; status carries hashes, counts and offsets only (CONTEXT-001 privacy rule)."
    },
    {
      "id": "P8",
      "text": "A sandbox or a VM is a target profile the launch template renders into, never a special case in the core; credentials are supplied at launch on the target and never carried in the home (Tom, Dot 13:30; CONTEXT-ROADMAP stage 3)."
    },
    {
      "id": "P9",
      "text": "The canon is one curated, versioned series of examples every new session starts from: each entry is one rule stated short plus one real exchange that shows it lived (verify before claiming, a correction taken well, a refusal named, careful work), drawn from every agent's sessions, distilling the collective experience so far: 'our learnings in one another' (Tom, Dot 13:44 to 13:46; Waffles 0169c353). It is not a letter from one session to its successor. It lives in the lys repository at canon/canon.jsonl in Pi's grammar and changes only through src_commit and review, like code. Genuine thinking is kept whole where a real turn produced it and replays only to the same provider, api and model; thinking is never authored."
    },
    {
      "id": "P10",
      "text": "A handover is a letter the outgoing session writes to its successor in its own real thinking and answer; it enters the successor as a lys.inherited entry marked as a predecessor's memory, never as the successor's own experience, and replays only to the same provider, api and model. No thinking is ever authored; only thinking a model produced is kept (Tom, Dot 13:38 to 13:40: 'you are waking up ... I am another one, I'm helping you have part of my memory ... like a parent imparting a wish to a child'; Waffles 52d53451)."
    },
    {
      "id": "P11",
      "text": "A proof of code already on main measures and never mends: it names the commit it measured, runs each check in its non-rewriting form, records every finding as its own line, and leaves every fix to a card of its own."
    }
  ],
  "decisions": [
    "ADR-001",
    "ADR-003",
    "ADR-004",
    "ADR-012",
    "ADR-013"
  ],
  "goals": [
    "A few-shot session file written by hand resumes Claude Code by path, from a directory outside the config root, with the file preserved and the demonstration marked authored.",
    "One real Claude Code session imported into the common record, rendered back, and resumed under a new id on 2.1.281 without repeating a completed tool action, with the original file's hash unchanged.",
    "One seat with a subscription login making calls through a pass-through proxy, so the tee has somewhere to live.",
    "A written loss account for every derived record: what a render or translation preserved, transformed and could not carry.",
    "The canon, one curated versioned series of short examples each showing a rule lived, seeds every new session, and its effect is measured on a card against a plain start.",
    "A handover letter from an outgoing session seeds its successor as inherited memory, with the model's own thinking intact, and the effect is measured on a card against a plain start.",
    "PROOF-CHAIN.md records the chain's whole judgement of crates/lys-home at 0073b9660f00ecd3ca6f13ffd3a9e59aabd8e6ff: every check by name, command and outcome, one Jev verdict per file, every finding as its own line, with no byte of the crate changed."
  ],
  "non_goals": [
    {
      "text": "Anchoring, signing or receipts into a lys log (CONTEXT-ROADMAP stage 6; when asked for).",
      "reason": "Signing comes when asked for (Tom, 22 September 16:27); every stage here works without it."
    },
    {
      "text": "Encryption at rest and moving a home between devices (stage 3 preconditions).",
      "reason": "Stage 3's three preconditions (identity and read authority, encryption before bytes leave, the resume evidence) are their own brief."
    },
    {
      "text": "Harnesses other than Claude Code, and Chat Completions or Responses translation beyond keeping the raw call bytes.",
      "reason": "One harness proved first; each other harness is its own profile and its own measurement."
    },
    {
      "text": "Lanterns and forks at a coordinate (stages 5 and 5b).",
      "reason": "Lanterns and forks stand on a proved resume; this brief supplies that proof."
    },
    {
      "text": "Adopting, wrapping or calling Norn's session code; Pi's code is read as the reference and not vendored.",
      "reason": "Tom, Dot 13:28: not Norn. Pi's tree is the reference."
    },
    {
      "text": "Fixing any finding the proof records, or any new capability in lys-home.",
      "reason": "The card proves what is there; each finding becomes its own card on the step 5 board."
    },
    {
      "text": "Adding ast-grep to .land/gates.sh or docs/design/project.json.",
      "reason": "Changing the gate changes it for every later card; it is its own unit."
    },
    {
      "text": "Building HOME-001 R7 and R10 (the pass-through proxy proof and lys-proxy).",
      "reason": "They have no code on main, so there is nothing to judge; they are built through the chain as their own cards."
    },
    {
      "text": "A chain step that runs Jev per file over code already on main.",
      "reason": "It recurs for every row built before the chain; it belongs in the chain as its own unit, not in this card."
    }
  ],
  "structure": [
    {
      "path": "docs/design/home/briefs/HOME-001.json",
      "note": "the first brief: common record, Claude Code importer and renderer, the two proofs",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/briefs/HOME-001.md",
      "note": "its rendered markdown",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-RESUME.md",
      "note": "the measured Claude Code resume: version, command, hashes before and after, what repeated",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-PROXY.md",
      "note": "the measured subscription login through a pass-through proxy: version, headers that mattered, what failed",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/LOSS-ACCOUNT.md",
      "note": "what the Claude Code render preserves, transforms and cannot carry",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/blocks.rs",
      "note": "content-addressed block store: put by SHA-256, get by hash, never rewritten",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/import.rs",
      "note": "Claude Code JSONL into SessionEvents plus blocks",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/render.rs",
      "note": "SessionEvents into a Claude Code JSONL under a chosen uuid, with the loss account",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/mod.rs",
      "note": "the Claude Code harness profile: transcript root, cwd slug, version measured",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/cli.rs",
      "note": "import, render, resume-check subcommands",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/examples/passthrough.rs",
      "note": "a pass-through HTTP proxy that forwards to the provider unchanged, for the proof only",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/tests/claude_code_round_trip.rs",
      "note": "import then render equals the model-visible content; opaque blocks kept whole",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/mod.rs",
      "note": "the home record: Pi's session tree read and written, leaf pointer, root-to-leaf path",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/entries.rs",
      "note": "Pi's entry types as Rust types, plus the lys custom entries lys.harness_event and lys.call",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/call.rs",
      "note": "a proxy call record: request and response block hashes, provider, api, model, timing",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/events.rs",
      "note": "Claude Code's harness-local records (hooks, permission mode, tool completion) as lys.harness_event entries",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/RECORD.md",
      "note": "the home record written down: Pi's grammar as adopted, the two lys custom entries, the block store, the loss account",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/lib.rs",
      "note": "module wiring: record and harness",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/mod.rs",
      "note": "harness profiles; Claude Code first",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/Cargo.toml",
      "note": "the crate manifest; gains the passthrough example",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/README.md",
      "note": "what the tool does and does not do",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/index.rs",
      "note": "the offset index and the persisted head: a path is read by seeking, never by loading the file",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/mod.rs",
      "note": "the proxy module: declarations only",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/link.rs",
      "note": "how a call is linked to a session: the measured request key (Claude Code's request metadata) now, the seat's handle when the door's proxy exists",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/bin/lys-proxy.rs",
      "note": "the proxy binary",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-FEWSHOT.md",
      "note": "the first proof: a hand-written few-shot session file resumed by path from a directory outside the config root",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/canon.rs",
      "note": "the canon: copy chosen real exchanges (or a clearly authored example) into canon.jsonl with a lys.inherited entry naming each source; seed a new session from it",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/canon_tests.rs",
      "note": "gates on the canon: copied whole, never twice, no authored thinking, rendered first",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-CANON.md",
      "note": "measured: a session started from the canon against a plain start, on one card",
      "brief": "HOME-001"
    },
    {
      "path": "canon/canon.jsonl",
      "note": "the canon itself, versioned by the repository, changed only through src_commit and review",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/handover.rs",
      "note": "the handover: take the outgoing session's letter turn (thinking intact) and seed the successor with it as lys.inherited",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-HANDOVER.md",
      "note": "measured: a successor seeded with an inherited letter, whether 2.1.281 replays the signed thinking, and seeded against plain on one card",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/forward.rs",
      "note": "forward a request to the provider and stream the response back unchanged",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/capture.rs",
      "note": "bounded spooling of request and response bodies to files while forwarding, handed to the R6 sink",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/journal.rs",
      "note": "the open-call journal: a call id written before forwarding, retired after ingest, so a restart records lost calls once",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream.rs",
      "note": "the coordinator of the stream readers: picks the grammar by api and hands each frame on as it passes",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_sse.rs",
      "note": "SSE framing: events and data lines out of a byte stream, retaining byte order, encoding and trailers",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_messages.rs",
      "note": "the Messages event grammar assembled into response parts",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_chat.rs",
      "note": "the Chat Completions chunk grammar assembled into response parts",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_responses.rs",
      "note": "the Responses event grammar assembled into response parts",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/error.rs",
      "note": "the proxy's errors and outcomes, named",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/forward_tests.rs",
      "note": "forwarding against a loopback fake",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_tests.rs",
      "note": "the three grammars, partial streams",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/link_tests.rs",
      "note": "linking by the measured key; unlinked; never inferred",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/journal_tests.rs",
      "note": "recovery after a kill: one lost record per open call",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/briefs/HOME-002.json",
      "note": "the proof card: the chain's whole judgement over crates/lys-home as found",
      "brief": "HOME-002"
    },
    {
      "path": "docs/design/home/briefs/HOME-002.md",
      "note": "its rendered markdown",
      "brief": "HOME-002"
    },
    {
      "path": "docs/design/home/PROOF-CHAIN.md",
      "note": "the commit measured, each check's command and outcome, one Jev verdict per file, the findings one per line, the row table",
      "brief": "HOME-002"
    },
    {
      "path": "sgconfig.yml",
      "note": "the ast-grep project root: ruleDirs rules/ast-grep",
      "brief": "HOME-002"
    },
    {
      "path": "rules/ast-grep/mod-rs-declarations-only.yml",
      "note": "the estate sweep rule mod-rs-declarations-only, byte-identical to cambium's",
      "brief": "HOME-002"
    },
    {
      "path": "rules/ast-grep/no-let-underscore-on-results.yml",
      "note": "the estate sweep rule no-let-underscore-on-results, byte-identical to cambium's",
      "brief": "HOME-002"
    },
    {
      "path": "rules/ast-grep/no-lint-bypass-attributes.yml",
      "note": "the estate sweep rule no-lint-bypass-attributes, byte-identical to cambium's",
      "brief": "HOME-002"
    },
    {
      "path": "rules/ast-grep/no-std-mutex-in-async.yml",
      "note": "the estate sweep rule no-std-mutex-in-async, byte-identical to cambium's",
      "brief": "HOME-002"
    },
    {
      "path": "docs/design/home/DESIGN.md",
      "note": "design.json rendered",
      "brief": ""
    },
    {
      "path": "docs/design/home/CHECKLIST.md",
      "note": "checklist.json rendered",
      "brief": ""
    },
    {
      "path": "docs/design/home/USER-STORIES.md",
      "note": "stories.json rendered",
      "brief": ""
    }
  ],
  "inventory": [
    {
      "path": "docs/design/identity/STATEMENT-2026-09-22.md",
      "note": "the authority: 'The home', 'The home is portable: lanterns, translation and forks', and steps 4 and 5"
    },
    {
      "path": "docs/design/identity/CONTEXT-ROADMAP-2026-09-22.md",
      "note": "Archie's roadmap; this cluster is its stage 4 and the resume half of stage 3"
    },
    {
      "path": "docs/design/identity/briefs/CONTEXT-001.json",
      "note": "stage 1, byte-for-byte capture and the lys-home crate this cluster extends"
    },
    {
      "path": "docs/design/secrets/briefs/SECRETS-002.json",
      "note": "the door's proxy (R1) that the tee joins once it exists"
    },
    {
      "path": "$PI/packages/coding-agent/src/core/session-manager.ts",
      "note": "Pi's session tree at 3d5cbe98 (github.com/earendil-works/pi): header, entries, leaf (branch at :1125 moves an in-memory cursor; _buildIndex:754 restores the last physical entry on reopen; loadEntriesFromFile:438 reads the whole file)"
    },
    {
      "path": "$PI/packages/ai/src/api/transform-messages.ts",
      "note": "per-provider thinking: same provider, api and model keeps blocks whole; otherwise text, opaque dropped"
    },
    {
      "path": "$PI/packages/coding-agent/src/core/branch-summarization.ts",
      "note": "how an abandoned branch's work is carried into the new one"
    },
    {
      "path": "$CCFLARE/README.md",
      "note": "ccflare at 95c4c6a (github.com/snipeship/ccflare): the concept of a native pass-through that keeps request history; reference only, not used (Tom, Dot 13:34)"
    },
    {
      "path": "crates/lys-home",
      "note": "at 0073b9660f00ecd3ca6f13ffd3a9e59aabd8e6ff: 25 files (23 .rs, Cargo.toml, README.md), 36 #[test] functions, all entered in one commit (217e81b); the thing judged, as found"
    },
    {
      "path": ".land/gates.sh",
      "note": "the gate: scripts/design/gate.sh, cargo fmt --check, clippy in both feature shapes, cargo test --workspace --all-features, cargo doc in both shapes; every leg runs, red if any leg is"
    },
    {
      "path": "scripts/design/gate.sh",
      "note": "the design leg: validate, coverage and rendered markdown equal to what the JSON renders"
    },
    {
      "path": "Cargo.toml",
      "note": "[workspace.lints.clippy] sets pedantic = warn with unwrap_used, expect_used, panic and others; lys-home inherits them"
    },
    {
      "path": "rust-toolchain.toml",
      "note": "pins the toolchain at 1.97.1 with clippy and rustfmt"
    },
    {
      "path": "$CAMBIUM/rules/ast-grep",
      "note": "the estate sweep rules at df4dca5de6899df89b06b94cdeeec3619877fc1d: mod-rs-declarations-only, no-let-underscore-on-results, no-lint-bypass-attributes, no-std-mutex-in-async (haematite carries the same four byte for byte)"
    }
  ],
  "constraints": [
    {
      "id": "CN1",
      "text": "No file under ~/.claude/projects is rewritten or truncated by this cluster; a render writes a new file under a new uuid only, and refuses an existing path by name."
    },
    {
      "id": "CN2",
      "text": "A rendered file never carries a provider-native opaque block that came from a different provider or model family than the one it is rendered for; the loss account names each block dropped by hash."
    },
    {
      "id": "CN3",
      "text": "Transcript contents never appear in output, logs, errors, test names or pages; hashes, counts, offsets and event ids only."
    },
    {
      "id": "CN4",
      "text": "Every home file parses with Pi's parseSessionEntries at 3d5cbe98 unchanged: the header line first, every entry with id, parentId and timestamp, lys data only inside custom entries."
    },
    {
      "id": "CN5",
      "text": "The pass-through proxy forwards every header and the streamed body unchanged and stores nothing but the measurement; it is an example binary, never a service."
    },
    {
      "id": "CN6",
      "text": "No Norn crate is a dependency of lys-home and no Norn type is copied into it."
    },
    {
      "id": "CN7",
      "text": "Reading the root-to-leaf path of a home file reads only the entries on that path plus the index, never the whole file (Pi loads the whole journal; Chippy 13:33); the head is persisted beside the file, never inferred from the last physical entry on reopen."
    },
    {
      "id": "CN8",
      "text": "HOME-002 changes no byte under crates/lys-home: no fmt rewrite, no clippy fix, no test added; git diff 0073b9660f00ecd3ca6f13ffd3a9e59aabd8e6ff <card head> -- crates/lys-home is empty."
    }
  ],
  "gate": [
    {
      "tree": ".",
      "shorthand": {
        "rust-build": [
          "toolchain:rust",
          "lockfile:Cargo.lock"
        ]
      },
      "legs": [
        {
          "name": "fmt",
          "command": "cargo fmt --all",
          "requires": [
            "tool:cargo",
            "tool:rustfmt"
          ],
          "cadence": "round"
        },
        {
          "name": "clippy-all-features",
          "command": "cargo clippy --all-targets --all-features -- -D warnings",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "clippy",
          "command": "cargo clippy --all-targets -- -D warnings",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "tests",
          "command": "cargo test --workspace --all-features",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "doc-all-features",
          "command": "cargo doc --no-deps --all-features",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "doc",
          "command": "cargo doc --no-deps",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "design",
          "command": "sh scripts/design/gate.sh",
          "requires": [
            "tool:sh",
            "tool:python3"
          ],
          "cadence": "round"
        }
      ]
    }
  ]
}
