{
  "cluster": "home",
  "title": "The home: a session held under its identity, resumable by any harness that can be measured",
  "intention": "A session's history lives under the agent's identity, not under a harness. The record is what the model was given and what came back, in order, with each provider's native blocks kept whole. A harness's own resume file is rendered from that record on demand, so an agent moves machines, harnesses or providers and continues; the original bytes are never rewritten.",
  "problem": "Today a session exists only as its harness's file. Archie's Claude Code file measured 141,931,097 bytes on 24 September 2026: the conversation itself is under 3% of it, tool results 14%, thinking 6%, and about 60% is harness bookkeeping the model never sees; Waffles' file is 3.37 GB and Claude Code loads it whole. Nothing central holds a session, so nothing can render a smaller working session, move one to another device, swap provider and come back, or say what a session was given. Norn already has the canonical event model (SessionEvent, conversion to provider messages, compaction as a derived event, provider epoch boundaries) proved by its own tests; Claude Code has no importer to it and no renderer from it.",
  "solution": "Adopt Pi's session tree as the home record (Tom, Dot 13:27 and 13:28: Pi's tree, not Norn): one append-only JSONL per session, a header line, then entries each carrying id, parentId and timestamp, a leaf pointer for the current position, forks by moving the pointer, compaction and branch summaries as entries that keep their originals. lys adds nothing to that grammar: harness events (approvals, tool completion) and proxy call records ride Pi's custom entry type under lys customType names, so a home file stays readable by Pi's own parser. Two captures feed it: the model traffic through the door's proxy (the same process that swaps the credential, SECRETS-002 R1), and the harness's own events from its transcript. Content blocks are stored once by hash; entries reference them. A harness resume file is a rendered projection of the root-to-leaf path: for Claude Code, a JSONL written under a chosen uuid at the harness's own path, then resumed by that id with --fork-session. Provider-native reasoning stays on the message with its provider, api and model and is rendered whole only to the same three; another model gets readable thinking as text and opaque blocks dropped, each named in a loss account. lys grants say who may read and resume. Every resume path is measured on a named harness version before anything relies on it. A session is launched on a harness from a launch template kept in the home (ADR-012): one JSON object per harness, schema in docs/design/home/launch-template.schema.json, stored under templates/ by its SHA-256 beside sessions/ and blocks/, so a template's version is its hash. Its named slots map the home onto the harness: the transcript (the one slot no template maps generically, so the template names how the harness fills it; Claude Code fills it by resuming the rendered file by path with --fork-session, since a bare resume writes onto the rendered file's own name), the MCP configuration, the environment, the secrets (use-only ones written as their handle; readable ones refused until the secrets rows build the broker reader, ADR-001) and the appended instructions. lys-home render-launch reads a template and a session, writes the R4 render, its loss account, an MCP file, an environment file and an instructions file into one directory, and prints the launch line in its report without running it (ADR-007). Each render is recorded on the session as a lys.harness_event of the sixth kind, template_render, hung as a side leaf beside the context path so the head and the session head hash (SHA-256 of the head entry's line) do not move; the written paths ride in a manifest block the event names by hash, under the 512-byte cap. At render, the launch template also records the context record: a lys.given custom entry after the render event naming, by path, byte length and SHA-256 only, the instruction documents Claude Code will load for the session's working directory and the files the render wrote, in the order measured on a named Claude Code version, with the environment variable names the template set and the kinds of document resolved and left unlisted (@-imports and .claude/rules, which a second lys.given entry at the first request records), so a reader sees what was measured and can check a file on disk against it without anyone reading its contents. Lanterns (stage 5, HOME-004) are the first thing that stands on the record: a lantern is a lys.lantern custom entry lit on purpose and appended at a session's head, naming an existing non-lantern entry of that session as its point (ADR-014); its note grows only by lys.lantern_epilogue entries appended after it, so a lantern's story is its entry followed by its epilogues and nothing is rewritten. Recall reads every session of the home without owning any of it: a read-only reader takes no lock and writes no index, seeks to lantern and epilogue rows by the index's custom column (CN7), and names any session it could not read rather than shortening the list in silence. A lantern lives in the home only; the Claude Code renderer already leaves custom entries out. A render is one function of the session head, the target (session id, cwd, model, version, canon) and the lys-home version: the same inputs give the same bytes (CN9), so a rendered file is told by its hash and a receipted render event can name what was written. The walk takes the order the record gives, entry order along the context path then part order within a message, never a map's or a set's order. A value the render needs that the record does not hold is derived from the record, never drawn fresh: a timestamp is the entry's own stamp, and a Claude Code uuid for an entry whose id is not already uuid-shaped (the importer's `<uuid>-r<i>` ids for split tool results, hand-authored ids, canon ids) is a UUID version 5 (RFC 9562, SHA-1 name-based) under the session's namespace over the name `<entry id>#<role>`. The session's namespace is UUIDv5 of one fixed lys namespace, 32c05904-d1f1-550c-9eee-2f6c8f98b665, over the id of the session being rendered; that lys namespace is itself UUIDv5 of the RFC 9562 URL namespace (6ba7b811-9dad-11d1-80b4-00c04fd430c8) over the name `lys/home/claude-code/render-uuid/v1`. The session in the namespace is the salt: the same hand-authored or split-result id in two sessions never derives one uuid, and the target session id, which the record does not hold, is never mixed in. The name is the entry's own id, then `#`, then the role the id plays in the rendered file, as UTF-8 bytes; roles carry no `#`, so the last `#` splits a name unambiguously and two roles on one entry never share a name. The roles are closed: `record`, the rendered record's `uuid`. The parentUuid of the next record, a summary's leafUuid and an assistant's `msg_` id follow from it unchanged. A uuid-shaped entry id passes through as it is, so an imported Claude Code session keeps its source's uuids. A derived uuid carries version nibble 5 and Claude Code's own uuids carry 4, so a derived uuid cannot equal an imported one. The namespace, the session namespace rule, the name form and the roles are fixed (ADR-016); the fork and launch cards derive their ids by this scheme, and a change to it is a new version alongside, never a mutation. The fork (stage 5b, HOME-006) is the pathway a lantern makes: lys-home fork resolves a lantern to the session it was lit in, cuts that session's root-to-point chain at the last assistant message at or before the point, reading through the index, and writes a child session under the parent's cwd whose header's parentSession is the parent file's path relative to the home, holding the parent's own line bytes for each cut entry, then a lys.forked_from entry naming the parent, the lantern, the point and the cut (ADR-017); the parent gains one lys.fork entry at its head naming the child. Nothing else is copied and no block is written. A user-message point is carried, not copied: the child's Claude Code render writes its text parts as a seed prompt beside the rendered file under an in-band marker line, and the template's launch line, printed by render-launch only, passes it as the first prompt (ADR-018). The child renders and launches like any session, through the launch template.",
  "principles": [
    {
      "id": "P1",
      "text": "The original bytes are never rewritten. A rendered resume file, a compaction and a translation are derived records stored beside their source and pointing at it (CONTEXT-ROADMAP-2026-09-22.md stage 4)."
    },
    {
      "id": "P2",
      "text": "The record is Pi's session tree as read from the Pi checkout at 3d5cbe98 (packages/coding-agent/src/core/session-manager.ts: SessionHeader, SessionEntryBase {type, id, parentId, timestamp}, message, model_change, compaction {summary, firstKeptEntryId}, branch_summary {fromId, summary}, label, custom {customType, data}). lys adds entry kinds only as custom entries; it never adds a field to Pi's grammar and never adopts Norn's SessionEvent (Tom, Dot 13:28)."
    },
    {
      "id": "P3",
      "text": "A provider's opaque blocks (Anthropic signed thinking, OpenAI encrypted reasoning) stay on the assistant message with its provider, api and model, and are rendered whole only when all three match the target (Pi transform-messages.ts:95-109); another model gets readable thinking as plain text and opaque blocks dropped, each named by hash in the loss account (Tom, Dot 13:24: keep reasoning traces per provider so a session can swap and swap back)."
    },
    {
      "id": "P4",
      "text": "A content block is stored once by its hash; requests that resend the whole conversation reference blocks, they do not copy them."
    },
    {
      "id": "P5",
      "text": "Capture the model traffic and the harness's local events both, so the two can be mapped against each other (Tom, Dot 13:23); neither alone is the record."
    },
    {
      "id": "P6",
      "text": "A resume path is a per-harness, per-version measurement, never an assumption: Claude Code 2.1.281's --resume takes a session id and reads ~/.claude/projects/<cwd-slug>/<id>.jsonl; a seeded two-record file resumed there at 13:25 on 24 September and answered from its content."
    },
    {
      "id": "P7",
      "text": "Transcript contents (message, tool and compaction content) never appear in a post, a log line, an error, a test name or a rendered page; status carries hashes, counts and offsets only (CONTEXT-001 privacy rule). A lantern's note and its epilogues are the one exception: they are the person's own annotations, not transcript, and only recall prints them (ADR-015)."
    },
    {
      "id": "P8",
      "text": "A sandbox or a VM is a target profile the launch template renders into, never a special case in the core; credentials are supplied at launch on the target and never carried in the home (Tom, Dot 13:30; CONTEXT-ROADMAP stage 3)."
    },
    {
      "id": "P9",
      "text": "The canon is one curated, versioned series of examples every new session starts from: each entry is one rule stated short plus one real exchange that shows it lived (verify before claiming, a correction taken well, a refusal named, careful work), drawn from every agent's sessions, distilling the collective experience so far: 'our learnings in one another' (Tom, Dot 13:44 to 13:46; Waffles 0169c353). It is not a letter from one session to its successor. It lives in the lys repository at canon/canon.jsonl in Pi's grammar and changes only through src_commit and review, like code. Genuine thinking is kept whole where a real turn produced it and replays only to the same provider, api and model; thinking is never authored."
    },
    {
      "id": "P10",
      "text": "A handover is a letter the outgoing session writes to its successor in its own real thinking and answer; it enters the successor as a lys.inherited entry marked as a predecessor's memory, never as the successor's own experience, and replays only to the same provider, api and model. No thinking is ever authored; only thinking a model produced is kept (Tom, Dot 13:38 to 13:40: 'you are waking up ... I am another one, I'm helping you have part of my memory ... like a parent imparting a wish to a child'; Waffles 52d53451)."
    }
  ],
  "decisions": [
    "ADR-001",
    "ADR-003",
    "ADR-004",
    "ADR-007",
    "ADR-012",
    "ADR-013",
    "ADR-014",
    "ADR-015",
    "ADR-016",
    "ADR-017",
    "ADR-018"
  ],
  "goals": [
    "A few-shot session file written by hand resumes Claude Code by path, from a directory outside the config root, with the file preserved and the demonstration marked authored.",
    "One real Claude Code session imported into the common record, rendered back, and resumed under a new id on 2.1.281 without repeating a completed tool action, with the original file's hash unchanged.",
    "One seat with a subscription login making calls through a pass-through proxy, so the tee has somewhere to live.",
    "A written loss account for every derived record: what a render or translation preserved, transformed and could not carry.",
    "The canon, one curated versioned series of short examples each showing a rule lived, seeds every new session, and its effect is measured on a card against a plain start.",
    "A handover letter from an outgoing session seeds its successor as inherited memory, with the model's own thinking intact, and the effect is measured on a card against a plain start.",
    "A session in the home renders, from a Claude Code launch template kept in the home by hash, into a directory of launch files whose hashes are identical on a second render, and the printed launch line resumes it on the installed Claude Code version, measured.",
    "Every launch-template render records what the session was given: one lys.given entry after the render event naming each instruction document Claude Code will load, and each file the render wrote, by path, byte length and SHA-256 in the measured order, with the environment variable names the template set, and never a document's content; lys-home lists those records and checks a file on disk against one by hash.",
    "A lantern lit on purpose at a past entry of a session is recalled from the home by a word of its note and by its point, with its epilogues after it and never a line of the transcript around it.",
    "The same session head rendered twice with the same target and the same lys-home version gives files of equal SHA-256, a fixture holding a tool record with more than one result included, and the fixture's hash is pinned by a test and recorded in PROOF-RESUME.md.",
    "A session forked from a lantern's point holds the parent's own lines up to the last assistant message before the point, carries its ancestry on both sides, adds nothing to the block store, and is rendered and launched on the installed Claude Code version, measured, with a user-message point carried as the child's first prompt."
  ],
  "non_goals": [
    {
      "text": "Anchoring, signing or receipts into a lys log (CONTEXT-ROADMAP stage 6; when asked for).",
      "reason": "Signing comes when asked for (Tom, 22 September 16:27); every stage here works without it."
    },
    {
      "text": "Encryption at rest and moving a home between devices (stage 3 preconditions).",
      "reason": "Stage 3's three preconditions (identity and read authority, encryption before bytes leave, the resume evidence) are their own brief."
    },
    {
      "text": "Harnesses other than Claude Code, and Chat Completions or Responses translation beyond keeping the raw call bytes.",
      "reason": "One harness proved first; each other harness is its own profile and its own measurement."
    },
    {
      "text": "Cutting at a point no lantern names, forks with tools, a lens or a briefing on what changed since the point, and leases, budgets and kill rules for a fork.",
      "reason": "The fork through a lantern (stage 5b) is HOME-006; it takes a lantern id only, gives the child nothing but its own record, and what runs a fork and for how long belongs to the engine that launches it."
    },
    {
      "text": "Adopting, wrapping or calling Norn's session code; Pi's code is read as the reference and not vendored.",
      "reason": "Tom, Dot 13:28: not Norn. Pi's tree is the reference."
    },
    {
      "text": "Reading a secret's value through the broker at launch.",
      "reason": "The broker's own read belongs to the secrets rows (SECRETS-002); until its reader exists a readable secret is refused and only handles render."
    },
    {
      "text": "Recall by resonance, whispers or vectors, any ranking, dimming lanterns by code churn, Norn's tables and Cambium's lantern screen.",
      "reason": "Out of scope of stage 5: recall is by an exact case-folded phrase or by point; each of these is its own retrieval or surface design."
    },
    {
      "text": "Making the Claude Code import deterministic for the same transcript (sidechain labels, the lys.authored mark, compaction ids and side-leaf harness events draw fresh ids).",
      "reason": "The importer is out of scope for the deterministic render; determinism is claimed for a render of one session head, not for two imports of one transcript."
    },
    {
      "text": "Rendering tool parts from the stored block, and moving the render's default Claude Code version off 2.1.281.",
      "reason": "Both change what a rendered file holds; the deterministic render changes nothing a file holds beyond the determinism."
    }
  ],
  "structure": [
    {
      "path": "docs/design/home/briefs/HOME-001.json",
      "note": "the first brief: common record, Claude Code importer and renderer, the two proofs",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/briefs/HOME-001.md",
      "note": "its rendered markdown",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-RESUME.md",
      "note": "the measured Claude Code resume: version, command, hashes before and after, what repeated",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-PROXY.md",
      "note": "the measured subscription login through a pass-through proxy: version, headers that mattered, what failed",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/LOSS-ACCOUNT.md",
      "note": "what the Claude Code render preserves, transforms and cannot carry",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/blocks.rs",
      "note": "content-addressed block store: put by SHA-256, get by hash, never rewritten",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/import.rs",
      "note": "Claude Code JSONL into SessionEvents plus blocks",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/render.rs",
      "note": "SessionEvents into a Claude Code JSONL under a chosen uuid, with the loss account",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/mod.rs",
      "note": "the Claude Code harness profile: transcript root, cwd slug, version measured",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/cli.rs",
      "note": "import, render, resume-check subcommands",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/examples/passthrough.rs",
      "note": "a pass-through HTTP proxy that forwards to the provider unchanged, for the proof only",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/tests/claude_code_round_trip.rs",
      "note": "import then render equals the model-visible content; opaque blocks kept whole",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/mod.rs",
      "note": "the home record: Pi's session tree read and written, leaf pointer, root-to-leaf path",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/entries.rs",
      "note": "Pi's entry types as Rust types, plus the lys custom entries lys.harness_event and lys.call",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/call.rs",
      "note": "a proxy call record: request and response block hashes, provider, api, model, timing",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/events.rs",
      "note": "Claude Code's harness-local records (hooks, permission mode, tool completion) as lys.harness_event entries",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/RECORD.md",
      "note": "the home record written down: Pi's grammar as adopted, the two lys custom entries, the block store, the loss account",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/lib.rs",
      "note": "module wiring: record and harness",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/harness/mod.rs",
      "note": "harness profiles; Claude Code first",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/Cargo.toml",
      "note": "the crate manifest; gains the passthrough example",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/README.md",
      "note": "what the tool does and does not do",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/index.rs",
      "note": "the offset index and the persisted head: a path is read by seeking, never by loading the file",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/mod.rs",
      "note": "the proxy module: declarations only",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/link.rs",
      "note": "how a call is linked to a session: the measured request key (Claude Code's request metadata) now, the seat's handle when the door's proxy exists",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/bin/lys-proxy.rs",
      "note": "the proxy binary",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-FEWSHOT.md",
      "note": "the first proof: a hand-written few-shot session file resumed by path from a directory outside the config root",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/canon.rs",
      "note": "the canon: copy chosen real exchanges (or a clearly authored example) into canon.jsonl with a lys.inherited entry naming each source; seed a new session from it",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/canon_tests.rs",
      "note": "gates on the canon: copied whole, never twice, no authored thinking, rendered first",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-CANON.md",
      "note": "measured: a session started from the canon against a plain start, on one card",
      "brief": "HOME-001"
    },
    {
      "path": "canon/canon.jsonl",
      "note": "the canon itself, versioned by the repository, changed only through src_commit and review",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/record/handover.rs",
      "note": "the handover: take the outgoing session's letter turn (thinking intact) and seed the successor with it as lys.inherited",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/PROOF-HANDOVER.md",
      "note": "measured: a successor seeded with an inherited letter, whether 2.1.281 replays the signed thinking, and seeded against plain on one card",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/forward.rs",
      "note": "forward a request to the provider and stream the response back unchanged",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/capture.rs",
      "note": "bounded spooling of request and response bodies to files while forwarding, handed to the R6 sink",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/journal.rs",
      "note": "the open-call journal: a call id written before forwarding, retired after ingest, so a restart records lost calls once",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream.rs",
      "note": "the coordinator of the stream readers: picks the grammar by api and hands each frame on as it passes",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_sse.rs",
      "note": "SSE framing: events and data lines out of a byte stream, retaining byte order, encoding and trailers",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_messages.rs",
      "note": "the Messages event grammar assembled into response parts",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_chat.rs",
      "note": "the Chat Completions chunk grammar assembled into response parts",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_responses.rs",
      "note": "the Responses event grammar assembled into response parts",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/error.rs",
      "note": "the proxy's errors and outcomes, named",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/forward_tests.rs",
      "note": "forwarding against a loopback fake",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/stream_tests.rs",
      "note": "the three grammars, partial streams",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/link_tests.rs",
      "note": "linking by the measured key; unlinked; never inferred",
      "brief": "HOME-001"
    },
    {
      "path": "crates/lys-home/src/proxy/journal_tests.rs",
      "note": "recovery after a kill: one lost record per open call",
      "brief": "HOME-001"
    },
    {
      "path": "docs/design/home/briefs/HOME-002.json",
      "note": "the second brief: the Claude Code launch template and render-launch",
      "brief": "HOME-002"
    },
    {
      "path": "docs/design/home/briefs/HOME-002.md",
      "note": "its rendered markdown",
      "brief": "HOME-002"
    },
    {
      "path": "docs/design/home/launch-template.schema.json",
      "note": "the Claude Code launch template's JSON Schema: harness, flags and the five named slots",
      "brief": "HOME-002"
    },
    {
      "path": "docs/design/home/PROOF-LAUNCH.md",
      "note": "the measured launch: installed version, template hash, written paths and hashes, the launch line, the resume outcome; no transcript",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/template.rs",
      "note": "the launch template parsed and checked: unknown or missing slot, readable secret, duplicate variable refused by name",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/template_tests.rs",
      "note": "gates on the template parser and its agreement with the schema file",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/launch_env.rs",
      "note": "the environment file: template variables and use-only secrets as handles",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/launch.rs",
      "note": "render-launch: its arguments, the five files, the launch line, the manifest and the event",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/events_tests.rs",
      "note": "gates on the template_render event and the 512-byte cap",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/record/templates.rs",
      "note": "the home's template store: templates/<hh>/<hash>, written once",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/record/templates_tests.rs",
      "note": "gates on the template store",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/record/beside.rs",
      "note": "append beside the context path without moving the head; the session head hash",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/record/beside_tests.rs",
      "note": "gates on the side-leaf append and the head hash",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/tests/launch_template.rs",
      "note": "render-launch end to end: recorded hashes, twice identical, handle only, refusals, exit 2",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/tests/fixtures/launch/template.json",
      "note": "the fixture Claude Code launch template, handle-only secrets",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/tests/fixtures/launch/session.jsonl",
      "note": "the synthetic fixture session in Pi's grammar, no transcript content",
      "brief": "HOME-002"
    },
    {
      "path": "crates/lys-home/src/error.rs",
      "note": "the home's errors; gains the template refusals",
      "brief": ""
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/render_tests.rs",
      "note": "gates on the R4 render, the deterministic render among them",
      "brief": ""
    },
    {
      "path": "docs/design/identity/STATEMENT-2026-09-22.md",
      "note": "the statement; its steps 4 and 5 entry carries the ruling the launch template stands on",
      "brief": ""
    },
    {
      "path": "docs/design/home/DESIGN.md",
      "note": "the cluster design, rendered from design.json",
      "brief": ""
    },
    {
      "path": "docs/design/home/CHECKLIST.md",
      "note": "the checklist, rendered from checklist.json",
      "brief": ""
    },
    {
      "path": "docs/design/home/USER-STORIES.md",
      "note": "the stories, rendered from stories.json",
      "brief": ""
    },
    {
      "path": "docs/design/home/briefs/HOME-003.json",
      "note": "the third brief: the context record (lys.given) made at render, listed and checked by hash",
      "brief": "HOME-003"
    },
    {
      "path": "docs/design/home/briefs/HOME-003.md",
      "note": "its rendered markdown",
      "brief": "HOME-003"
    },
    {
      "path": "docs/design/home/PROOF-GIVEN.md",
      "note": "the measured Claude Code 2.1.283 instruction load order and slug rule, and one real render recorded as paths, counts and hashes",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/paths.rs",
      "note": "the Claude Code project slug as measured: every character that is not an ASCII letter or digit becomes '-'",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/paths_tests.rs",
      "note": "the slug rule against dotted, underscored and hyphenated working directories",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/given.rs",
      "note": "resolving, in the measured order, the documents Claude Code will load for a working directory plus the files a render wrote",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/given_tests.rs",
      "note": "resolution order, absent documents omitted, lengths and hashes",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/src/record/given.rs",
      "note": "the lys.given entry data: harness, version, kinds, config directory and its source, documents, environment names; appended and read back",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/src/record/given_tests.rs",
      "note": "the lys.given shape: no content field, parented on the render event, read back equal",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/src/cli/given.rs",
      "note": "the given and given-check subcommands, split out of cli.rs",
      "brief": "HOME-003"
    },
    {
      "path": "crates/lys-home/tests/given_record.rs",
      "note": "end to end on the fixture template: order, two renders equal, one byte changed, matches and differs, no content",
      "brief": "HOME-003"
    },
    {
      "path": "docs/design/home/briefs/HOME-004.json",
      "note": "the lantern brief: light, epilogue and recall by note and by point",
      "brief": "HOME-004"
    },
    {
      "path": "docs/design/home/briefs/HOME-004.md",
      "note": "its rendered markdown",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/reader.rs",
      "note": "a session read without owning it: no lock, no index or head written; the home's session ids",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/reader_tests.rs",
      "note": "gates on the reader: reads a held session, writes nothing, lists every session",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/lantern.rs",
      "note": "lighting a lantern: checks the point and the note, appends one lys.lantern entry at the head",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/lantern_tests.rs",
      "note": "gates on lighting: earlier bytes kept, head is the lantern, each refusal by name",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/epilogue.rs",
      "note": "adding an epilogue: names a lantern of the session, appends one lys.lantern_epilogue entry at the head",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/epilogue_tests.rs",
      "note": "gates on epilogues: appended after the lantern, unknown lantern and blank words refused",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/recall.rs",
      "note": "recall by note (exact phrase, case-folded) and by point, over every session, skipped sessions named",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/record/recall_tests.rs",
      "note": "gates on recall: phrase in one text, both lanterns on a point, epilogues in order, no transcript",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/src/cli_lantern.rs",
      "note": "the lantern subcommands: light, epilogue, recall, each printing one JSON report",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/tests/lantern_cli.rs",
      "note": "the lantern subcommands run as the binary: reports, refusals, exit codes, no transcript in output",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/tests/lantern_home.rs",
      "note": "a lantern lives in the home: the rendered Claude Code file carries no lantern line",
      "brief": "HOME-004"
    },
    {
      "path": "docs/design/home/PROOF-LANTERN.md",
      "note": "the dev record: Pi's parser run under node on a session holding lanterns and epilogues",
      "brief": "HOME-004"
    },
    {
      "path": "crates/lys-home/tests/fixtures/multi_result.jsonl",
      "note": "a synthetic Claude Code transcript with a two-result tool record and a one-result-plus-text record, the determinism fixture",
      "brief": "HOME-007"
    },
    {
      "path": "Cargo.toml",
      "note": "the workspace manifest; gains the sha1 dependency the render's UUIDv5 needs",
      "brief": ""
    },
    {
      "path": "Cargo.lock",
      "note": "the workspace lockfile",
      "brief": ""
    },
    {
      "path": "docs/design/home/briefs/HOME-007.json",
      "note": "the deterministic Claude Code render",
      "brief": "HOME-007"
    },
    {
      "path": "docs/design/home/briefs/HOME-007.md",
      "note": "its rendered markdown",
      "brief": "HOME-007"
    },
    {
      "path": "docs/design/home/briefs/HOME-006.json",
      "note": "the fork brief: cut at a lantern's point, ancestry on both sides, the seed and the launch line",
      "brief": "HOME-006"
    },
    {
      "path": "docs/design/home/briefs/HOME-006.md",
      "note": "its rendered markdown",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/src/record/fork_cut.rs",
      "note": "resolving a lantern to its lit-in session and cutting the chain at the last assistant message, through the index",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/src/record/fork_cut_tests.rs",
      "note": "gates on the names, the refusals and the cut: order, side leaves out, bytes read, each refusal by name",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/src/record/fork.rs",
      "note": "writing the child from the parent's own lines, lys.forked_from in the child and lys.fork at the parent's head",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/src/record/fork_tests.rs",
      "note": "gates on the fork: lines hash-equal, both ancestry entries, no block written, held parent refused, the report's counts",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/src/record/fork_report.rs",
      "note": "the fork report: entries copied, block hashes held and unstored, ids only",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/src/cli_fork.rs",
      "note": "the fork subcommand: one JSON report, refusals on stderr",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/seed.rs",
      "note": "the seed prompt of a carried user message: the marker line, the text parts, the seed argument for the template's launch line",
      "brief": "HOME-006"
    },
    {
      "path": "crates/lys-home/tests/fork.rs",
      "note": "the fork run as the binary: five forks and five refusals, blocks unchanged, both ancestry sides",
      "brief": "HOME-006"
    },
    {
      "path": "docs/design/home/PROOF-FORK.md",
      "note": "the measured fork: Pi's parentSession read, a real session forked, rendered and launched, as hashes, counts and exit codes",
      "brief": "HOME-006"
    },
    {
      "path": "docs/design/home/briefs/HOME-008.json",
      "note": "the rendered-uuid contract: the rule and its vector in RECORD.md pinned by a test, the launch card amended, ADR-016 decided",
      "brief": "HOME-008"
    },
    {
      "path": "docs/design/home/briefs/HOME-008.md",
      "note": "its rendered markdown",
      "brief": "HOME-008"
    },
    {
      "path": "docs/design/decisions.json",
      "note": "the decision ledger; HOME-008 moves ADR-016 to decided",
      "brief": "HOME-008"
    }
  ],
  "inventory": [
    {
      "path": "docs/design/identity/STATEMENT-2026-09-22.md",
      "note": "the authority: 'The home', 'The home is portable: lanterns, translation and forks', and steps 4 and 5"
    },
    {
      "path": "docs/design/identity/CONTEXT-ROADMAP-2026-09-22.md",
      "note": "Archie's roadmap; this cluster is its stage 4 and the resume half of stage 3"
    },
    {
      "path": "docs/design/identity/briefs/CONTEXT-001.json",
      "note": "stage 1, byte-for-byte capture and the lys-home crate this cluster extends"
    },
    {
      "path": "docs/design/secrets/briefs/SECRETS-002.json",
      "note": "the door's proxy (R1) that the tee joins once it exists"
    },
    {
      "path": "$PI/packages/coding-agent/src/core/session-manager.ts",
      "note": "Pi's session tree at 3d5cbe98 (github.com/earendil-works/pi): header, entries, leaf (branch at :1125 moves an in-memory cursor; _buildIndex:754 restores the last physical entry on reopen; loadEntriesFromFile:438 reads the whole file)"
    },
    {
      "path": "$PI/packages/ai/src/api/transform-messages.ts",
      "note": "per-provider thinking: same provider, api and model keeps blocks whole; otherwise text, opaque dropped"
    },
    {
      "path": "$PI/packages/coding-agent/src/core/branch-summarization.ts",
      "note": "how an abandoned branch's work is carried into the new one"
    },
    {
      "path": "$CCFLARE/README.md",
      "note": "ccflare at 95c4c6a (github.com/snipeship/ccflare): the concept of a native pass-through that keeps request history; reference only, not used (Tom, Dot 13:34)"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/render.rs",
      "note": "on 0073b966 record_uuid gives an entry id that is not uuid-shaped a fresh random uuid (record::fresh_id) on every render, so two renders of a session holding the importer's `<uuid>-r<i>` ids differ; the launch template then shaped one from the SHA-256 of the entry id alone, outside any namespace, without a role or a session; it reads no clock and iterates no map or set"
    },
    {
      "path": "crates/lys-home/src/harness/claude_code/import.rs",
      "note": "a user record's tool results each become an entry with id `<uuid>-r<i>`; only the last keeps the record's uuid, and only when the record has no parts of its own; unchanged by this cluster's determinism work"
    }
  ],
  "constraints": [
    {
      "id": "CN1",
      "text": "No file under ~/.claude/projects is rewritten or truncated by this cluster; a render writes a new file under a new uuid only, and refuses an existing path by name."
    },
    {
      "id": "CN2",
      "text": "A rendered file never carries a provider-native opaque block that came from a different provider or model family than the one it is rendered for; the loss account names each block dropped by hash."
    },
    {
      "id": "CN3",
      "text": "Transcript lines (message, tool and compaction content) never appear in output, logs, errors, test names or pages; hashes, counts, offsets and event ids only. A lantern's note and its epilogues are the one exception, printed only by recall, and a recall row never carries a line of the transcript around its point (ADR-015)."
    },
    {
      "id": "CN4",
      "text": "Every home file parses with Pi's parseSessionEntries at 3d5cbe98 unchanged: the header line first, every entry with id, parentId and timestamp, lys data only inside custom entries."
    },
    {
      "id": "CN5",
      "text": "The pass-through proxy forwards every header and the streamed body unchanged and stores nothing but the measurement; it is an example binary, never a service."
    },
    {
      "id": "CN6",
      "text": "No Norn crate is a dependency of lys-home and no Norn type is copied into it."
    },
    {
      "id": "CN7",
      "text": "Reading the root-to-leaf path of a home file reads only the entries on that path plus the index, never the whole file (Pi loads the whole journal; Chippy 13:33); the head is persisted beside the file, never inferred from the last physical entry on reopen."
    },
    {
      "id": "CN8",
      "text": "No secret value is written to any file, report, launch line, error or entry by lys-home; a use-only secret appears only as its handle."
    },
    {
      "id": "CN9",
      "text": "A render of the same session head with the same target and the same lys-home version writes the same bytes: the render walks entry order then part order and never a map's or a set's order, reads no clock and no random source, and derives any uuid the record does not hold as UUIDv5 under the session's namespace (UUIDv5 of 32c05904-d1f1-550c-9eee-2f6c8f98b665 over the session id) and the name `<entry id>#<role>` (ADR-016)."
    },
    {
      "id": "CN10",
      "text": "A fork copies the parent file's own line bytes and writes no block; nothing of the parent's header but cwd, and no credential, handle or launch setting, enters the child; the parent gains one lys.fork entry at its head and no earlier byte of it changes."
    }
  ],
  "gate": [
    {
      "tree": ".",
      "shorthand": {
        "rust-build": [
          "toolchain:rust",
          "lockfile:Cargo.lock"
        ]
      },
      "legs": [
        {
          "name": "fmt",
          "command": "cargo fmt --all",
          "requires": [
            "tool:cargo",
            "tool:rustfmt"
          ],
          "cadence": "round"
        },
        {
          "name": "clippy-all-features",
          "command": "cargo clippy --all-targets --all-features -- -D warnings",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "clippy",
          "command": "cargo clippy --all-targets -- -D warnings",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "tests",
          "command": "cargo test --workspace --all-features",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "doc-all-features",
          "command": "cargo doc --no-deps --all-features",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "doc",
          "command": "cargo doc --no-deps",
          "requires": [
            "rust-build"
          ],
          "cadence": "round"
        },
        {
          "name": "design",
          "command": "sh scripts/design/gate.sh",
          "requires": [
            "tool:sh",
            "tool:python3"
          ],
          "cadence": "round"
        }
      ]
    }
  ]
}
