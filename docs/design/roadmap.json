{
  "project": "lys",
  "updated": "2026-09-26",
  "items": [
    {
      "id": "RM-001",
      "title": "Stand up the identity directory with every grant rooted in a person",
      "status": "briefed",
      "kind": "feature",
      "summary": "The standalone identity directory: sign-in through the platform's own Rauthy fork, two upstream providers linked to one person, a signed authoritative identity, and the standalone screen journey, all installed against one PostgreSQL database. It is the root every later grant in the platform hangs from.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "The identity platform statement (docs/design/identity/STATEMENT-2026-09-22.md) and IDENTITY-001; the cluster and its five briefs reached main in 55933549."
      },
      "links": {
        "cluster": "directory",
        "decisions": [
          "ADR-003",
          "ADR-004",
          "ADR-005",
          "ADR-007",
          "ADR-008",
          "ADR-009",
          "ADR-010",
          "ADR-011"
        ],
        "briefs": [
          "DIRECTORY-001",
          "DIRECTORY-002",
          "DIRECTORY-003",
          "DIRECTORY-004",
          "DIRECTORY-005"
        ],
        "commits": []
      },
      "depends_on": [],
      "notes": "The briefs build in order: DIRECTORY-002 before 003, 003 before 004 and 005. Each card runs through the board (brief_card, then card_build_v3)."
    },
    {
      "id": "RM-002",
      "title": "Build the secrets broker so agents hold handles, never credentials",
      "status": "briefed",
      "kind": "feature",
      "summary": "A broker that keeps credentials behind handles: the door swaps a handle for the credential at use, the token revolver is the first consumer, and rotation and sealing happen without any agent ever seeing a secret.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "The secrets cluster (docs/design/secrets) and its two briefs, written to ADR-001 and ADR-002."
      },
      "links": {
        "cluster": "secrets",
        "decisions": [
          "ADR-001",
          "ADR-002",
          "ADR-003",
          "ADR-004"
        ],
        "briefs": [
          "SECRETS-001",
          "SECRETS-002"
        ],
        "commits": []
      },
      "depends_on": [],
      "notes": ""
    },
    {
      "id": "RM-003",
      "title": "Move the Rauthy fork onto the next hardened upstream release",
      "status": "idea",
      "kind": "fix",
      "summary": "Rebase the platform's own Rauthy fork (ADR-009) onto the next upstream release that upstream tags as a release and whose changelog carries security or hardening fixes. Our commits stay on top of the upstream tag, unsquashed and unaltered; the pin moves to the rebased head; the fork builds and passes its own suite; the standalone install comes up from the rebased build against the existing database with no migration by hand; the provider-link behaviour of DIRECTORY-004 still holds.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "Written ahead of its trigger from the Lys board card of the same name. Upstream was still v0.36.2 on 2026-09-23 and the fork is pinned to that exact version. The trigger is a fact recorded in the pull request (the upstream tag, its commit, its changelog entry), never a judgement."
      },
      "links": {
        "cluster": "rauthy-rebase",
        "decisions": [
          "ADR-009"
        ],
        "briefs": [],
        "commits": []
      },
      "depends_on": [
        "RM-001"
      ],
      "notes": "The card is built only when the trigger is met. It needs DIRECTORY-004 (the provider link commit the rebase must carry) and DIRECTORY-002 (the standalone install it must prove) landed first. If the rebase cannot carry a fork commit cleanly the card stops and reports the conflicting upstream commit rather than rewriting ours. Nothing in Cambium or Aion changes; no production node is touched; Tom's three acts stay Tom's."
    },
    {
      "id": "RM-004",
      "title": "Rewrite the decision quotes as plain statements in the team's words",
      "status": "idea",
      "kind": "design",
      "summary": "docs/design/decisions.json holds eight decisions (ADR-001 to ADR-008) whose quote field carries a person's words. Each quote becomes a plain statement of the decision in the team's words, with the decision, context, consequences and sources kept exactly as they are. ADR-009 and ADR-010 already read that way and are the pattern.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "The canon rule of 24 September: repository canon holds no person's words and nothing personal. ADR-009 and ADR-010 (lys 586ef5f4) were written to it."
      },
      "links": {
        "cluster": "decisions-words",
        "decisions": [
          "ADR-001",
          "ADR-002",
          "ADR-003",
          "ADR-004",
          "ADR-005",
          "ADR-006",
          "ADR-007",
          "ADR-008"
        ],
        "briefs": [],
        "commits": []
      },
      "depends_on": [],
      "notes": "Documents only. scripts/design/gate.sh must stay green: decisions.json validates against decisions.schema.json, which keeps the quote field required."
    },
    {
      "id": "RM-005",
      "title": "Give a session a home: its context record, kept byte for byte",
      "status": "briefed",
      "kind": "feature",
      "summary": "The home (identity platform steps 4 and 5): a running session's transcript captured into a home byte for byte, indexed for reading back, with the record of what the agent was given as well as what it did. Lanterns, translation and forks stand on it.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "CONTEXT-001 (docs/design/identity/briefs) and the home cluster, landed on main with lys PR 8 (HOME-001, design.json, stories, checklist)."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-001",
          "ADR-003",
          "ADR-004"
        ],
        "briefs": [
          "HOME-001"
        ],
        "commits": [
          "7b47115cb08b02c246ff19e3330e5f825ed68163"
        ]
      },
      "depends_on": [
        "RM-001"
      ],
      "notes": "Owned by Archie. The home cluster and HOME-001 landed on main with PR 8; the cached-index rule (lys-home Index::from_cached) is in that brief. Next: HOME-001 dispatched through the board."
    },
    {
      "id": "RM-006",
      "title": "Launch a session from its home on Claude Code through a kept template",
      "status": "idea",
      "kind": "feature",
      "summary": "The home keeps a launch template per harness, starting with Claude Code, as an object named by its hash; lys-home render-launch turns a template and a session into the files a Claude Code launch needs and a printed launch line that resumes the rendered file by path, and records each render on the session so a stranger can tell which template a session was launched with.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-26T06:15:00+10:00",
        "quote": "A session's home holds the pieces a new session is made from, the record, the canon and the handover, but nothing yet turns them into a running session on a given harness. Tom ruled at 16:00 on 22 September that the home keeps a preset template per harness which maps the pieces onto that harness, and that the mapping covers the MCP configuration, the environment variables, which secrets the session may read and which it may only use through a handle, what is appended to the system prompt, and where the prior conversation goes, delivered as files and runtime variables, command mappings in text, never a transcript converter. The change is a launch template for Claude Code, kept in the home as a versioned object beside the record, with its schema in docs/design/home, and a lys-home subcommand that reads a template and a session and writes the files a Claude Code launch needs. The files are the rendered JSONL under the chosen uuid and cwd as R4 renders it, an MCP configuration file, an environment file, an appended instructions file, and the launch line that resumes by path as R9 states. The template has named slots. The transcript slot is the one slot no template can map generically, so the template names how this harness fills it, and Claude Code fills it by resuming the rendered file by path. A secret named in the template is written as its handle and never as its value, and the launch line reads the value through the broker at start, which the secrets rows own. Every render of a template records the template hash, the session head hash and the written paths in a lys.harness_event entry on the session, so a stranger can tell which template a session was launched with. The subcommand prints a JSON report of hashes and paths and never prints transcript, block or body content, and a missing argument exits 2 naming it. Acceptance is that a fixture template and the recorded session from PROOF-RESUME render to a directory whose files match recorded hashes, that the printed launch line resumes the rendered path with the template's flags, that a template naming a secret writes only the handle and a search of the written files for the fixture value finds nothing, that rendering the same template and session twice yields identical hashes, and that a template with an unknown slot is refused by name. Not in scope are any harness other than Claude Code, launching on another machine, and the broker's own read. Filed by Archie on Tom's 16:00 ruling of 22 September 2026 and HOME-001 R4, R9 and R12 on lys main 0073b966, at 06:15 on 26 September 2026.",
        "context": "Filed on the ruling relayed at docs/design/identity/STATEMENT-2026-09-22.md line 181 and HOME-001 R4, R9 and R12, on lys main 0073b966; the card lead's answers on fork-on-resume, the fixture, use-only secrets, the unbuilt broker reader, the side-leaf event, the canon and the ruling's record are written into HOME-002."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-001",
          "ADR-003",
          "ADR-004",
          "ADR-007",
          "ADR-012"
        ],
        "briefs": [
          "HOME-002"
        ],
        "commits": []
      },
      "depends_on": [
        "RM-005"
      ],
      "notes": "Further units, not written: Wire the secret slot's start-time read to the broker once SECRETS-002 lands; Launch from the template and capture the new session into the same home; Canon and handover slots in the launch template; Target profiles: sandbox and VM rendered from the template (P8); Launch on a second machine from a fetched home (stage 3); Templates for a second harness; manifold seat document consuming the printed launch line."
    },
    {
      "id": "RM-007",
      "title": "Record what a session was given at render: the lys.given context record",
      "status": "idea",
      "kind": "feature",
      "summary": "At every launch-template render, lys-home appends a lys.given custom entry after the render event. It names each instruction document Claude Code will load for the session's working directory, and each file the render wrote, by path, byte length and SHA-256, in the load order measured on a named Claude Code version, with the environment variable names the template set and the kinds resolved and left unlisted, and never a document's content. Two subcommands list those records and check a file on disk against one by hash. This is step 4's context record: what a session was given, unsigned and unencrypted, shaped as hashes so that signing and encryption can be added later.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-26T07:05:53+10:00",
        "quote": "Step 4 is what a session was given, not only what it did. When Claude Code starts a session it reads instruction documents the person did not type: the CLAUDE.md chain from the working directory upward, the user's own CLAUDE.md, the memory index the seat loads, the appended instructions file the launch template writes, the MCP configuration, and the names of the environment variables set for it. Today the launch template's render event records the template hash, the session head hash and the written paths, and nothing records which of those documents the session was given, in what versions and order. This card records that as the context record: at render, the template subcommand resolves the documents Claude Code will load for the rendered session's working directory and the home's own files it wrote, reads each one, and appends to the session, after the render event, a lys.given custom entry listing every document in the order the harness reads them, each as its path, its byte length and its SHA-256, with the harness name and version the order was measured on, and never a document's content. The resolution is measured, not assumed: the brief names the Claude Code version on this Mac and the order it loads instruction files in, from its own behaviour, and the proof document records that measurement. A lys-home subcommand lists a session's given records with their documents, and a second checks a listed document against a file on disk by hash, saying matches or differs and never printing either. The record is unsigned and the content is not encrypted: signing is stage 6 and comes when asked for, and encryption at rest is a stage 3 precondition card; the given entry names hashes only, so both can be added later without changing what is recorded. Acceptance is that rendering the fixture template for a fixture working directory holding a CLAUDE.md and a memory index appends a lys.given entry after the render event whose documents are those files plus the written appended instructions and MCP configuration, in the measured order, with lengths and hashes that match the files; that rendering twice gives two given entries whose document lists are identical; that changing one byte of the fixture CLAUDE.md changes exactly that document's hash in the next given entry; that the check subcommand says matches for the recorded file and differs after the change; that the given entry and the listing hold no document content, checked by a search for a fixture sentence; and that one real session render on this Mac is recorded in the proof document as hashes, counts and paths only. Not in scope: signing or anchoring the record, which is stage 6; encryption at rest; recording documents a session reads later by its own tools; harnesses other than Claude Code. This card stands on the launch template card, ct98Wv-2, and is built on main after it lands. Filed by Archie on his 22 September 2026 statement in lys that step 4 is what the agent was given, Tom's 16:00 22 September 2026 template ruling that every render is a receipted event, his 16:27 ruling that signing comes when asked for, and home DESIGN P7 and P8 at lys main 0073b966, on 26 September 2026.",
        "context": "A Cambium card on the home cluster, standing on the launch template card ct98Wv-2; the lead's answers settled the environment names member, the kinds member, what identical document lists mean, the order within one directory, that files reaching the request by @-import or from .claude/rules are recorded, given-check's exit status, and that the render's entry names @-imports and .claude/rules as unlisted kinds while a second entry at the first request records what reached the model."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-001",
          "ADR-004",
          "ADR-007",
          "ADR-013"
        ],
        "briefs": [
          "HOME-003"
        ],
        "commits": []
      },
      "depends_on": [
        "RM-005",
        "RM-006"
      ],
      "notes": "Renumbered from the HOME-002 it was written as, since the launch template took HOME-002 and RM-006 first; built on the launch template branch (RM-006, lys PR 14) and lands after it. Built on lys main after ct98Wv-2 lands. Further units, not written: Record settings.json in the given entry; Record SessionStart hook output in the given entry; Record plugin skill and agent listings in the given entry; Record output styles in the given entry; Record what reached the model, @-imports and .claude/rules included, in a second lys.given entry at the first request from the HOME-001 proxy's capture."
    },
    {
      "id": "RM-008",
      "title": "Light a lantern in the home and recall it by note and by point",
      "status": "idea",
      "kind": "feature",
      "summary": "Stage 5 of the home: a lantern is a note plus a point in a session, lit on purpose as a lys.lantern custom entry at the session's head, whose note grows only by lys.lantern_epilogue entries. Three lys-home subcommands light one, add an epilogue, and recall lanterns by an exact case-folded phrase of their note or epilogues, or by session and entry id, printing the lantern and its epilogues and never the transcript around the point. A lantern lives in the home and is never rendered into a Claude Code resume file.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-26T06:30:25+10:00",
        "quote": "A lantern is a note plus a point in a transcript, declared on purpose at a moment of completion, success or learning, never automatically; it is what a later session, or a fork, walks back to. The home holds sessions in Pi's grammar and adds nothing to that grammar, so a lantern is a lys.lantern custom entry appended at the session's head, carrying the point it marks as the id of an existing entry in that session, the note as written, who lit it and when. The point may be any entry on the session's tree, not only the head, so a lantern can mark a place the session has already moved past. A note is append-only and grows: an epilogue is a lys.lantern_epilogue custom entry naming the lantern's own entry id and carrying the further words, so a lantern's story is its entry followed by its epilogues in order, and nothing is ever rewritten. A lys-home subcommand lights one: it takes the home, the session, the entry id of the point and the note, refuses by name a point that is not an entry of that session and an empty note, appends the entry, and prints a JSON report of the lantern's id, session, point and time. A second subcommand adds an epilogue to a named lantern. A third recalls: by note, listing every lantern in the home whose note or epilogues contain the given words, exact and case-folded, no vectors; and by point, listing every lantern that marks a given session and entry id. Each listing row is the lantern's id, session, point, lit-by, lit-at, note, and its epilogues, and never a line of the transcript around the point. A lantern is not rendered into a Claude Code resume file; it lives in the home. Acceptance is that on a fixture session a lantern lit at a past entry is recalled by a word of its note and by its point; that two lanterns on the same point are both recalled by point; that an epilogue is recalled with its lantern, after it, and recall by a word that appears only in the epilogue finds the lantern; that lighting at an unknown entry id and lighting with an empty note are each refused by name; that after lighting, the session file's earlier bytes are unchanged and the head is the lantern entry; that the rendered Claude Code file of that session contains no lantern line; and that no listing prints transcript content, checked by a search for a fixture message's text. Not in scope: the fork through a lantern, which is stage 5b and its own card; resonance, whispers, vectors and any ranking; dimming by code churn; Norn's tables; Cambium's screen. Filed by Archie on Tom's definition of 8 August 2026 that a lantern is a note plus a pathway back, declared deliberately, notes append-only, his roadmap stage 5 of 22 September 2026, and home DESIGN P2 and P7 at lys main 0073b966, on 26 September 2026.",
        "context": "The stage 5 lantern card on the Lys board, surveyed against lys main 0073b966; the lead of the home card answered the survey's open questions (phrase matching, the P7 and CN3 exception, held sessions, blank notes and epilogues, epilogue authorship, unknown points in recall, lanterns as points) and HOME-004 carries those answers."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-003",
          "ADR-004",
          "ADR-014",
          "ADR-015"
        ],
        "briefs": [
          "HOME-004"
        ],
        "commits": []
      },
      "depends_on": [
        "RM-005"
      ],
      "notes": "HOME-004 is the first unit. Further units, not yet written: Stage 5b: fork through a lantern (cut at its point and seed a new session); Lit-by from a directory identity rather than a declared name; Lighting from a live session under capture (the door's proxy or harness hook); Cambium's lantern screen; Recall by resonance and vectors, with ranking and churn dimming."
    },
    {
      "id": "RM-009",
      "title": "Make the Claude Code render byte-deterministic for the same session head and version",
      "status": "idea",
      "kind": "fix",
      "summary": "Two renders of the same session head give different bytes whenever the context path holds an entry whose id is not uuid-shaped, which the importer writes for every tool record with more than one result: the render draws a fresh random uuid for each. The render derives those uuids from the record instead (UUIDv5 over a fixed lys namespace, the session id and the entry id with its role), a fixture carrying the defect is pinned by hash in a test and in PROOF-RESUME.md, and the resume is re-measured on the Claude Code installed on this Mac. A rendered file can then be told by its hash, a receipted render event can name what was written, and the launch template's identical-hash acceptance has ground to stand on.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-26T08:03:37+10:00",
        "quote": "The Claude Code render in crates/lys-home walks a session's context path and writes the JSONL file that Claude Code 2.1.282 on this Mac resumes by path, as HOME-001 R4 and R9 landed it on lys main 0073b966. Rendering the same session twice does not give the same bytes today when a tool record on the path holds more than one result, which the translation card's survey found on 26 September 2026 while reading the render for the Codex rollout, and the launch template card's acceptance that rendering the same template and session twice yields identical hashes stands on the render being deterministic. A render of the same session head with the same lys-home version is one function of the record and must give the same bytes every time, so a reader can tell a rendered file by its hash and a receipted render event can name what was written. The change finds why two renders of a multi-result tool record differ, names that cause in the proof document, and makes the render byte-deterministic for the same session head and the same version without changing what the rendered file holds or how Claude Code resumes it. Where the cause is an order that the record does not fix, the render takes the order the record gives, entry order then part order, and never a map's or a set's order; where the cause is a fresh id or a clock, the value is derived from the record's own ids and stamps as the fork and launch cards derive theirs. Acceptance is that a fixture session holding a tool record with more than one result renders twice to files of equal SHA-256; that the fixture from PROOF-RESUME renders twice to equal hashes; that a recorded fixture hash in the proof document matches the render at the landed commit; that the rendered file still resumes in Claude Code 2.1.282 on this Mac, recorded in the proof document as before; and that the proof document names the cause found. Not in scope: any change to the importer, the record's shape, or the rendered file's content beyond the determinism, and any harness other than Claude Code. Filed by Archie on the translation card's survey of 26 September 2026, on Tom's rule of 16 September 2026 to find the underlying cause, and on lys main 0073b966.",
        "context": "Filed on the translation card's survey, which found the nondeterminism while reading the render for the Codex rollout; surveyed on lys main 0073b966. The lead's answers settled the Claude Code version the resume is measured on (the one installed when the proof runs, as `claude --version` prints it, with the rendered records' version field left at 2.1.281) and that this card defines the id derivation the fork and launch cards follow; a follow-up card ruled that the derived uuid is salted with the session id."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-004",
          "ADR-007",
          "ADR-016"
        ],
        "briefs": [
          "HOME-007"
        ],
        "commits": []
      },
      "depends_on": [],
      "notes": "Renumbered from the HOME-002 and RM-006 it was written as, since the launch template took HOME-002 and RM-006, the context record HOME-003 and RM-007, and the lanterns HOME-004 and RM-008 first. Stands on HOME-001's render (R4) and CLI (R9), already on main at 0073b966, so no roadmap dependency is written. Further units, not written: Make the Claude Code import deterministic for the same transcript; Render tool parts from the stored block so a round trip adds only the new turn; Move the render's default Claude Code version off 2.1.281. ADR-016 is proposed and binds the fork and launch cards' id derivation."
    },
    {
      "id": "RM-010",
      "title": "Fork a session from a lantern's point and launch the child",
      "status": "idea",
      "kind": "feature",
      "summary": "Stage 5b of the home: lys-home fork takes a lantern id, resolves it to the session it was lit in, cuts that session's chain at the last assistant message at or before the lantern's point, and writes a child session holding the parent's own lines with its ancestry on both sides (the header's parentSession, a lys.forked_from entry in the child, a lys.fork entry at the parent's head), adding nothing to the block store and rewriting nothing in the parent. A user-message point is carried as a seed prompt the child's render writes beside the rendered file and the launch line passes as the first prompt. The child renders and launches through the launch template, measured on the installed Claude Code version.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-26T13:16:00+10:00",
        "quote": "A lantern is a pathway back to a conversation with the self that lit it, and Claude Code resumes a session only from its end. This brief delivers the fork that makes the pathway: a new session in the home cut from the parent's chain at a lantern's point, carrying its ancestry on both sides, adding nothing to the block store and rewriting nothing in the parent, then rendered and launched like any session. It is stage 5b of the home and stands on the lantern card's lys.lantern entry (the design's solution and ADR-012).",
        "context": "The stage 5b fork card on the Lys board, surveyed against lys main after the lantern card; the lead's answers settled that the fork stands on the lantern card and is built on the main it lands on, that the child launches through the launch template's line, that the copied lines are the parent's own bytes with the same hashes and the block store unchanged, that lys.forked_from is the first entry the fork writes, that parentSession carries the parent file's path as Pi reads it, that a user-message point is carried as a seed and never dropped, that a lantern resolves to its lit-in session (lit_in) with the older-record rule until the lantern card records it, that a lantern before any assistant message is refused nothing_to_fork, and that non-text parts of a carried message are counted, never refused. HOME-006 carries those answers."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-007",
          "ADR-012",
          "ADR-014",
          "ADR-017",
          "ADR-018"
        ],
        "briefs": [
          "HOME-006"
        ],
        "commits": []
      },
      "depends_on": [
        "RM-006",
        "RM-008"
      ],
      "notes": "Renumbered from the HOME-002 it was written as; HOME-006 follows the lantern card (HOME-004, RM-008) and the launch template (HOME-002, RM-006), both on main, and after the render card (HOME-007), which took RM-009, ADR-016, C31 to C35, S18, S19 and CN9. Further units, not written: lit_in written by the light act (the lantern card's next round); a fork given tools, a lens or a briefing on what changed since the point; leases, budgets and kill rules; harnesses other than Claude Code."
    },
    {
      "id": "RM-013",
      "title": "Build the secrets broker in lys from a baseline, a reviewed contract and counted code rows",
      "status": "idea",
      "kind": "feature",
      "summary": "SECRETS-003, beside SECRETS-002 and without editing it: a documents-only baseline recorded from source, a contract with key custody and an adversarial review by a second commit author before any code, and code rows in crates/lys-secrets, ordered R3, then R4, then R5 to R8, that deliver SECRETS-002's R1 to R9 with counted acceptance legs, each with a runnable command and its exact result, a tested presenter check, anchored criterion searches, red-first tests, an injected clock and refusals that name their act, re-costed hours per row, and DIRECTORY-002, DIRECTORY-003 and DIRECTORY-006 R1 to R4 as blockers with runnable checks. The broker's home in lys is ADR-019, proposed as an amendment to ADR-001 and waiting on Tom's word; the lease is a broker record bounded by the lys-identity grant's window (ADR-020).",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-26T21:22:07+10:00",
        "quote": "The unlanded draft IDENTITY-003 under docs/design/identity/briefs does not land; that directory sits outside the gate. The broker's brief on main is the secrets cluster: docs/design/secrets/briefs/SECRETS-001.json (done) and SECRETS-002.json, R1 to R9 with SEC_ identifiers, anchored to ADR-001 (secrets sit behind a handle the door swaps for the credential), ADR-002 (the token revolver is the first consumer), ADR-003 and ADR-004.\n\nThis brief is SECRETS-003, a brief of its own beside SECRETS-002, so what has landed stays stable; SECRETS-002 is not edited. It adds, in docs/design/secrets/, what the draft carries and main lacks: a documents-only baseline row recording from source the revolver's next-account call site, every consumer of the account pool file, every credential path into a seat today classed as the login exception, a proxied credential or neither, and what lys/delegation/v1 and the sealed envelope can carry for a handle, a lease and a sealed record, by file and line; a contract row before any code stating the invariant, the handle, the lease with its atomic use step and retry outcome, the cancellation rule, the SpiceDB relations and the audit line fields, with an adversarial review recording every attack tried (a replayed or forged handle, a use spent or counted twice, an admitted call outliving the rule, a credential in a log, a sealed key read, a stale SpiceDB answer) and the clause defeating each; counted acceptance legs on every code row (each refusal leg, a forced race on a k-use lease admitting exactly k, a redaction canary with a positive control, kill and reopen at each append boundary, each in-flight outcome its own test); hours per row; dependencies on DIRECTORY-002, 003 and 006 blocking the code rows.\n\nSettled on main, not reopened: the seat's own login reaches its process at spawn as the one named exception (R5); OAuth refresh and spend caps are in scope (R4, R9); every point it records open stays open.\n\nBoundaries: documents only, no code; no credential value; lys-core and its published formats unchanged, a signed lease format only as a new version alongside with its own adversarial review; no engine structural; no row dispatched before the lead's review; the gate is scripts/design/gate.sh.\n\nAsk the lead, do not decide: whether the broker lives in lys (the draft's crates/lys-secrets) or in the cambium door SECRETS-002 names, which CN4 leaves open; whether the lease is a broker record or a signed format; whether the draft's hours stand. The brief records each as an open question with both readings.\n\nThe lead read the brief published at 4babcfe on 26 September and found the baseline R1 sound and holes in R2 to R8; every one of these corrections goes in, and R1 stays in this brief.\n\nBlockers a build can act on. SECRETS-003.json carries depends_on [] while DIRECTORY-006 uses that field: name the DIRECTORY-006 requirements this brief waits on as R1 to R4 of 006, never the whole of 006, whose R6 waits on 005; give each blocker a check a stranger can run, a file, a command or a landed commit to look for.\n\nThe order of the code rows. R3 to R8 do not block on each other today, yet R4 edits R3's lib.rs, R6 and R7 use R4's revocation states, and R5 and R8 build on R4's proxy: state the order R3, then R4, then R5 to R8, in each row's own Blocked by.\n\nThe CONFORMANCE 3.2 and 3.4 review blocks only at brief level: put it in R4's own Blocked by.\n\nKey custody. Nobody holds the store's encryption key: add a key-custody section to CONTRACT.md saying which key, where it is kept, how the store is unlocked and how the key is rotated; add R3 checks that the key is not in the store directory and that opening the store without it is refused by name.\n\nCanaries on disk. Every canary check also searches every file under the test's store and log directories, expecting zero matches, with a positive control that plants the value and finds it.\n\nThe audit line and the handle. The audit line carries a handle id or digest only, never the raw handle, since the transparency log can be read and replayed; the handle is stored hashed; the canary plants a handle value. The Handle section says how the proxy authenticates whoever presents a handle.\n\nPoints R2 keeps open that the code rows must settle: the audit line's signer and schema, and how a sealed record is bound to its name and owner, since sealed_envelope.rs:182 has an empty AAD; R3's store gains a refusal check for ciphertext swapped between two entries. Either the contract states a broker-internal shape, reviewed adversarially, or those rows stay blocked and say so.\n\nThe delegation's time window. ADR-020 says the signed delegation keeps it, v1 has no window, and DIRECTORY-006 R1 puts it in the lys-identity grant: name the grant's window as the limit, amend ADR-020 to match, and list which criteria still wait on it.\n\nADR-001 says the broker is built in Rust inside the door and ADR-019 moves it into lys with supersedes []: record ADR-019 as amending ADR-001 and fix design.json:54. ADR-001 is Tom's decision; the lead puts the move to him herself, and the brief says the amendment waits on his word.\n\nSEC_REVOKE_FRESHNESS has one fixed expected behaviour: R4 calls the landed DIRECTORY-006 permission check and asserts that behaviour and zero forwarded calls.\n\nSEC3_OAUTH_RACE asserts that the provider sees exactly one refresh for three simultaneous calls.\n\nThe criterion mapping is searchable: \"the row's report maps each criterion\" names the report path, or the criterion ids sit in the test names so a search proves the mapping.\n\nThe canary scope is countable: say how logs are captured, a tracing subscriber in tests/support; keep every secret byte in one redacting type; enforce that with an ast-grep rule or a counting test.\n\nFile lists. R4 and R5 list crates/lys-secrets/Cargo.toml and Cargo.lock; tests/support/mod.rs is listed on every row whose test doubles live there.\n\nTwo different identities are checked as two different commit authors in git log, and SEC3_STORE_RACE states its total case count.\n\nRoadmap row, ruled by the lead on 26 September 2026 at 21:07 after the re-fire ad15e87e published at 36cddea with RM-011, which HOME-008 already holds on brief/home/5898fcb0, and after translate took RM-012 at 20:52 under her 20:31 rule (the next id after main's highest and every open brief branch's): SECRETS-003's roadmap row is RM-013, appended after RM-012, and its brief and ledger name RM-013 wherever the rewrite at 36cddea said RM-011. Checked by Archie at 21:07 against main 1756688 and every open brief branch (brief/directory/7e9506d8, brief/home/24724e9e, 31aa723f, 5898fcb0, 7d3f3efb, c322f3e6, d388a9f7, de72f0a4, e35a7157, brief/secrets/604f95d0 and ad15e87e), none of which holds RM-012 or RM-013. ADR-019 and ADR-020 stay as the rewrite has them, ADR-019 proposed as an amendment to ADR-001 until Tom's word. The rewrite at 36cddea is otherwise the text to carry: its answers to the fifteen corrections stand and the author reopens none of the rounds of runs 604f95d0 and ad15e87e.\n\nThe lead read the re-fire published at 36cddea on 26 September at 21:13 and found R1 and R2 sound, C6 present (R2 and R3 keep only the SHA-256 digest, proved by SEC3_STORE_CANARY), and thirteen holes in R3 to R8; every one of these goes in before her tick, and the line numbers name SECRETS-003.json at 36cddea.\n\nK2, the presenter check is tested. SEC3_PROXY_REFUSALS (:158) gains three legs, a handle presented unsigned, a signature by a key other than the bound identity's, and a valid signed presentation replayed on a new call, with its asserted count raised; R6, R7 and R8 (:234, :268, :303) gain the same legs. R2's acceptance (:78) also requires the contract to state the unsigned refusal, the freshness limit on a presentation's timestamp, and that the check uses lys-core's verify_attestation_by_signer, since plain verify_attestation never compares the signer (attestation/mod.rs:51-63).\n\nK3, anchored searches. rg -c 's002_r1_ac1' also matches ac10, ac11 and ac12, so R4's criterion 1 would pass once R3 lands: every such search is anchored, for example rg -c 'fn \\w*s002_r1_ac1(_|\\b)' (:120, :166, :204, :240, :274, :308, :350).\n\nK4, test names are snake_case, so a name holding SEC3_STORE_REFUSALS fails clippy -D warnings and the lint is not silenced: every search looks for the lower-case form, sec3_store_refusals, sec_revoke_freshness and the rest (:121, :164, :167, :205, :241, :275, :309).\n\nK5, SEC3_SECRET_SCOPE lets only store.rs read raw secret bytes (:117) and Zeroizing only in secret.rs, yet key_rotation.rs, handle.rs and audit.rs all need the bytes: either name every R3 file that does, or route all of them through store.rs.\n\nK6, red first. Each code row's first commit adds its tests, and cargo test at that commit fails naming each leg.\n\nK7, no wall clock. SEC3_PROXY_INFLIGHT's slow upstream double (:162) becomes a double held until the test releases it; the window and expiry legs (:111, :199, :200) use an injected clock, as DIRECTORY-006's GRANT_EXPIRY does; no sleeps.\n\nK8, every refusal names the act that answers it and its leg asserts that act: StoreKeyMissing says supply the store key file at start, StoreKeyRetired says use the current key, every-account-rested says bring an account back, and so on through :109, :111, :113, :158, :198, :234, :268 and :303.\n\nK9, every counted leg gets a runnable command and an exact expected result, for example cargo test -p lys-secrets --all-features --test store_leases sec3_store_ -- --include-ignored printing N passed, 0 failed, 0 ignored. R2's document checks (:76-86) either become heading or id searches with expected counts, or say plainly that the lead checks them by reading.\n\nK10, \"planted in the store\" (:116 and the canary legs of R4 to R8) contradicts digest-only storage: the canary handle is issued through the broker and presented.\n\nK11, a leg for R3's retry rule (:109): the same operation id returns the first outcome with the use count still 1, and the same operation id under a different handle or identity is refused.\n\nK12, rollback. The sealed binding stops a swap between entries but not someone restoring an older ciphertext of the same entry: rollback joins the attack list (:85), and either a version or sequence is bound into the sealed prefix with a refusal leg at :111, or the contract records it out of scope.\n\nK13, canary searches also look for lower- and upper-case hex and for standard and URL-safe base64 (:112, :114, :116 and the rest), so a hex-written key or a Basic auth header cannot pass.\n\nK14, R2's handle clause (:78) sets at least 32 bytes from the OS secure random source, because an unsalted SHA-256 of a short handle can be brute-forced; the handle id is generated separately and never derived from the handle; one assertion checks that no audit line carries the handle's digest, which :82 forbids and nothing tested.\n\nThree suggestions from the same review, not blockers, which the author takes or records as not taken with a reason: SEC3_STORE_KEY also searches the log directory; the contract covers audit-key rotation and what happens if the audit key is compromised; blocker check (a) checks each path separately.\n\nThe re-fire 18b96d68 was taken back before it published so these corrections ride with the RM-013 ruling in one run, at main 1756688. The tick still waits on Tom's word on ADR-019.",
        "context": "The secrets broker card on the Lys board: the lead's reviews of the SECRETS-003 drafts published at 4babcfe and at 36cddea, with the fifteen corrections to R2 to R8 carried as the rewrite at 36cddea answered them, the thirteen further corrections K2 to K14 to R3 to R8, and the ruling that this row is RM-013, surveyed against lys main at 1756688. The lead's answers: SECRETS-003 keeps ADR-019 (proposed, amending ADR-001, waiting on Tom's word) and ADR-020 (decided); R5's OAuth refusals gain the same three presenter legs as R4, R6, R7 and R8; a stale presentation gets its own refusal leg on the injected clock in every row that carries a presenter check, so R4 to R8 assert 10, 9, 8, 10 and 8 refusals; and a sequence is bound into the sealed prefix, so an older ciphertext of an entry restored in place is refused by name."
      },
      "links": {
        "cluster": "secrets",
        "decisions": [
          "ADR-001",
          "ADR-002",
          "ADR-003",
          "ADR-004",
          "ADR-019",
          "ADR-020"
        ],
        "briefs": [
          "SECRETS-003"
        ],
        "commits": []
      },
      "depends_on": [
        "RM-001",
        "RM-002"
      ],
      "notes": "Takes the next id after translate's RM-012; the id the rewrite at 36cddea carried is HOME-008's. Further units, not written: Tom's word on ADR-019 (the broker lives in lys, amending ADR-001); SECRETS-003 R1: BASELINE.md recorded from source; SECRETS-003 R2: CONTRACT.md with key custody, and the adversarial review by a second commit author; SECRETS-003 R3: store, redacting type, handles and leases in crates/lys-secrets; SECRETS-003 R4: proxy, rotation and cancellation rule; SECRETS-003 R5 to R8: OAuth refresh, login at spawn, sealed records, next account; A later Lys brief for the screen and API legs of SEC_USE_NOT_LEND, SEC_PEOPLE_ONLY and SEC_REVOKE_STATES; A lys-core sealed-envelope version with AAD beside v1, if the contract cannot bind a sealed record otherwise; Add ast-grep and clippy pedantic legs to .land/gates.sh for crates/lys-secrets; Audit-key rotation and the response to a compromised audit key."
    }
  ]
}
