{
  "project": "lys",
  "updated": "2026-09-26",
  "items": [
    {
      "id": "RM-001",
      "title": "Stand up the identity directory with every grant rooted in a person",
      "status": "briefed",
      "kind": "feature",
      "summary": "The standalone identity directory: sign-in through the platform's own Rauthy fork, two upstream providers linked to one person, a signed authoritative identity, and the standalone screen journey, all installed against one PostgreSQL database. It is the root every later grant in the platform hangs from.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "The identity platform statement (docs/design/identity/STATEMENT-2026-09-22.md) and IDENTITY-001; the cluster and its five briefs reached main in 55933549."
      },
      "links": {
        "cluster": "directory",
        "decisions": [
          "ADR-003",
          "ADR-004",
          "ADR-005",
          "ADR-007",
          "ADR-008",
          "ADR-009",
          "ADR-010",
          "ADR-011"
        ],
        "briefs": [
          "DIRECTORY-001",
          "DIRECTORY-002",
          "DIRECTORY-003",
          "DIRECTORY-004",
          "DIRECTORY-005"
        ],
        "commits": []
      },
      "depends_on": [],
      "notes": "The briefs build in order: DIRECTORY-002 before 003, 003 before 004 and 005. Each card runs through the board (brief_card, then card_build_v3)."
    },
    {
      "id": "RM-002",
      "title": "Build the secrets broker so agents hold handles, never credentials",
      "status": "briefed",
      "kind": "feature",
      "summary": "A broker that keeps credentials behind handles: the door swaps a handle for the credential at use, the token revolver is the first consumer, and rotation and sealing happen without any agent ever seeing a secret.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "The secrets cluster (docs/design/secrets) and its two briefs, written to ADR-001 and ADR-002."
      },
      "links": {
        "cluster": "secrets",
        "decisions": [
          "ADR-001",
          "ADR-002",
          "ADR-003",
          "ADR-004"
        ],
        "briefs": [
          "SECRETS-001",
          "SECRETS-002"
        ],
        "commits": []
      },
      "depends_on": [],
      "notes": ""
    },
    {
      "id": "RM-003",
      "title": "Move the Rauthy fork onto the next hardened upstream release",
      "status": "idea",
      "kind": "fix",
      "summary": "Rebase the platform's own Rauthy fork (ADR-009) onto the next upstream release that upstream tags as a release and whose changelog carries security or hardening fixes. Our commits stay on top of the upstream tag, unsquashed and unaltered; the pin moves to the rebased head; the fork builds and passes its own suite; the standalone install comes up from the rebased build against the existing database with no migration by hand; the provider-link behaviour of DIRECTORY-004 still holds.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "Written ahead of its trigger from the Lys board card of the same name. Upstream was still v0.36.2 on 2026-09-23 and the fork is pinned to that exact version. The trigger is a fact recorded in the pull request (the upstream tag, its commit, its changelog entry), never a judgement."
      },
      "links": {
        "cluster": "rauthy-rebase",
        "decisions": [
          "ADR-009"
        ],
        "briefs": [],
        "commits": []
      },
      "depends_on": [
        "RM-001"
      ],
      "notes": "The card is built only when the trigger is met. It needs DIRECTORY-004 (the provider link commit the rebase must carry) and DIRECTORY-002 (the standalone install it must prove) landed first. If the rebase cannot carry a fork commit cleanly the card stops and reports the conflicting upstream commit rather than rewriting ours. Nothing in Cambium or Aion changes; no production node is touched; Tom's three acts stay Tom's."
    },
    {
      "id": "RM-004",
      "title": "Rewrite the decision quotes as plain statements in the team's words",
      "status": "idea",
      "kind": "design",
      "summary": "docs/design/decisions.json holds eight decisions (ADR-001 to ADR-008) whose quote field carries a person's words. Each quote becomes a plain statement of the decision in the team's words, with the decision, context, consequences and sources kept exactly as they are. ADR-009 and ADR-010 already read that way and are the pattern.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "The canon rule of 24 September: repository canon holds no person's words and nothing personal. ADR-009 and ADR-010 (lys 586ef5f4) were written to it."
      },
      "links": {
        "cluster": "decisions-words",
        "decisions": [
          "ADR-001",
          "ADR-002",
          "ADR-003",
          "ADR-004",
          "ADR-005",
          "ADR-006",
          "ADR-007",
          "ADR-008"
        ],
        "briefs": [],
        "commits": []
      },
      "depends_on": [],
      "notes": "Documents only. scripts/design/gate.sh must stay green: decisions.json validates against decisions.schema.json, which keeps the quote field required."
    },
    {
      "id": "RM-005",
      "title": "Give a session a home: its context record, kept byte for byte",
      "status": "briefed",
      "kind": "feature",
      "summary": "The home (identity platform steps 4 and 5): a running session's transcript captured into a home byte for byte, indexed for reading back, with the record of what the agent was given as well as what it did. Lanterns, translation and forks stand on it.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-25",
        "quote": "",
        "context": "CONTEXT-001 (docs/design/identity/briefs) and the home cluster, landed on main with lys PR 8 (HOME-001, design.json, stories, checklist)."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-001",
          "ADR-003",
          "ADR-004"
        ],
        "briefs": [
          "HOME-001"
        ],
        "commits": [
          "7b47115cb08b02c246ff19e3330e5f825ed68163"
        ]
      },
      "depends_on": [
        "RM-001"
      ],
      "notes": "Owned by Archie. The home cluster and HOME-001 landed on main with PR 8; the cached-index rule (lys-home Index::from_cached) is in that brief. Next: HOME-001 dispatched through the board."
    },
    {
      "id": "RM-006",
      "title": "Say what a compaction could not keep, and prove the original is all still there",
      "status": "idea",
      "kind": "feature",
      "summary": "When a compacted Claude Code session is imported into its home, each compaction entry is followed by a lys.loss entry naming the span it summarised as ids, counts and hashes, the hash of every stored part is kept beside the session, a lys-home subcommand lists each compaction and checks that every summarised entry and block is still held, and the compaction renders for Claude Code in the shape the measured version writes. Proved on a fixture and on one real compacted session.",
      "provenance": {
        "requested_by": "tom",
        "date": "2026-09-26T06:36:44+10:00",
        "quote": "A compaction is the first derived record, because the harness itself produces it: Claude Code writes a summary record and keeps everything before it in its own file, and the importer already turns that record into Pi's compaction entry with its summary and first kept entry. What the home does not yet say is what the compaction could not keep, and nothing yet proves, on a compacted session, that the original is all still there. This card adds both. When a compaction entry enters a session, on import of a Claude Code file that carries a summary record, the importer appends directly after it a lys.loss custom entry that points at the compaction by entry id and names what fell outside the kept range: the first and last entry id of the span it summarises, from the root or the previous compaction to the entry before the first kept one, and the counts of entries, messages, tool calls, tool results and blocks in that span with their bytes, and the tokensBefore the harness reported; it names entry ids and hashes and never a word of content. The summarised entries are not touched: they stay on the tree, their blocks stay in the store, and the context path is Pi's reading as today, the compaction first, then the kept entries. A lys-home subcommand lists a session's compactions: for each, the compaction entry, its loss entry, and a check that every entry in the summarised span is still readable by id and every block it references is still held, printed as a JSON report of ids, counts and hashes. A render for Claude Code writes the summary record and the kept entries as R4 does now, and never the loss entry, so the harness sees what it expects while the home keeps the whole. Acceptance is that a fixture Claude Code file with a summary record imports to a session whose compaction entry is followed by a loss entry naming that compaction, the span's first and last ids and counts that match the fixture; that every entry before the first kept one is still on the tree with its blocks present, checked by the listing; that the context path is the compaction followed by the kept entries and nothing from the span; that the rendered file holds the summary record and the kept entries and no loss line; that importing the same fixture twice into two homes gives byte-identical loss entries apart from ids and timestamps; that a summary record whose first kept entry is not on record is refused by uuid as the importer refuses an unknown parent today; and that one real compacted Claude Code session is imported on this Mac and its listing recorded in a proof document as counts and hashes only. Not in scope: the home compacting a session itself; translation, which is stage 4b and its own card; changing what Claude Code writes; the handover letter, which is HOME-001 R12. Filed by Archie on Tom's roadmap stage 4 of 22 September 2026, a compaction is stored beside its original, points at it and says what it could not keep, the original never replaced, and on home DESIGN P1, P2 and P7 and HOME-001 R3 and R4 at lys main 0073b966, on 26 September 2026.",
        "context": "The card's words for roadmap stage 4 (a compaction is stored beside its original, points at it and says what it could not keep), on the home design's P1, P2 and P7 and HOME-001 R3 and R4, with the lead's answers to the card's survey (the compact_boundary pair, the render shape, the block rows, the keep-nothing compaction)."
      },
      "links": {
        "cluster": "home",
        "decisions": [
          "ADR-012",
          "ADR-013"
        ],
        "briefs": [
          "HOME-002"
        ],
        "commits": []
      },
      "depends_on": [
        "RM-005"
      ],
      "notes": "Further units, not written: LOSS-ACCOUNT.md: the written loss account across render and compaction. Open for the lead: a compact_boundary whose isCompactSummary record never arrives before the end of the file; whether the compaction listing exits non-zero when an entry or block is missing."
    }
  ]
}
